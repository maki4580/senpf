データ連携仕様書にこれらを記載すべき理由と、具体的にどのような項目を書けばよいかを整理しました。
------------------------------
## 💡 なぜデータ連携仕様書に書くべきなのか？

   1. 「タイムアウトの連鎖」によるシステムダウンを防ぐため
   * 例：スナップショットのREST送信がタイムアウト（30秒）する前に、手前の画像解析パイプライン側のタイムアウト（例：10秒）が先に切れてしまうと、接続エラーが多発してシステム全体がハングアップします。
   2. リトライのバースト（二次災害）を防ぐため
   * クラウドが重いときに、デバイス側が「ノンストップで即時リトライ」を繰り返すと、クラウドをさらに攻撃することになり（DoS状態）、復旧を遅らせます。
   3. テスターが「テストケース」を作れるようにするため
   * 「3回リトライしてダメならエラーアラームが上がるか」をテストするためには、仕様書に回数が明記されている必要があります。
   
------------------------------
## 📝 今回のIF（IF-W01）で追加すべきパラメータ項目
現在の「連携パラメータ」の表に、以下の項目を追加、または具体化するのがおすすめです。
## 1. REST（HTTPS）通信のタイムアウトとリトライ
スナップショットJPEGを送信するREST APIは、MQTTに比べてデータ量が大きく、失敗しやすいため厳密に定義します。

* 接続タイムアウト（Connect Timeout）: クラウドとのセッションが確立するまでの上限（例: 5〜10秒）。網断の検知を早くするために短めにします。
* 応答タイムアウト（Read Timeout）: リクエスト送信後、データが返ってくるまでの上限（現在の表にある「30秒」がこれにあたります）。
* リトライ回数: 何回失敗したら諦めるか（例: 3回）。
* リトライ間隔（バックオフ）: 連続で送るか、間隔をあけるか。「指数バックオフ（Exponential Backoff）」（1回目：2秒、2回目：4秒、3回目：8秒…）や、少し時間をずらす「Jitter」を入れる設計にするかを明記します。

## 2. MQTT通信のリトライとライフサイクル
MQTTはプロトコル自体にKeepaliveや再接続の仕組みがありますが、薄膜デバイス（thin-edge.io）がどう振る舞うかを定義します。

* 再接続リトライ間隔: MQTTブローカー（Cumulocity）を切断された後、再接続を試みる間隔。ここも通常は指数バックオフ（最大値を15分などに固定）を設定します。
* QoS（Quality of Service）: すでに「at-least-once（D16）」とあるので QoS 1 であることをパラメータ表にも明記します。

------------------------------
## 📊 パラメータ表のアップデート案
既存の表にこれらを組み込むと、以下のような形になります。

| パラメータ | 意味 | 初期値 | 確定条件・備考 |
|---|---|---|---|
| MQTT keepalive | 接続維持確認の間隔 | 60秒 | thin-edge/ブローカー設定。 |
| MQTT QoS | メッセージの到達保証レベル | QoS 1 | At-Least-Once（D16）を満たすための必須設定。 |
| MQTT 再接続バックオフ | 網復旧時のMQTT再接続試行の間隔 | 初期5秒〜最大15分 | 指数バックオフを適用し、クラウド復旧時のバーストを抑止。 |
| ストア&フォワードキュー容量 | 網断中に保持するメッセージ量の上限 | 要確定(A7) | 「想定最長断時間×バースト時レート」から逆算。 |
| REST添付タイムアウト | スナップショット添付の応答待ち上限 | 30秒 | 添付サイズ上限(A8)と連動（Read Timeout）。 |
| REST 接続タイムアウト | スナップショット添付の接続確立上限 | 10秒 | 網断を早期に検知し、キューへ退避させるため短めに設定（Connect Timeout）。 |
| REST リトライ回数 | スナップショット添付の最大試行回数 | 3回 | 3回失敗した場合は、添付スプールに保持したまま一定時間（例：15分）送信を保留。 |
| c8y_RequiredAvailability... | 受信なし時に接続断と判定する時間 | 死活間隔×2を分単位 | 単位は分のための丸め（既記述の通り）。 |

------------------------------
## 💡 付け加える際のアドバイス
もし現時点で「何秒、何回にするか」が決まっていなくても、「要確定（A9）」 のように仮の初期値を入れつつ、確定条件の列に「画像解析側のタイムアウト値より大きくすること」や「PoC時のネットワーク瞬断頻度を踏まえてチューニング」と書いておくだけで、設計書としての価値が跳ね上がります。
もし、これらのタイムアウト値を決めるにあたって、手前のシステム（画像解析パイプライン）やネットワーク環境の制約で悩んでいる部分があれば、一緒に数値を検討しましょう！

