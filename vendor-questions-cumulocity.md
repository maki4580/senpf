# Cumulocity社への確認事項一覧

- 作成日: 2026-08-02
- 目的: 技術調査([research-findings-2026-08-01.md](research-findings-2026-08-01.md) / [research-findings-2026-08-02.md](research-findings-2026-08-02.md))で**公開ドキュメントからは確定できなかった事項**を、ベンダー確認用に集約する
- 使い方: 各項目は「質問(そのまま送れる文面)/ なぜ必要か / 現時点で判明していること / 回答が否だった場合の影響」で構成。**§2の4件は契約・PoC着手前に回答が必要**
- 前提の共有: 確認依頼時には**実トポロジー(拠点分散・閉域網・中央集約1インスタンス・カメラ1000台オーダー)を開示する**こと(design-decisions.md 転換2の方針)
- 関連文書: [data-integration-spec.md](data-integration-spec.md)(§5 未確定事項一覧 N6/N8)、[design-decisions.md](design-decisions.md)(D17)

---

## 1. 確認事項サマリー

| # | 確認事項 | 優先度 | 止まっている判断 | 期限の目安 |
|---|---|---|---|---|
| V1 | **Edge 1インスタンスへの多拠点収容のライセンス・サポート可否** | **最重要** | D17(中央集約配置)の成否そのもの | 契約・PoC前 |
| V2 | **多拠点合算スループットの公式サイジング根拠** | **最重要** | D17成立条件(1)・VM1調達仕様 | 契約・PoC前 |
| V3 | **採用すべきEdgeリリース版と提供形態・サポート期限** | **最重要** | 調達計画・全設計の前提バージョン | 契約・PoC前 |
| V4 | **Edgeのバックアップ/リストアの公式手順とHA構成** | **最重要** | D17成立条件(2)・N9 | 契約・PoC前 |
| V5 | Notification 2.0 の Edge 2026 での提供状況と運用パラメータ | 高 | IF-H13a(通知)の詳細設計 | 設計フェーズ |
| V6 | Streaming Analytics(Apama)の商用ライセンス条件 | 高 | IF-H13aのフォールバック可否 | 設計フェーズ |
| V7 | 外部OIDC(Keycloak)SSOのEdge対応可否 | 高 | IF-P04・D8(IdP同梱方針) | 設計フェーズ |
| V8 | certificate-authority機能のEdge対応可否とGA時期 | 高 | IF-P01(証明書ライフサイクル)の方式選定 | 設計フェーズ |
| V9 | Notification 2.0 のRBACバイパスに対する回避策 | 高 | D17の拠点分離(権限設計)の成立 | 設計フェーズ |
| V10 | Inventoryロールの大規模時(2000件超)の挙動 | 中 | IF-H06のページネーション設計 | 設計フェーズ |
| V11 | DataHub の Edge 提供条件(2025「Optional」/2026「Professional Services経由」の差) | 中 | N7(オフロード自作 vs DataHub) | 設計フェーズ |
| V12 | retention rules の Edge における変更可能範囲 | 中 | §3.10(保持期限設計) | 設計フェーズ |
| V13 | device token 方式の廃止予定と後継 | 中 | §3.5(REST経路の認証) | 設計フェーズ |
| V14 | MQTTレート制限の具体値と超過時挙動 | 中 | §3.5(バースト時の切断リスク) | PoC設計時 |
| V15 | thin-edge.io の child device 数百台規模での実績・上限 | 中 | D16/A7 | PoC設計時 |
| V16 | イベント添付のサイズ上限設定変更のEdgeでの可否 | 低 | A8(スナップショット設計) | 設計フェーズ |
| **V17** | **そもそもEdgeが適切な製品か(オンプレCoreとの比較)** | **最重要** | 製品選定そのもの。D17の前提 | 契約・PoC前 |
| V18 | External ID・MQTT ClientId・トピックの文字種/長さ制約 | 高 | §4.1 識別子規約(全IF横断) | 設計フェーズ |
| V19 | 発報中Alarmのseverity昇格の正しい方法 | 中 | §3.21(通知の重大度伝達) | 設計フェーズ |
| V20 | thin-edge.io と Edge のバージョン互換・商用サポート有無 | 中 | D16(標準エージェント採用の裏付け) | 契約前 |
| V21 | MQTT 16KiB上限のEdge適用範囲・Messaging Service障害時挙動 | 中 | §3.5・§3.21 | 設計フェーズ |

---

## 2. 最重要(契約・PoC着手前に必須)

### V1. Edge 1インスタンスへの多拠点収容 — ライセンス・サポート可否

**質問**:
> 弊社は顧客の閉域WANを前提に、**複数拠点(初期数拠点〜10、将来50拠点規模)の全デバイスを本社サーバールームの Cumulocity Edge 1インスタンスに集約**する構成を検討しています。
> 1. この「中央サーバー的な利用」はライセンス上・サポート上許容されますか。Edgeの想定利用形態(1拠点1インスタンス)から外れる場合、制約や追加条件をご教示ください。
> 2. Edgeライセンスに**デバイス数の上限**はありますか。ある場合、その数え方(ゲートウェイのみか、child deviceを含むか)をご教示ください。**カメラをchild deviceとして登録するため、総数は1000台オーダー(将来的に50拠点×数百台)**になります。
> 3. Edge 2025のライセンスキーが**ドメイン名に紐づく**と理解していますが、この方式で上記構成に支障はありませんか。
> 4. 参考として、拠点ごとにEdgeを設置する構成(10〜50インスタンス)との**3年TCO比較見積り**をいただけますか。

**なぜ必要か**: D17(中央集約配置への転換)の成立条件(3)。**本件の配置設計そのものの成否を決める**。否であれば拠点ごとインスタンス配置(転換1の原案)へ全面的に戻す必要があり、データ連携仕様の前提(単一テナント・拠点プレフィックス付きExternal ID・グループによる拠点分離)がすべて変わる。

**現時点で判明していること**:
- Edgeライセンスは**ドメイン名に紐づき、ライセンスファイルはプロダクトサポートへの申請で入手**(Edge CRの `licenseKey` フィールド。2025 K8s版ドキュメント)
- Edgeは**シングルテナント**(Cumulocity Coreのみマルチテナント)
- 収容形態の可否・デバイス数上限に関する記載は公開ドキュメントに**見当たらない**

**否だった場合の影響**: D17を撤回し拠点分散配置へ。data-integration-spec.md の §1.0・§4.1(識別子規約)・§3.10(オフロードキー)・IF-W01〜W06の経路定義を全面改訂。

---

### V2. 多拠点合算スループットの公式サイジング根拠

**質問**:
> Edgeの受信性能について、公式のサイジング根拠をご提示いただけますか。
> 1. Edge 2025(Kubernetes版)のドキュメントには「Vertical scalability: Yes, limited to appr. **100 tps per CPU core**」との記載がありますが、**Edge 2026のドキュメントには同記載が見当たりません**。2026版における公式のサイジング指標をご教示ください。
> 2. 弊社の想定負荷は以下です。この負荷を単一Edgeで受けるために必要なCPU/メモリ/ディスク構成をご提示いただけますか。
>    - 定常: カメラ死活監視の Measurement — 50拠点 × 300台 ÷ 60秒間隔 ≒ **250 msg/s**
>    - バースト: AI検知イベント(Event)+ 各イベントへのスナップショットJPEG添付(REST)
>    - 加えて: 日次オフロードバッチによる大量 REST 取得、Web UI/アプリからの参照
> 3. **MongoDB(Operational Store)の容量見積り**の考え方をご教示ください。特に**イベント添付バイナリが同一MongoDBに格納される**ため、画像サイズ×イベント件数がDB容量に直結すると理解しています。

**なぜ必要か**: D17成立条件(1)。VM1の調達仕様(CPU/メモリ/ディスク)が決まらない。PoCで実測する場合も、比較対象となる上限値がなければ「収まった/収まらない」を判定できない。

**現時点で判明していること**:
- 「約100tps/CPUコア・水平スケール不可」は**2025 K8s版ドキュメントに実在**(検証確定)。2026版introページには同記載がない(**版依存**)
- ベースハードウェア要件: 2025 K8s版で **6 x86-64コア/10GB RAM/100GBディスク**、CPUは**AVX命令対応必須**(MongoDB要件)。2026版では **8コア/16GB/150GB** との情報あり(未検証)
- オプション加算: Messaging Service **+2コア/+4GB/+15GB**、DataHub **+10コア/+16GB/+100GB**

**否(提示不可)だった場合の影響**: PoC実測を唯一の根拠とせざるを得ず、**PoC失敗時のリカバリ期間が読めない**リスクを受容することになる。

---

### V3. 採用すべきEdgeリリース版・提供形態・サポート期限

**質問**:
> 1. 2026年8月時点で**新規導入に推奨されるEdgeのリリース版**はどれですか(2025 / 2026)。
> 2. 各リリースの**提供形態**をご教示ください。Edge 2025は「シングルノードKubernetesクラスタのみ・Kubernetes 1.32.x のみサポート」と理解していますが、2026版の対応K8sバージョンと、**OVA(仮想アプライアンス)形態の提供有無**をご教示ください(弊社の以前の理解ではOVA提供がありましたが、現行ドキュメントでは確認できませんでした)。
> 3. K8sクラスタは **c8yedge ツールによるプロビジョニング**と**自前構築**のどちらが推奨ですか。
> 4. 各リリースの**サポート期限(EOL)とリリースサイクル**、およびバージョンアップ時の**ダウンタイムとデータ移行**の想定をご教示ください。

**なぜ必要か**: 調達計画(サーバー仕様・K8s運用体制の要否)に直結。加えて**Notification 2.0は2025で新規追加された機能**(2024には無い)であり、採用版が通知方式(IF-H13a)の成否を決める。弊社チームはKubernetes運用経験が薄く(D5)、提供形態は運用体制の前提を変える。

**現時点で判明していること**:
- Edge 2025: シングルノードK8sのみ・K8s 1.32.x のみ(検証確定)
- Edge 2026: シングルノードK8sクラスタのみテスト・サポート対象。K8s 1.34.x サポートとの情報あり(未検証)
- **Notification 2.0(Messaging Service)は2025で新規追加**。2024のEdge CRには当該フィールドが存在しない(検証確定)
- サポート期限・リリースサイクルは未調査

**否(K8s必須)だった場合の影響**: D5(最小構成スタート・k8s自前運用は避ける)との整合を取るため、K8s運用の外部委託またはチーム体制強化がコスト計画に加わる。

---

### V4. バックアップ/リストアの公式手順とHA構成

**質問**:
> 中央集約構成ではEdgeが全拠点のSPOFになるため、可用性設計の確証が必要です。
> 1. **Edge の公式なバックアップ/リストア手順**をご教示ください(対象: Operational Store(MongoDB)、Inventory、設定、イベント添付バイナリ)。オンライン取得の可否、所要時間の目安、リストア時のダウンタイムを含めてお願いします。
> 2. 「EdgeのHA能力は下位の仮想化基盤に依存し、サーバー障害は一時的なダウンタイムにつながり得る」と理解しています。**推奨される仮想化基盤HA構成**と、その構成での**実効的なRTO/RPO**の目安をご教示ください。
> 3. Edge停止中にデバイス(thin-edge.io)側で保持されるデータの**想定最大保持期間・容量**について、推奨値はありますか。

**なぜ必要か**: D17成立条件(2)。中央集約により全拠点が同時に影響を受けるため、RPO/RTO目標(N9)が定まらないと可用性設計書が書けない。また「Edge停止中も現場の録画・検知は継続する」構成の前提(architecture-camera-monitoring.md §7)が成立するかの確認も兼ねる。

**現時点で判明していること**:
- 「HA capabilities depend on the underlying virtualization technology, server failure could lead to temporary downtime」は公式記載(検証確定)
- Edge自体のクラスタ構成・水平スケールは不可(検証確定)
- 具体的なバックアップ手順は未調査

---

## 3. 機能提供可否(設計の前提)

### V5. Notification 2.0 の Edge 2026 での提供状況と運用パラメータ

**質問**:
> 弊社は Alarm 通知の実装手段として **Notification 2.0 の購読型コンシューマ**を第一候補としています。
> 1. Edge **2026** でも Messaging Service による Notification 2.0 は提供されますか(2025での提供は確認済み)。有効化手順とリソース要件に変更はありますか。
> 2. **受信トークン(consumer token)の有効期限の既定値**と、変更可否をご教示ください。
> 3. コンシューマが停止している間の**購読滞留(バックログ)の上限**はいくつですか。**上限を超過した場合の挙動**(古いものから破棄されるか、書き込み側にエラーを返すか)と、**超過を検知する手段**をご教示ください。
> 4. Notification 2.0 のバックログがクォータ上限に達した際、対応する書き込みAPI(`POST /measurement/measurements` 等)が **HTTP 500 を返す一方で Operational Store には書き込まれる**ケースがあると理解しています。この挙動の正確な条件をご教示ください(単純なリトライが重複を生むため、リトライ設計に必要です)。

**なぜ必要か**: IF-H13a の詳細設計(トークン更新のリードタイム、滞留上限超過時の欠落検知)が決まらない。特に4点目は§4.5のリトライ方針に直結する。

**現時点で判明していること**:
- **Edge 2025 では利用可能**(`spec.messagingService.enabled=true`、Pulsarベース、+2コア/+4GB/+15GB/PV×3)— 検証確定
- 配信保証は **at-least-once・順序保証・要ack**(公式API仕様)— 検証確定
- 通知メッセージに**製品側の重複排除キーは付与されず、重複排除は消費側の責務**(未検証・引用あり)
- トークン有効期限・滞留上限は**公開ドキュメントに記載を確認できず**

---

### V6. Streaming Analytics(Apama)の商用ライセンス条件

**質問**:
> Edge 2025のドキュメントでは Streaming Analytics(Analytics Builder含む)が **"Included"** と記載されていますが、これは**デプロイ上の同梱**を意味するものと理解しています。
> 1. **商用ライセンス上、追加費用なしで利用可能**でしょうか。追加ライセンスが必要な場合はその条件をご教示ください。
> 2. EPL(Event Processing Language)によるカスタムルール作成は上記に含まれますか。それとも Analytics Builder(GUI)のみですか。
> 3. **Smart Rules** の利用条件も併せてご教示ください。

**なぜ必要か**: IF-H13a(通知)のフォールバック手段の可否。および Event→Alarm 昇格ロジック(D7)の実装先の選択。追加ライセンスが必要なら、昇格・通知ロジックはすべて自作コンシューマ側に寄せる設計判断となる。

**現時点で判明していること**:
- Edge 2025 の機能表で Streaming Analytics は **"Included"**(検証確定)。ただし同表の "Included" はデプロイ/インストール上の位置づけであり、**課金条件はドキュメントの範囲外**
- Edge operator は Apama・Smart Rules・OPCUA Management Server を既定マイクロサービスとして展開(未検証・引用あり)
- 2026版ドキュメント基準の先行調査では「Edge に Streaming Analytics が Included」との主張が**反証**されており(1-2)、**版差または表記差の可能性**がある

---

### V7. 外部OIDC(Keycloak)SSO の Edge 対応可否

**質問**:
> 弊社は Keycloak を IdP として、基盤UI・案件アプリ・Cumulocity Web UI のSSOを実現する設計です。
> 1. **Cumulocity Edge で外部OIDCプロバイダ(Keycloak)によるSSOは利用可能**ですか。SSOのドキュメントにはEdgeに関する記載がなく、Edge カスタムリソースにもSSO関連の設定項目が見当たりませんでした。
> 2. 利用可能な場合、**Edge環境での設定手順**(特にオンプレ設置で前提となる **domain-based tenant resolution** の構成方法)をご教示ください。
> 3. 利用不可の場合、**推奨される代替**をご教示ください(Cumulocityローカルユーザー管理での運用、リバースプロキシによる前段認証の可否など)。
> 4. Keycloakレルムの**署名鍵はRSAのみ対応・楕円曲線(EC)鍵は非対応**という理解で正しいですか。

**なぜ必要か**: IF-P04。D8(IdPを初期同梱・全アセット共通SSO)は基盤の設計方針の一つであり、Cumulocity Web UI(IF-H16)と保守アクセス(IF-M01)がSSOに乗らない場合、ローカルユーザー管理の二重運用が恒久化する。

**現時点で判明していること**:
- SSO は **OAuth2 認可コードグラント+JWT のみ・SAML非対応**(検証確定)
- **Keycloak は Azure AD と並ぶ明示サポートプロバイダ**。グローバルログアウトは Keycloak 12.0.0以上(検証確定)
- オンプレ設置では **domain-based tenant resolution の正しい構成がSSOの前提条件**(検証確定)
- **SSOドキュメントにEdge固有の記載が一切ないことも検証で確定**(=対応可否は不明。不在の証明ではない)
- 署名鍵RSA限定は未検証(引用あり)

---

### V8. certificate-authority 機能の Edge 対応可否と GA 時期

**質問**:
> デバイス(thin-edge.io ゲートウェイ)の X.509 証明書ライフサイクル管理について。
> 1. **Cumulocity certificate-authority 機能(EST ベース)は Edge で利用可能**ですか。ドキュメントに Edge への言及が見当たりませんでした。
> 2. 同機能は thin-edge.io 1.5.0 のリリースノート時点で **PUBLIC PREVIEW** と記載されています。**2026年8月時点のステータス(GA済みか)** と、**GA予定時期**をご教示ください。
> 3. テナントでの**有効化手順**(サポート依頼が必要か)をご教示ください。
> 4. 利用できない場合、**閉域環境での推奨代替**をご教示ください(自前CAをtrusted certificateとして登録する従来方式での、証明書更新の運用推奨を含む)。
> 5. デバイス証明書の**失効(CRL)運用**について、拠点撤収・機器盗難時の推奨手順をご教示ください。

**なぜ必要か**: IF-P01。50拠点規模で証明書の発行・更新・失効を手作業で回すことは不可能であり、自動化方式が決まらないとプロビジョニング(IF-P02)の設計が完了しない。

**現時点で判明していること**:
- thin-edge.io 1.5.0(2025-05-05)が **CA機能によるenrollment(デバイス固有ワンタイムパスワード)+自動証明書更新サービス+PKCS#11/HSM対応**を追加(検証確定)
- **1.5.0時点でCumulocity側CA機能は PUBLIC PREVIEW**、テナント有効化にサポート依頼が必要(検証確定)
- EST(RFC 7030)ベースで `/simpleenroll`・`/simplereenroll`(未検証・引用あり)
- デバイス証明書1年・テナントCA 3年で毎年自動更新・**CAはテナントに1つのみ**(未検証・引用あり)
- **certificate-authority ドキュメントにEdgeへの言及なし**

---

### V9. Notification 2.0 のRBACバイパスに対する回避策

**質問**:
> 公式APIドキュメントに以下の注意書きがあります。
> > "If you assign Notification 2.0 roles or permissions to users, they can create Notification 2.0 subscriptions and receive notifications for **any device**, including those to which assigned inventory roles do not grant access, **bypassing the inventory role RBAC**."
>
> 弊社は**単一テナント内で拠点ごとのデータ・権限分離**をInventoryロール+資産グループで実現する設計のため、この挙動は分離の前提を崩します。
> 1. この記載の理解は正しいですか。**Notification 2.0の購読権限を持つと、Inventoryロールの制限に関わらず全デバイスの通知を受信できる**ということでしょうか。
> 2. **購読対象を特定のグループ・デバイスに制限する方法**はありますか(サブスクリプションのスコープ指定等)。
> 3. ない場合、**推奨される回避策**をご教示ください。

**なぜ必要か**: D17の拠点分離の成立条件。回避策がない場合、Notification 2.0の権限は「全拠点データへのアクセス権」と等価として扱い、基盤の通知コンシューマ専用サービスアカウント以外への付与を全面禁止する運用規定が必要(現仕様書§3.21・§4.4はこの前提で記載済み)。

**現時点で判明していること**: 上記の公式記載を確認(未検証・引用あり)。回避策の記載は見当たらない。

---

### V10. Inventoryロールの大規模時(2000件超)の挙動

**質問**:
> Inventoryロールによるアクセス制限について、以下の記載を確認しました。
> > 「Inventoryロールで許可された対象が約2000件以上になると、プラットフォームはレガシーな検索アルゴリズムに切り替わり、**空のページが返ることがあるため `statistics.next` URL でのページネーションが必要**になる」
>
> 1. この理解は正しいですか。閾値の正確な値と、切り替わり後の具体的な挙動をご教示ください。
> 2. 弊社は**50拠点×数百台=1万件超のManaged Object**を単一テナントで扱うため、拠点スコープのInventoryロールを持つアカウントは確実にこの条件に該当します。**API利用側(紐づけ確認アプリ)が実装すべき対処**をご教示ください。
> 3. **Inventoryロール以外に、単一テナント内で拠点分離を実現する推奨手法**はありますか。

**なぜ必要か**: IF-H06(イベント/アラーム参照)のページネーション実装。空ページを終端と誤判定すると、データがあるのに「該当なし」と表示される致命的な不具合になる。

**現時点で判明していること**:
- 上記の約2000件フォールバックの記載(未検証・引用あり)
- Inventory権限を含む**グローバルロール保有者はInventoryロール制限を無視して全デバイスにアクセス可能**(未検証・引用あり)
- Inventoryロールは資産階層を継承。権限レベルはREAD/CHANGE/ALLの3段階(未検証・引用あり)

---

## 4. 運用・制約の詳細

### V11. DataHub の Edge 提供条件

**質問**:
> Operational Store から長期保存用オブジェクトストレージへのデータオフロードについて、DataHubの採用可否を評価しています。
> 1. Edge 2026のドキュメントでは DataHub が **"On request via Professional Services"**、Edge 2025(K8s)では **"Optional"** と記載が異なります。**現行の提供条件**をご教示ください。
> 2. 提供される場合の**追加費用・リソース要件**(+10コア/+16GB/+100GB と理解しています)をご教示ください。
> 3. DataHub は**イベント添付バイナリ(スナップショット画像)をオフロードしない**という理解で正しいですか。正しい場合、**画像の長期保存に関する推奨アプローチ**をご教示ください。

**なぜ必要か**: N7。弊社要件では「Operational Store の保持期間経過でスナップショット画像が恒久消失する」ことを避ける必要があり、画像がオフロード対象外なら自作バッチが必須となる(現仕様書は自作を既定路線として記載済み)。

**現時点で判明していること**:
- Edge 2026: **"On request via Professional Services"**(検証確定・2026-08-01調査)
- Edge 2025(K8s): **"Optional"**、+10コア(最小6)/+16GB(最小10)/+100GB(検証確定)
- 「DataHubはイベント添付をオフロードしない」は先行調査時点の理解(要確認)

---

### V12. retention rules の Edge における変更可能範囲

**質問**:
> データ保持期限を retention rules で管理する設計です。
> 1. retention rule の **`editable` フラグは Management テナントのみ変更可能**と理解していますが、**Edge環境において「Managementテナント」に相当するのは誰(どの権限)**でしょうか。Edge運用者が自テナントの保持ルールを自由に作成・変更できますか。
> 2. **既定では全履歴データが60日で削除される**と理解しています。Edgeにおける既定値と、変更方法(システム設定の場所)をご教示ください。
> 3. **retention rules による Event 削除時、当該イベントに添付されたバイナリも連動削除される**という理解で正しいですか(公式コミュニティでの回答は確認しましたが、製品ドキュメント本文で確認できませんでした)。
> 4. retention rules は**ファイルリポジトリ(Inventoryバイナリ)には適用されない**と理解しています。**ソフトウェアリポジトリに登録したAIモデルバイナリの世代管理・削除**について推奨手順をご教示ください。

**なぜ必要か**: §3.10(オフロード)。3点目は「オフロード成功を検証してから保持期限で削除させる」という安全設計の前提。4点目はモデル配布(IF-D01)を数ヶ月ごとに繰り返すとリポジトリが単調増加するため、削除運用の確立が必要。

**現時点で判明していること**:
- dataType(ALARM/AUDIT/BULK_OPERATION/EVENT/MEASUREMENT/OPERATION/'*')× `maximumAge`(日数・最大10年)、`editable` はManagementテナントのみ変更可(検証確定)
- 絞り込み条件の適用範囲: **fragmentTypeはEVENT/MEASUREMENT/OPERATION/BULK_OPERATIONのみ、typeはALARM/AUDIT/EVENT/MEASUREMENTのみ**(検証確定)
- **Alarmは status=CLEARED のもののみ削除される**(未検証・引用あり)
- 既定60日・ファイルリポジトリ非適用・添付の連動削除(いずれも未検証・引用あり)

---

### V13. device token 方式の廃止予定と後継

**質問**:
> thin-edge.io がスナップショット画像を REST で添付する経路について。
> 1. デバイスが X.509 証明書を提示して取得する **device token(既定寿命1時間)** で以後のHTTPリクエストを認証する方式について、公式ドキュメントに「**将来のある時点で廃止予定(will be deprecated at some point in the future)**」との記載があります。**廃止時期の見込みと後継方式**をご教示ください。
> 2. 弊社は閉域網の切断が長時間に及ぶケースを想定しており、**トークン失効状態から復旧して再送を開始するシーケンス**を設計する必要があります。推奨される実装をご教示ください。
> 3. `device-token.lifespan.seconds` の変更はEdgeで可能ですか。

**なぜ必要か**: §3.5。廃止予定の方式に依存した再送設計を作り込むと、バージョンアップで作り直しになる。

**現時点で判明していること**: 上記の廃止予定記載(未検証・引用あり)。既定寿命1時間・テナントオプションで変更可(未検証)。

---

### V14. MQTTレート制限の具体値と超過時挙動

**質問**:
> AI検知イベントはバースト的に発生します。
> 1. Edge における **MQTT のメッセージレート制限の既定値**と、変更可否をご教示ください。
> 2. 「レート制限を超過すると要求はキューで遅延され、**キュー上限を超過するとクライアントは切断される**」と理解しています。この挙動の正確な条件(キュー上限値)をご教示ください。
> 3. バースト時に切断→再接続→ストア&フォワードの一括再送というループを避けるため、**デバイス側で推奨される送信レート平準化(スロットリング)の設定**はありますか。
> 4. 死活監視のような欠損許容度の高い計測に **transient トピック(`t/us`)** を用いる場合、標準トピック(`s/us`)との**保証レベルの正確な差**をご教示ください(Edgeの負荷軽減手段として検討しています)。

**なぜ必要か**: §3.5。定常負荷250msg/sの大半が死活測定であり、4点目が有効ならEdgeサイジング(V2)の前提が変わる。

**現時点で判明していること**:
- レート制限超過時の遅延→キュー超過で切断(⚠️2-1・解釈に幅あり)
- `s/us`(標準)/`t/us`(transient)/`q/us`(quiescent)/`c/us`(CEP)のトピック体系は確認済み。**保証レベルの正確な差は未確認**

---

### V15. thin-edge.io の child device 数百台規模での実績・上限

**質問**:
> 弊社は thin-edge.io を標準エージェントのランタイムとし、**1ゲートウェイあたり数百台のカメラを child device として代理登録**する設計です(貴社がメンテナンスするOSSのため、実績をお伺いします)。
> 1. **1ゲートウェイあたりの child device 数の上限、または実績値**をご教示ください。数百台規模の運用実績はありますか。
> 2. mapper のスループット上限、および child device 数がメモリ使用量に与える影響をご教示ください。
> 3. **ストア&フォワード(オフラインバッファ)の容量制御**について。thin-edge 2.0.0 で built-in bridge が既定化されたことにより、**オフライン時のキュー上限設定はどこで行い、上限到達時の挙動(古いものから破棄 / 新規を拒否)はどうなる**かをご教示ください。長時間の網断でディスクが枯渇するリスクへの推奨対策も併せてお願いします。
> 4. thin-edge の**自動登録を無効化し、規約準拠のExternal IDで明示登録する**運用(`@id` 指定/Entity Store HTTP API)は、数百台規模のプロビジョニングで推奨されますか。

**なぜ必要か**: D16採用の裏付け(A7)。特に3点目は、障害モードが「一部欠落」なのか「装置全断」なのかを分ける。

**現時点で判明していること**:
- **thin-edge 2.0.0(2026-04-10)で built-in bridge が既定化**し、クラウド接続は tedge-mapper が直接処理(mosquitto はローカルMQTT専用)— 検証確定
- child device の明示登録は retained MQTT + **`@id` 指定でExternal IDを直接制御可能**。1.5.0 で Entity Store HTTP API 追加(検証確定)
- **child device 数の上限に関する記載はドキュメントに見当たらない**
- mosquitto bridge のキュー問題(issue #1729)は詳細挙動が**検証で反証**され、深刻度は未確定

---

### V16. イベント添付のサイズ上限設定変更の Edge での可否

**質問**:
> 検知イベントにスナップショットJPEGを添付する設計です。
> 1. イベント添付の**サイズ上限(既定50MiB)・チャンクサイズ(既定5MiB)を Edge で変更可能**ですか。可能な場合の設定方法をご教示ください。
> 2. **1イベントにつき添付は1件のみ(2件目は HTTP 409)** という理解で正しいですか。複数フレーム(検知前後の画像)を1イベントに紐づける方法はありますか。
> 3. イベント添付バイナリは **Operational Store と同一の MongoDB に格納される**という理解で正しいですか。

**なぜ必要か**: A8。2点目は「1イベント1画像」という業務仕様の制約になり、3点目は V2 のDB容量見積りに直結する。

**現時点で判明していること**:
- **1イベント1バイナリ・2件目は409・既定50MiB(チャンク5MiB)・設定変更可**(検証確定)
- 同一MongoDB格納は先行設計文書の記載(architecture-camera-monitoring.md §4.6)。要確認

---

## 5. こちらの前提・読み取りが正しいか自信がない事項

**この節の位置づけ**: §2〜§4は「ドキュメントに書かれていないこと」を尋ねる質問である。本節は性質が異なり、**弊社側の読み取りや前提そのものが誤っている可能性がある事項**を扱う。誤っていた場合、設計の広い範囲が影響を受けるため、早い段階で否定してほしい項目である。

### V17. そもそもEdgeが適切な製品か — オンプレCore等との比較【最重要】

**質問**:
> 弊社の構成は「**本社サーバールームに1インスタンスを設置し、閉域WAN経由で全拠点(将来50拠点・カメラ1000台オーダー)のデバイスを集約する**」というものです。これは製品名としての "Edge"(拠点設置・エッジ処理)が本来想定する使い方とは異なるのではないか、という懸念を持っています。
> 1. **この用途に対して貴社が推奨する製品構成**をご教示ください。Cumulocity Edge が適切ですか、それとも**オンプレミス設置可能な Cumulocity(Core / 従来のon-premises版など)**が適切ですか。
> 2. オンプレミス版 Cumulocity(Edgeでないもの)の提供は現在ありますか。ある場合、**Edgeとの機能差**(マルチテナント、水平スケール、HA、Notification 2.0、SSO、DataHub、サポート形態)と**ライセンス体系の違い**をご教示ください。
> 3. Edgeを選んだ場合と比べ、**将来の拡張(拠点数増加・他案件への横展開)における移行パス**はどうなりますか。
> 4. インターネット接続が一切ない**完全閉域環境**でのライセンス認証・更新は、いずれの製品でも可能ですか(オンライン認証が必須でないこと)。

**なぜ必要か**: **弊社はEdge採用を前提に設計を進めているが、その前提自体を検証していない。** 本来「拠点ごとにEdge」という製品設計を「中央に1台のEdge」に転用しようとしており(D17)、V1〜V4の懸念(ライセンス、性能、HA、シングルテナント)はすべてこの転用に起因する。**製品選定が誤っていれば、V1〜V16の確認は無意味になる。**

**現時点で判明していること**:
- Edgeは**シングルテナント・シングルノードK8sのみ・水平スケール不可・HAは仮想化基盤依存**(いずれも検証確定)。これらはすべて「拠点設置の小規模インスタンス」としては自然な制約だが、**全社集約基盤の制約としては厳しい**
- Cumulocity Core(クラウド版)はマルチテナント対応と記載があるが、**Coreのオンプレミス提供可否は未調査**
- 弊社の製品選定基準(design-decisions.md §3)には「閉域・オフライン環境でのライセンス運用が現実的であること」が含まれるが、**いずれの製品についても未確認**

**否(Edgeは不適)だった場合の影響**: 製品選定のやり直し。ただし**設計の早期に判明すれば損失は小さい**。data-integration-spec.md の論理設計(IF採番・データモデル・信頼性要件)は製品非依存の部分が多く再利用可能。逆に**構築着手後に判明すると致命的**。

---

### V18. External ID・MQTT ClientId・トピックの文字種/長さ制約

**質問**:
> 弊社は多拠点を単一テナントに集約するため、**識別子に拠点プレフィックスを付ける命名規約**を定義しました。この規約が製品制約に抵触しないか確認させてください。
> 1. **External ID に使用できる文字種と長さの上限**をご教示ください。弊社規約は `site-001:CAM-SN12345` の形式で、**コロン `:` とハイフンを含みます**。
> 2. **MQTT ClientId の `deviceIdentifier` 部分**に使用できる文字種・長さをご教示ください。弊社規約は `site-001-gw` です。
> 3. **証明書の Common Name が `deviceIdentifier` と一致必須**という理解で正しいですか。CNの文字種制約と併せてご教示ください。
> 4. thin-edge.io の**トピックセグメント(`te/device/<child-id>//` の `<child-id>` 部分)に使用できる文字種**をご教示ください。特にコロンを含む識別子を使えますか。
> 5. External ID の**type**(`c8y_Serial` 等)には、弊社独自の type を定義できますか。

**なぜ必要か**: §4.1(識別子規約)は**全IFが依存する土台**である。コロンが使えない、長さ上限に抵触する等が構築時に判明すると、命名規約・プロビジョニングツール・オフロードのオブジェクトキー(`sites/{siteId}/...`)・重複排除キーまで連鎖的に作り直しになる。

**現時点で判明していること**:
- **`@id` 指定でExternal IDを直接制御できる**ことは検証確定。ただし**指定可能な文字種・長さの制約はドキュメントに記載を確認できず**
- thin-edge の自動導出形式は `<メインデバイスID>:device:<child-id>` と**コロンを含む**ため、コロン自体は許容される可能性が高い(推測)
- MQTT ClientId は `connectionType:deviceIdentifier:defaultTemplateId` 形式で一意性は `deviceIdentifier` のみで判定、証明書CNとの一致必須(未検証・引用あり)

**懸念の性質**: **「制約があるはず」ではなく「制約の有無すら確認できていない」**。調査対象のドキュメントページに記載がないことは確認済みだが、別ページに記載がある可能性がある。

---

### V19. 発報中Alarmのseverity昇格の正しい方法

**質問**:
> Alarm の重複排除仕様について確認させてください。
> 1. 「未クリア(ACTIVE または ACKNOWLEDGED)の同一 source + type の Alarm が存在する場合、新規Alarmは作成されず既存の `count` が増分され `time` が更新される。**それ以外の変更は無視される**」という理解で正しいですか。
> 2. 正しい場合、**MINOR で発報中のAlarmを CRITICAL に昇格させる正しい方法**をご教示ください。弊社は以下を想定していますが、いずれが推奨ですか。
>    - (a) Alarm更新API(`PUT /alarm/alarms/{id}`)でseverityを明示的に更新する
>    - (b) SmartREST静的テンプレート **305(severity更新)** を使用する
>    - (c) 別の type として新規Alarmを発報する
> 3. severity 昇格時に、**通知(Notification 2.0 / Smart Rules)は再度発火しますか**。発火しない場合、重大化を運用者に伝える推奨手段をご教示ください。

**なぜ必要か**: 「検知が継続・悪化しているのに通知が飛ばない」という運用上の重大な見落としにつながる。弊社仕様書は現状「305テンプレートまたは更新APIを使う」と記載しているが、**これは製品ドキュメントの記述からの推測であり、実際に意図どおり動くかは未確認**。特に3点目(昇格が通知を再発火するか)は仕様書に記載がなく、**確認していない前提のまま設計している**。

**現時点で判明していること**:
- 重複排除の条件(ACTIVE/ACKNOWLEDGED)と「それ以外の変更は無視される」は検証確定(2026-08-01調査)
- SmartRESTテンプレート **305 = severity更新** の存在は確認したが、**それが上記の回避手段として機能するかは未検証の推論**
- 昇格時の通知再発火については**情報なし**

---

### V20. thin-edge.io と Edge のバージョン互換・商用サポート

**質問**:
> 弊社は標準エージェントのランタイムとして thin-edge.io を採用する方針です(貴社がメンテナンスするOSSであることが選定理由の一つです)。
> 1. **Cumulocity Edge のリリース版と thin-edge.io のバージョンの互換マトリクス**はありますか。例えば Edge 2025 に対して thin-edge 2.x を接続する構成はサポートされますか。
> 2. thin-edge.io に対する**貴社の商用サポート**(有償サポート契約の対象となるか、SLA、脆弱性対応の提供期間)はありますか。それとも**コミュニティサポートのみ**でしょうか。
> 3. thin-edge.io の**LTS(長期サポート)版**の考え方はありますか。2.0.0 は破壊的変更(旧 `tedge/` トピック廃止)を含むと理解しており、**弊社の50拠点規模のフリートを将来アップグレードする際の手順・ダウンタイム**の想定をご教示ください。
> 4. thin-edge.io を採用しない場合、貴社が推奨する**代替のデバイス側実装**(公式デバイスSDK、Cumulocity公式エージェント等)をご教示ください。

**なぜ必要か**: D16(標準エージェント=thin-edge.io)は「Cumulocity開発元がメンテナンスするOSSに配管を委ねる」ことを根拠に採用した判断である。**しかし商用サポートの有無を確認していない。** 「OSSだからサポート対象外」であれば、閉域環境の顧客案件で障害が起きた際の切り分け責任がすべて弊社に来るため、D16の前提(運用負荷の軽減)が崩れる。

**現時点で判明していること**:
- thin-edge.io は Apache 2.0 の OSS。GitHubリリースノートから 1.5.0(2025-05)・2.0.0(2026-04)の内容は確認済み(検証確定)
- **バージョン互換マトリクス・商用サポートの有無は未調査**

---

### V21. MQTT 16KiB上限のEdge適用範囲・Messaging Service障害時の挙動

**質問**:
> 1. 「プラットフォームへの全 Core MQTT 接続において、**受理される最大ペイロードサイズは 16,184 バイト(ヘッダ+ボディ)**」という記載を確認しました。この制限は **Cumulocity Edge でも同一に適用**されますか。また、Edge で上限値を変更することは可能ですか。
> 2. この 16KiB 上限は、**検知イベント本文のサイズ設計**に直結します(弊社のイベントは検出枠の座標配列等を含みます)。**上限に近づいた場合の挙動**(拒否されるか、切断されるか)をご教示ください。
> 3. Messaging Service(Pulsar)を有効化した構成において、**Messaging Service 自体が停止した場合の挙動**をご教示ください。Cumulocity本体のデータ受信(MQTT/REST)は継続しますか、それとも影響を受けますか。**シングルノード構成でPulsar/Zookeeper/Bookkeeperを追加することによる障害点の増加**をどう評価すべきかも併せてご教示ください。

**なぜ必要か**: 1・2点目は「スナップショットはRESTで添付する」という設計の根拠そのものであり、**Edgeで同じ上限が効くかを確認していない**。3点目は、通知のために可用性の低い構成要素を1つ増やすことになるため、**Messaging Service障害が全体停止を招くのかどうか**で採否の判断が変わる(弊社はKubernetes/DBの本番運用経験が薄く、構成要素の増加は運用リスクに直結する)。

**現時点で判明していること**:
- 16KiB上限は検証確定(2026-08-01調査)。ただし出典の文言は「Core MQTT connections **to the platform**」であり、**この "Core" が「Cumulocity Core(製品)」を指すのか「コアMQTT(接続方式)」を指すのか、弊社では判断できていない**。前者ならEdgeでの適用可否が変わる
- Messaging Service は Pulsar ベースで **Persistent Volume を3つ(Bookkeeper Ledgers/Journal, Zookeeper)追加**する(検証確定)。障害時の挙動は情報なし

---

## 6. 補足: 確認結果の反映先

回答受領後は以下を更新すること。

| 確認事項 | 反映先 |
|---|---|
| V1・V2・V3・V4 | [design-decisions.md](design-decisions.md) D17(成立条件の判定)、[data-integration-spec.md](data-integration-spec.md) §1.0・§3.5・§5 N6 |
| V5・V6・V9 | data-integration-spec.md §3.21(IF-H13a/H13b)・§4.4・§5 N8 |
| V7 | data-integration-spec.md §3.23(IF-P04)・§3.26(IF-H16)・§5 N8 |
| V8・V15 | data-integration-spec.md §3.23(IF-P01)・§4.1・§5 N8 |
| V10 | data-integration-spec.md §3.12(IF-H06)・§5 N6 |
| V11・V12 | data-integration-spec.md §3.10(IF-H04)・§5 N7 |
| V13・V14・V16・V21 | data-integration-spec.md §3.5(IF-W01)・§3.21 |
| **V17** | **[design-decisions.md](design-decisions.md) 転換2(製品選定)・D17。否の場合は本仕様書全体の前提** |
| V18 | data-integration-spec.md §4.1(識別子規約)・§3.23(IF-P02)・§3.10(オブジェクトキー) |
| V19 | data-integration-spec.md §3.21(IF-H13b)・§4.5(重複排除表) |
| V20 | design-decisions.md D16・data-integration-spec.md §4.5(A7) |

**判定の位置づけ**:

1. **V17が否の場合** — 製品選定のやり直し。V1〜V16の確認は前提が変わるため無意味になる。**最初に確認すべき項目**
2. **V1〜V4のいずれかが否の場合** — D17(中央集約配置)の再検討が必要であり、データ連携仕様の広範囲な改訂につながる
3. **V18が想定と異なる場合** — 識別子規約(§4.1)の作り直し。全IFに波及する

いずれも**契約前・構築着手前**の確認を強く推奨する。設計段階で判明すれば論理設計(IF採番・データモデル・信頼性要件)は製品非依存の部分が多く再利用できるが、構築着手後の判明は損失が大きい。
