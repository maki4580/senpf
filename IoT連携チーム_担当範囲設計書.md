# IoT連携チーム 担当範囲 設計書

**— Cumulocity Edge / thin-edge.io（画像解析装置・外部Gateway）のセットアップから提供開始まで —**

**対象構成**: [cumulocity-iot-architecture.drawio](cumulocity-iot-architecture.drawio) —「全体構成(配置構成・レビュー反映)」タブ**のみ**
**版**: rev.2（2026-08-19）— 4 系統のレビュー（thin-edge.io 一次情報の再検証・社内既存文書との整合性・文書内部の論理構成・実務者視点での実行可能性）を反映。ID 体系の衝突（`P1〜P12`→`DL-nn`、`R-1〜R-12`→`RB-nn`）の解消、相互参照の誤り修正、確度タグの精緻化、§10.2 チェックリストの抜け漏れ補完が主な変更
**位置づけ**: **IoT連携チーム**が担当する 3 つの対象物（Cumulocity Edge / 画像解析装置の thin-edge.io / 外部Gateway の thin-edge.io）について、**設置から「IoT基盤として他チームに提供できる状態」に到達するまでに必要な作業・設定・判断をすべて洗い出した設計書**
**チーム名**: IoT連携（以下「本チーム」）

> **読み方のショートカット**: 初読は §0〜§2 を推奨しますが、**すぐに手を動かす必要がある場合は §8（作業一覧）→ §9（検証）→ §10（提供開始の定義）から読んで構いません。** §0.5 に要点 8 点をまとめています。

---

## 0. 本書の位置づけと読み方

### 0.1 目的

本書は次の 3 つの問いに答えるために書かれています。

| # | 問い | 答える節 |
|---|---|---|
| Q1 | **何が本チームの担当で、何が担当でないか** | §1 |
| Q2 | **2 つの thin-edge.io は同一のもので良いか** | §2（本書の中心論点） |
| Q3 | **設置から提供開始まで、何をどの順でやるのか。終わったと言える条件は何か** | §3〜§10 |

### 0.2 既存文書との関係

本構成には既に 5 つの詳細文書があります。**本書はそれらを置き換えず、「本チームの担当範囲」という切り口で串刺しにする索引兼設計書**です。

| 文書 | 役割 | 本書との関係 |
|---|---|---|
| [Cumulocity構成準拠セットアップ設定書.md](Cumulocity構成準拠セットアップ設定書.md)（以下 **[設定書]**） | 構成図 → Cumulocity 設定のトレースと適用手順。**Cumulocity 側の実施手順の正** | 本書 §4 は本文書の項目 ID（E-/R-/T-/X-/S-/N-/P-/G-/D-/M-/K-/O-）を参照する。**手順の重複記述はしない** |
| [Cumulocity設定定義書.md](Cumulocity設定定義書.md)（**[定義書]**） | 設定項目の網羅カタログ | 参照のみ。[設定書] §0.2 の訂正が優先 |
| [Cumulocity設定エクスポート投入ガイド.md](Cumulocity設定エクスポート投入ガイド.md)（**[投入ガイド]**） | go-c8y-cli の実装技法・冪等化パターン | §8 の各タスクが参照する |
| [../../IoTPlatform_cc/data-integration-spec_new.md](../../IoTPlatform_cc/data-integration-spec_new.md)（**[連携仕様]**） | IF-nn 単位のデータ連携仕様 | **本書 §5〜§7 は IF-S04 / S07 / S08 / W01 / H01 / H02 / D01 / P01〜P03 の実装設計にあたる** |
| [../../IoTPlatform_cc/architecture-camera-monitoring.md](../../IoTPlatform_cc/architecture-camera-monitoring.md)（**[カメラ構成]**） | デバイスモデル・ペイロード規約（§4.1〜4.3） | §4.3 のデバイスモデルの原典 |
| [../../IoTPlatform_cc/design-decisions.md](../../IoTPlatform_cc/design-decisions.md)（**[判断記録]**） | D1〜D17 の設計判断 | §0.5 で引き継ぎ |

> **本書が新たに提供するもの**（既存文書に無いもの）:
> 1. **担当範囲の確定**（§1）— 構成図の全要素に対する担当/非担当の判定と、値の決定権の所在
> 2. **2 つの thin-edge.io の同一性分析と配置方式の提言**（§2）
> 3. **thin-edge.io 側の実装設計**（§5〜§7）— 既存文書は Cumulocity 側に厚く、thin-edge 側は「thin-edge.io を使う」という判断（D16）と IF 仕様までで、**設定値・プラグイン構成・プロファイル設計は未記述**だった
> 4. **提供開始（Ready for Service）の定義**（§10）

### 0.3 凡例

**確度**（[設定書] §0.4 を継承）:

| 記号 | 意味 |
|---|---|
| **[確]** | 公式ドキュメントまたは OpenAPI 仕様の逐語記述で確認済み |
| **[推]** | 一次情報からの演繹。直接の記述は無い |
| **[要]** | 未確認。実機検証またはベンダー確認が必要。§11 に一覧 |
| **[判]** | 技術的制約ではなく**本チームの設計判断**。本書で提案し、合意を求める |

**担当区分**:

| 記号 | 意味 |
|---|---|
| **◎** | 本チームが設計・構築・運用のすべてを持つ |
| **○** | 本チームが実装するが、**値・要件の決定権は他者**にある |
| **△** | 他チームの担当物だが、**本チームとの IF 契約がある**（IF 仕様は本チームが定義する） |
| **×** | 本チームの担当外。参照のみ |

### 0.4 引き継ぐ設計判断

本書は次を所与として設計します。ここを覆す場合は本書の広範囲が影響を受けます。

| # | 判断 | 本書への効き方 |
|---|---|---|
| **D9** | 拠点発アウトバウンドのみ。中央から拠点への着信接続なし | thin-edge の全通信が装置発。`c8y_RemoteAccessConnect` の扱い（§5.10）、メタ監視の向き（[設定書] 注-1）に効く |
| **D10** | 構成の版管理。拠点ごとの手作業カスタムを禁止 | thin-edge の設定を**すべて構成コード化**する（§5.4）。手動 `tedge config set` を禁止 |
| **D15** | 映像解析 AI は別筐体。AI 側は標準ペイロード規約にのみ従う | ローカル MQTT 受け口の IF 契約（§5.6）が本チームの提供物になる |
| **D16** | 標準エージェントのランタイムは thin-edge.io。カメラは child device として代理登録 | 本書 §5〜§7 の前提そのもの |
| **D17** | 全拠点を単一 Cumulocity Edge・単一テナントに集約。拠点区分は device group + Inventory ロール | **§2 の中心論点**。単一 thin-edge が複数拠点を代表することとの整合が問題になる |
| **変-1** | 通知は Notification 2.0 | 拠点グループ単位のサブスクリプションが成立することが、デバイスモデル設計（§4.3）の要件になる |
| **変-2** | メール通知は不採用 | 基盤自身の異常の通知先を Cumulocity の外に持つ必要（§9・§10） |

> ⚠️ **[判] D15 と現構成の矛盾を先に認識してください。** D15 は「映像解析 AI は基盤アプライアンスと別筐体」と定めましたが、レビュー反映タブでは **thin-edge.io は画像解析装置（＝AI 筐体）の内側**にあります。つまり**本チームの標準部品が、切り離したはずの AI 筐体に同居する構成**です。[設定書] SV-21 として起票済みですが、**本書 §1.4 TB-1 で「筐体の所有・OS 管理・構成管理の主体」を確定しない限り、§6 の作業の実施主体が決まりません**。

### 0.5 ⭐ 先に読むべき — 本書の調査で判明した、設計に影響する 8 点

**本書は thin-edge.io 2.0.1 の公式ドキュメントとソースを一次情報として調査しました。その結果、既存の設計文書の前提を修正すべき点が見つかりました。**

| # | 判明したこと | 影響 | 節 |
|---|---|---|---|
| **1** | ⚠️⚠️ **ストア&フォワードの既定バッファは 1000 件・非永続。1 拠点 300 台の死活で約 3 分の網断で溢れ、しかも黙って捨てられる** | **D16「thin-edge のストア&フォワードが網断を吸収する」は既定設定では成立しない。** mosquitto の設定を本チームが構成コードで管理する必要がある | §5.7 |
| **2** | ⚠️ **thin-edge は既定で全 child device に `c8y_RequiredAvailability = 60 分` を付ける** | 何もしないと**接続 1 時間後に全カメラ・全 BOX が一斉にアラームを上げる**（数百〜数千件） | §4.5 |
| **3** | ⚠️ **自動登録に任せると external ID が規約と食い違う**（`<main-device-id>:device:<child-id>` 形式になる） | **重複排除・映像突合・特徴量紐づけの三重の役割（C-2）が壊れる。** 自動登録を無効化し `@id` で統制する | §5.5 |
| **4** | ⚠️ **software_update に自動ロールバックは存在しない** | **AI モデル更新の失敗時ロールバック（IF-S07）は本チームが自作する**。プラグインの `prepare`/`finalize` かワークフローの `[rollback]` 状態 | §6.3 |
| **5** | ⚠️ **thin-edge の公式なエアギャップ導入手順が存在しない**（配布は Cloudsmith のみ、GitHub Releases に資産なし） | **閉域網への配布経路の構築が W0 の必須作業**になる | §5.1 |
| **6** | ⚠️ **`c8y_RemoteAccessConnect` を無効化する設定フラグが無い** | パッケージ非導入 + オペレーションファイル削除 + テナント側のロール制御の組み合わせで封じる | §5.10 |
| **7** | ✅ **スナップショット添付は `tedge upload c8y` 1 コマンドで完結できる**（イベント作成 + 添付 + ID 取得、認証は c8y-proxy が代行） | [連携仕様] §3.2 の「REST を 2 回叩く」設計を単純化できる。**添付ワーカーを基盤標準部品にする提案**（§6.6） | §5.6 |
| **8** | ✅ **child device の supported operations は capability メッセージで完全に制御できる**。ただし**一度申告すると child からは動的に削除できない** | BOX・カメラには最初から一切申告しない | §5.8 |

**そして、ご質問への回答（§2）**:

> **2 つの thin-edge.io は「同じソフトを入れる」は正しく、「同じ設定で動く」は誤りです。** ソフトウェア（版）は同一にすべき、設定プロファイルは別物、そして**外部Gateway を全拠点で 1 インスタンスにする点は設定の差ではなく設計の非対称**であり、先に決着させる必要があります。**本書は拠点ごとにインスタンスを分ける案 β を推奨します**（§2.5）。

---

## 1. 担当範囲の確定

### 1.1 担当対象物

| # | 対象物 | 配置 | 台数 | 区分 |
|---|---|---|---|---|
| **T1** | **Cumulocity Edge** | 本社 VM1（実配置は専用 VM） | 1（全拠点集約・D17） | ◎ |
| **T2** | **thin-edge.io（画像解析装置）** | 顧客拠点・画像解析装置（Linux） | 拠点数 × 装置数 | ◎（筐体所有は TB-1） |
| **T3** | **thin-edge.io（外部Gateway）** | 本社 VM1 | §2 の結論による | ◎ |

### 1.2 構成図の全要素に対する担当判定

構成図（レビュー反映タブ）の全ノードに対する判定です。「担当外」も判定として明示します。

#### 現場（顧客拠点）

| 構成要素 | 担当 | 本チームとの関係 |
|---|---|---|
| IP カメラ群（ONVIF/RTSP） | **△** | Cumulocity 上では **child device として本チームが代理登録**（D16）。機器自体の調達・設置・設定は案件/顧客側。**死活ポーリング（IF-S08）は本チームの thin-edge プラグイン** |
| サイト VMS（Genetec） | **×** | D13 の抽象レイヤー経由。本チームは触らない |
| 画像センシング BOX（改造不可） | **△** | Cumulocity 上では **外部Gateway の child device として本チームが代理登録**。機器自体は既存資産 |
| 画像解析装置（筐体・OS） | **⚠️ TB-1** | 所有・OS 管理主体が未確定。thin-edge を載せる以上、**最低でも OS の構成管理に本チームが関与する必要がある** |
| ├ Docker Compose | **×** | 画像解析側チーム |
| ├ フロントエンド / バックエンド | **×** | 同上。現場管理端末の UI 入口。Cumulocity ユーザーとは無関係 |
| ├ ログ処理（Grafana Alloy） | **×** | 同上。ただし thin-edge のログを収集対象に含める調整が必要（§5.9） |
| ├ 画像解析パイプライン | **△** | **IF-S04（ローカル MQTT 受け口）の契約相手**。IF 仕様は本チームが定義（§5.6） |
| └ **thin-edge.io** | **◎ T2** | 本書 §6 |
| 現場管理端末（ブラウザ） | **×** | 画像解析装置のフロントエンド／紐づけ確認アプリへアクセス。**Cumulocity Web UI へは直接アクセスしない**（[設定書] 訂-10） |

#### 本社 VM1

| 構成要素 | 担当 | 本チームとの関係 |
|---|---|---|
| **Cumulocity Edge**（REST/MQTT API・Web UI・デバイス管理・イベント/アラーム処理・Operational Store） | **◎ T1** | 本書 §4。**設定の投入はすべて本チームが行う。ただし値の決定権が他者にある項目がある**（§1.3） |
| **外部Gateway / thin-edge.io** | **◎ T3** | 本書 §7 |
| 外部Gateway / BOXアダプタ | **△ TB-2** | **IF-H01（ローカル MQTT）の契約相手**。担当主体が未確定 |
| Keycloak（IdP） | **×**（Cumulocity 側の `authConfig` は **○**） | Keycloak 本体は担当外。**Cumulocity 側の loginOptions / SSO 設定は本チームが投入**（[設定書] §4.3・A-01〜A-05）。realm/clientId/クレーム名は Keycloak 担当から受領する入力値 |
| Otel インフラ | **×**（Edge の Prometheus 接続は **○**） | Edge の `https://<domain>:3443/metrics` を Otel Collector に取り込む調整が必要（[設定書] O-01） |
| 運用知識基盤 | **△** | **Cumulocity の読み手かつ書き手**。本チームが読取/書込アカウント（R-06）とガードレール（[設定書] §4.9.3）を提供する。基盤自体の実装は担当外（N10 で未確定） |
| ベクトルDB / 特徴量エクスポートジョブ | **×** | Cumulocity 側設定なし。ただし **external ID がベクトルDB の紐づけキー**（§4.2 D-06） |
| オブジェクトストレージ | **×**（オフロードの発側は **○**） | オフロードバッチ（X-03）は Cumulocity から読み出すため、読取アカウントと API 規約は本チーム |

#### 本社 VM2・その他

| 構成要素 | 担当 | 本チームとの関係 |
|---|---|---|
| 紐づけ確認アプリ | **△** | Cumulocity 側の EXTERNAL アプリ登録・CORS・ロール（P-01/P-02/T-02/R-02）は本チームが投入 |
| 生体認証SA / 他アセット | **△** | CORS・サービスユーザー・**Notification 2.0 サブスクリプションとトークン発行**（N-01〜N-04）は本チームが提供 |
| Genetec Security Center | **×** | — |
| 業務端末（ブラウザ） | **△** | Cumulocity Web UI（Cockpit）の利用者。ロール（R-03）は本チームが定義 |
| 保守端末（保守拠点） | **△** | AI モデル登録・更新オペレーション発行の主体。**ソフトウェアリポジトリと更新経路（M-01〜M-03）は本チームが整備** |
| AI モデル改善サービス（クラウド） | **×** | — |

### 1.3 ⚠️ 「Cumulocity Edge 担当」の意味 — 投入責任と決定権は別

**Cumulocity Edge のすべての設定は本チームが投入します。しかし値を決める権利は必ずしも本チームにありません。** この区別を最初に合意しないと、「決まらないから投入できない」設定が滞留します。

| 設定 | 投入 | **値の決定権** | 受領期限 |
|---|---|---|---|
| ドメイン 2 つ・TLS SAN（E-01/E-04） | 本チーム | インフラ/NW チーム | **W0 の前**（後戻り不可） |
| ライセンス・registry credentials（E-03/E-10） | 本チーム | 調達/ベンダー窓口 | **W0 の前** |
| 拠点コード（英数字）・拠点グループ階層（G-01） | 本チーム | 案件チーム（拠点の実体を知る側） | W2 の前 |
| **イベント/アラーム型・ペイロード規約（S-04）** | 本チーム（規約文書として提供） | **本チームが起案 → 画像解析側・案件側と合意** | **最優先**（§1.5） |
| **external ID 採番規約（D-06）** | 本チーム | 同上（仮名化要否は法務/顧客合意） | **最優先** |
| `responseInterval` 実値（D-04） | 本チーム | 本チームが提案 → 案件側の許容誤検知率で決定 | W4 の前 |
| CORS 許可オリジン（T-02） | 本チーム | VM2 の各アプリ担当 | W2 の前 |
| SSO の issuer/clientId/JWKS/クレーム名（A-01/A-02） | 本チーム | Keycloak 担当 | W5 の前 |
| リテンション日数（X-01、特に `AUDIT`） | 本チーム | 顧客契約・監査要件 | W6 の前 |
| Notification 2.0 サブスクリプション定義（N-01） | 本チーム | 案件アプリ担当（何を受けたいか） | W6 の前 |
| スマートルール/EPL の判定ロジック（S-01/S-02） | **雛形のみ**本チーム | **案件側が保守**（D7） | — |

### 1.4 ⚠️ 未確定の担当境界（TB-nn）

**これらが決まらないと作業の実施主体が決まりません。§11 の未確定事項とは別に、着手前の合意事項として切り出します。**

> ⚠️ **TB-nn には TE-nn（§11）と違って `[判]` の暫定提案を付けていません。** 理由は性質の違いです。TE-nn は「本チームが検証すれば片付く技術的未確定」ですが、TB-nn は「他部門（画像解析側・インフラ/設備側）との**組織的な担当交渉**」であり、本チームが一方的に「こうします」と決めて良いものではありません。**だからこそ決定を急かす力が働かず、最も放置されやすい**（§3.2 参照）。決定権はいずれも**本チームの外**にあり、本チームの役割は「これを決めないと何が止まるか」を明示して交渉のテーブルに載せることです。

| # | 境界 | 論点 | 決まらないと何が止まるか | 決定権 | 対応 SV |
|---|---|---|---|---|---|
| **TB-1** | **画像解析装置の筐体・OS の所有と構成管理** | D15 は「AI は別筐体」としたが、現構成では本チームの thin-edge が AI 筐体に同居する。OS パッチ・ユーザー管理・ディスク・NTP・CA 信頼ストアを誰が持つか | **§6 のほぼ全作業**。特にプロビジョニング（§6.7）と証明書更新（§5.3） | 画像解析側 × 本チーム（要合意） | SV-21 |
| **TB-2** | **外部Gateway の VM/OS 所有と BOXアダプタの担当** | 図では BOXアダプタと thin-edge が同一の箱に描かれるが、本チームの担当は thin-edge のみ。ホストの所有者・アダプタの開発主体・ローカル MQTT の運用責任 | **§7 の全作業**、特に IF-H01 の契約確定 | 基盤運用側 × 本チーム（要合意） | SV-27 |
| **TB-3** | **トークン発行プロキシ（N-02）の担当** | 「基盤標準部品」だが、実装主体が本チームか別かが未定。`ROLE_NOTIFICATION_2_ADMIN` を持つ唯一の主体であり、拠点分離の要 | W6 の通知経路全体 | 基盤チーム内（要割当） | — |
| **TB-4** | **オフロードバッチ（X-03）の担当** | Cumulocity から読み出す側なので API 規約は本チームだが、実装・運用主体が未定。**稼働開始から 90 日以内が期限** | 90 日後の長期データ喪失 | 基盤チーム内（要割当） | — |
| **TB-5** | **メタ監視（IF-F02）と Edge 監視（O-01）の担当** | Edge の Prometheus エンドポイントを誰が刈り取り、誰に通報するか。**変-2 によりメールが無いため、ここが基盤異常の唯一の検知経路** | 基盤自身の異常検知 | 基盤運用側 × 本チーム（要合意） | SV-34 |

> ⚠️ **TB-3〜5 は「本チーム内で決めればよい」に見えますが、決める人が明示されないと放置されます。** 特に **TB-5 は基盤異常の唯一の検知経路**であり、**TB-4 は 90 日という具体的な期限**があります。§3.2 のクリティカルパス図と §8 の WBS では、この 3 つを W0 の議題として明示的に載せています。

### 1.5 ⚠️ 着手前に確定すべき「規約」— これが最優先の成果物

**設定でもコードでもなく、規約が最初のクリティカルパスです。** 後から変えると全設定・全実装が壊れます。

| # | 規約 | なぜ最初か | 起案 |
|---|---|---|---|
| **C-1** | **イベント/アラーム型名とペイロード規約**（S-04・[カメラ構成] §4.3） | `alarm.type.mapping` のキー／リテンションの type フィルタ／スマートルール条件／EPL マッチング／Notification 2.0 の `typeFilter`／オブジェクトストレージ上の突合キー、**全ての結節点** | 本チーム |
| **C-2** | **external ID 採番規約**（`{siteId}:{機器種別}-{シリアル}`・[連携仕様] §4.1） | ①BOX 再送の重複排除キー ②映像クリップとの突合キー ③ベクトルDB の紐づけキー、の**三重の役割**。③はクラウドへ出るため仮名化要否の法務論点が乗る | 本チーム |
| **C-3** | **拠点コード（英数字のみ）** | Notification 2.0 の `subscription` 名が **パターン `^[a-zA-Z0-9]+$`** [確]。日本語・ハイフン・アンダースコア不可。拠点グループ名とは**別に**トピック名用コードが要る | 本チーム |
| **C-4** | **ローカル MQTT 受け口の IF 契約**（IF-S04 / IF-H01） | 画像解析パイプライン・BOXアダプタの実装前提。**本チームの提供物であり、遅れると相手チームが止まる** | 本チーム（§5.6） |
| **C-5** | **child device の entity 登録規約**（`@id` による external ID 統制） | 自動登録に任せると external ID が C-2 と食い違い、**MO が二重に生える**（§5.5） | 本チーム |

---

## 2. ⭐ 論点: 2 つの thin-edge.io は同一で良いか

> ご質問「**外部Gateway の thin-edge.io は、画像解析装置にインストールされるものと同じものの想定**」に対する回答です。

### 2.1 結論

**3 つの層に分けて答えます。**

| 層 | 同一にすべきか | 理由 |
|---|---|---|
| **① ソフトウェア（パッケージ・バージョン）** | **✅ 同一にすべき** | 版が割れると mapper の挙動差・supported operations の差が出て、Cumulocity 側の設定（`c8y_SupportedOperations` 前提の運用）が装置ごとに変わる。**同一バージョンを固定し、同時に上げる**（§5.1） |
| **② 設定プロファイル（プラグイン構成・tedge config・オペレーション定義）** | **❌ 別物として管理する** | 役割が違う。§2.3 に 21 項目の差分。**共通ベース + オーバーレイ 2 種**として構成コード化する（§2.6） |
| **③ Cumulocity 上のデバイスモデル（1 インスタンスが何を代表するか）** | **⚠️ 構造が非対称。ここが本当の論点** | 画像解析装置は「1 インスタンス = 1 拠点の一部」。外部Gateway は現案では「1 インスタンス = **全拠点**」。**D17 の拠点分離設計と噛み合わない**（§2.4） |

> **短く言うと**: 「同じソフトを入れる」は正しい。「同じ設定で動く」は誤り。そして**外部Gateway を全拠点で 1 インスタンスにする点は、設定の差ではなく設計の非対称であり、先に決着させる必要があります**（§2.5 で案を提示）。

### 2.2 前提: thin-edge.io の 1 インスタンスが表現できる範囲

thin-edge.io は「**1 インスタンス = Cumulocity 上の 1 台のメインデバイス + その配下の child device 群**」というモデルです（D16 / [カメラ構成] §4.1）。この前提が §2.4 の議論の土台になります。

```
thin-edge.io インスタンス 1 つ
└── main device（＝この thin-edge 自身。証明書 1 枚・MQTT 接続 1 本）
    ├── child device
    ├── child device
    └── …
```

つまり **thin-edge のインスタンス境界が、そのまま Cumulocity 上のデバイス階層の境界**になります。

### 2.3 差分の全数

| # | 観点 | **プロファイル A: 画像解析装置** | **プロファイル B: 外部Gateway** | 差 |
|---|---|---|---|---|
| 1 | 設置場所 | 顧客拠点（現場） | 本社サーバールーム VM1 | ✔ |
| 2 | インスタンス数 | 拠点数 × 装置数 | 現案 1 / 推奨案は拠点数（§2.5） | ✔ |
| 3 | ホスト所有・OS 管理 | **未確定（TB-1）** | 基盤側 VM（TB-2） | ✔ |
| 4 | main device が代表するもの | その拠点の画像解析装置 1 台 | 現案では**全拠点の BOX 群の集約点** | ⚠️ |
| 5 | child device の種類 | IP カメラ | 画像センシング BOX | ✔ |
| 6 | child device 数 | 拠点あたり〜数百 | 現案では全拠点の BOX 合計 | ⚠️ |
| 7 | child の拠点帰属 | 全 child が同一拠点 | 現案では**child ごとに拠点が異なる** | ⚠️ |
| 8 | 上流 IF | IF-S04（画像解析パイプライン） | IF-H01（BOXアダプタ） | ✔ |
| 9 | 上流の担当チーム | 画像解析側 | TB-2（未確定） | ✔ |
| 10 | **閉域網（WAN）を越えるか** | **越える**（thin-edge → Edge が横断） | **越えない**（同一サーバールーム内） | ⚠️ |
| 11 | ストア&フォワードの役割 | **WAN 断の吸収が主目的。生命線** | Edge 停止の吸収のみ。**案α（単一インスタンス）では断時に全拠点分が一度に溜まる。案βならプロファイル A と同じく 1 拠点分に縮む** | ⚠️ |
| 12 | バッファサイジング根拠 | 想定最長 WAN 断 × 拠点内バースト | Edge 停止許容時間 × **全拠点合算**レート | ✔ |
| 13 | 重複排除 | 不要（パイプラインが一次生成） | **BOXアダプタが担当**（thin-edge の責務外） | ✔ |
| 14 | ソフトウェア更新（sm-plugin） | **必須**。AI モデル配布 → 適用 → ロールバック（IF-D01/S07） | **不要**（BOX は改造不可）。GW 自身の更新は別議論（SV-27） | ⚠️ |
| 15 | 設定管理（config plugin） | パイプライン設定・死活間隔を配布 | GW 自身のみ。**BOX へは配れない** | ✔ |
| 16 | カメラ死活監視プラグイン | **必要**（IF-S08・ONVIF/ICMP） | 不要 | ✔ |
| 17 | supported operations | フルセット + カスタム | **縮退が必要**（更新系を BOX に出さない・§7.3） | ⚠️ |
| 18 | `c8y_RemoteAccessConnect` | **無効化を推奨**（現場への対話的経路） | 自社 VM。運用判断 | ✔ |
| 19 | 証明書更新の運用容易性 | 現場・自動更新が必須（テナント CA の毎年 10/2 自動更新問題・SV-25） | VM 上・手が届く | ✔ |
| 20 | 障害時の影響範囲 | **1 拠点のみ** | 現案では**全レガシー拠点が同時停止** | ⚠️ |
| 21 | **D17 の拠点分離との整合** | **自然に成立**（1 装置 = 1 拠点） | 現案では**成立せず追加設計が必要**（§2.4） | ⚠️ |

**⚠️ の 8 項目は「設定を変えれば済む」話ではなく、配置設計の問題です。**

### 2.4 ⚠️ 構造的な非対称 — 1 インスタンスが複数拠点を代表することの帰結

D17 は「拠点区分は **device group（資産階層）+ Inventory ロール**で実現する」と定めています。ところが §2.2 のとおり、thin-edge が作る親子関係は**接続階層（`childDevices`）**であって資産階層（`childAssets`）ではありません。この 2 つの階層が食い違うことで、次の 4 つが起きます。

#### (1) BOX の MO を「拠点グループ」と「GW 配下」に二重所属させる工程が要る

```
資産階層（グループ）              接続階層（thin-edge が作る）
拠点A (group)                     外部Gateway (main device)
 ├── 画像解析装置 ─┐               ├── BOX_A1  ← 拠点Aの機器
 │    └── カメラ    │              ├── BOX_B1  ← 拠点Bの機器
 └── BOX_A1  ←─────┴── 同一MOを     └── BOX_C1  ← 拠点Cの機器
                     両方から参照
```

- Cumulocity では 1 つの MO を「GW の `childDevices`」かつ「拠点グループの `childAssets`」にできます [確]。したがって**技術的には成立します**
- ただし **thin-edge の child device 登録はグループ所属を作りません**。プロビジョニング（IF-P02 手順 5「拠点グループへの編成」）で**本チームが明示的に `childAssets` へ追加する工程**が必須です（§7.2）
- 画像解析装置側は「拠点グループ → 装置 → カメラ」と階層が一致するため、この工程は 1 回で済みます。**BOX 側は台数分すべてに必要**です

#### (2) ⚠️ 拠点グループ購読（Notification 2.0）の成否が SV-35 に丸ごと依存する

`alarmsWithChildren` は *"all of its descendant managed objects"* を購読します [確]。**この "descendant" が `childAssets` を辿るのか `childDevices` だけなのかが未確認（[設定書] SV-35）**です。

| SV-35 の結果 | 画像解析装置経路 | BOX 経路 |
|---|---|---|
| `childAssets` を辿る | 成立 | 成立（(1) の二重所属が前提） |
| `childDevices` のみ | **拠点グループ購読が全滅** → D17 の拠点分離方式そのものが破綻 | 同左 |

> ⚠️ **SV-35 は「BOX 経路だけの問題」ではなく、拠点分離設計全体の成立条件です。** 検証環境で最初に潰すべき項目として §9 に置いています。

#### (3) 外部Gateway 本体のアラームが、どの拠点からも見えない

> 表記注: 以下「GW」は **thin-edge B の main device**（Cumulocity 上の1個体、type `x_BoxGateway`）を指します。本社 VM1 上のホスト・システム全体を指す「外部Gateway」（T3）とは別の階層の概念です。混同しないでください。

GW の main device MO はどの拠点にも属せないため、共通グループに置くことになります。その結果:

- **Inventory ロール**: 拠点オペレーターは GW 本体を見られない
- **Notification 2.0**: 拠点グループ購読に GW 本体のアラームが乗らない → **案件アプリは GW 障害を知れない**
- 拠点側からは「その拠点の BOX が全部黙った」ようにしか見えず、原因が GW であることに辿り着けない

#### (4) 死活監視のアラームストームと child ID 名前空間

- `c8y_RequiredAvailability` を BOX child に設定すると、**GW 停止時に全拠点の BOX が同時に `c8y_UnavailabilityAlarm`** を上げます。アラームは `CLEARED` にならない限り消えません（[設定書] X-01）
- thin-edge の child ID は 1 インスタンス内で一意である必要があるため、**全拠点分の BOX 名を 1 つの名前空間に押し込む**ことになります（C-2 の `{siteId}:` プレフィックスで回避可能だが、命名の自由度は失われる）

### 2.5 配置方式の選択肢と推奨

| 案 | 内容 | 評価 |
|---|---|---|
| **α（現案）** | 単一 thin-edge が全拠点の BOX を収容 | §2.4 の (1)〜(4) をすべて追加設計で埋める必要がある。SPOF は残る |
| **β（推奨）** | **外部Gateway の箱の中に、拠点ごとに thin-edge インスタンスを立てる**（コンテナ等）。BOXアダプタは共通でも拠点別でもよい | §2.4 の (3)(4) が**消滅**、(1) が**大幅に軽くなる**。プロファイル A と構造が揃い、運用手順・検証項目・構成コードが共通化できる |
| γ | 外部Gateway を拠点側に設置 | 「BOX がバッファ+再送を持つので本社集約できる」という構成図の設計意図（配置の要点）に反する。設置コスト増。**不採用** |
| δ | thin-edge をやめ、BOXアダプタが直接 Cumulocity API を叩く | 部品が減りイベント ID も取れるが、**デバイス証明書による機器認証を捨てることになる**（[連携仕様] §4.4「デバイス = X.509」原則）。デバイス管理（死活・supported operations）も自作になる。**不採用** |

> **[判] 推奨: 案 β。** ただし採用理由を正確に述べます。
>
> - **拠点分離そのものは α でも β' でも成立し得ます**（(1) の二重所属工程 + SV-35 が OK なら）。β の主たる価値は**そこではありません**
> - β の価値は **(3) 障害の可視性 ・(4) アラームストームと名前空間 ・障害影響範囲（差分表 #20）・プロファイル A/B の運用の対称性** の 4 点です
> - **コスト**: 拠点数ぶんのコンテナ、デバイス証明書、main device MO が増えます。thin-edge 自体は軽量（mosquitto + 数プロセス）なので、数十拠点規模なら VM1 のリソースで収まる見込み **[推 → §11 TE-21 で実測]**
> - **副作用**: BOXアダプタが「どの拠点の thin-edge に publish するか」を振り分ける必要が出ます。**IF-H01 の契約に宛先ブローカーの決定規則を追加**してください（§7.4・TB-2 と連動）

#### 案 β を採る場合の構成

```
本社 VM1 / 外部Gateway ホスト
├── BOXアダプタ（TB-2）              ← 独自形式 → 標準ペイロード + 重複排除
│     └─ siteId で publish 先を振り分け
├── thin-edge インスタンス [site-001]  ← main device: site-001:GW-BOX
│     └── child: site-001:BOX-xxxx …
├── thin-edge インスタンス [site-002]
└── …
```

- 各インスタンスの main device MO を**その拠点グループの `childAssets` に置く**（1 拠点 1 回）。child（BOX）は接続階層で自然に配下に入る
- 構成コードは**プロファイル B を 1 つ書き、拠点変数（`siteId`・証明書・ブローカーポート）で N 個生成**する
- 拠点数が多く運用が煩雑になる場合は「拠点グループ単位」で束ねる中間解も取れる（実装は同一）

### 2.6 プロファイルの管理方式

**[判] 共通ベース + オーバーレイ 2 種**として構成コード化します（D10: 手作業カスタム禁止）。

```
tedge-profiles/
  base/                       # A/B 共通（§5）
    tedge.config.yaml         # c8y.url, root_cert_path, mqtt, http …
    operations.allow.toml     # supported operations の許可/禁止（§5.8）
    logging.yaml  health.yaml
  profile-a-analyzer/         # 画像解析装置（§6）
    plugins/onvif-health/     # カメラ死活監視
    sm-plugins/ai-model/      # AIモデル適用
    tedge-configuration-plugin.toml
    tedge-log-plugin.toml
  profile-b-boxgw/            # 外部Gateway（§7）
    tedge-configuration-plugin.toml   # GW 自身の設定のみ
    child-registration/               # BOX の entity 登録定義
  sites/                      # 拠点ごとの値のみ（証明書・siteId・カメラ台数）
    site-001.yaml  site-002.yaml …
```

> **この分離ができているかが、2 拠点目・2 案件目を `sites/` の追加だけで立ち上げられるかを決めます**（[設定書] §0.3 の L0/L1-B/L1-C 分離と同じ思想）。

---

## 3. 作業構造（WBS）

### 3.1 フェーズ定義

**T1（Edge）と T2/T3（thin-edge）は並行で進みますが、W1 と W5 で必ず合流します。**

> ⚠️ **表中の「[設定書] 対応」列は [設定書] 自身が持つ 2 系統の `P` 記法を区別しています。** [設定書] §1「着手前に固定すべき前提」は **`P-0`〜`P-4`（ハイフン付き）**、[設定書] §5「適用手順」は **`P0`〜`P7`（ハイフンなし）** を使い、意味が異なります（例: `P-2` はドメイン/TLS の前提、`P2` はテナント基盤フェーズ）。**これは表記の揺れではなく [設定書] 側の既存の体系**なので、本書もそれに合わせて使い分けています。

| フェーズ | 内容 | T1 Edge | T2/T3 thin-edge | [設定書] 対応 | 完了ゲート |
|---|---|---|---|---|---|
| **W0** | **前提・規約の確定** | 入力値の受領（§1.3） | プロファイル設計の確定（§2） | P-0〜P-4 | **C-1〜C-5 が文書として存在し、TB-1〜TB-5 の担当が決まっている** |
| **W1** | **検証環境の構築と重大未確定の解消** | 検証 Edge の構築 | 検証装置 1 台 + 疑似 BOX | P0 0-1 | **SV-35 / SV-05 / SV-25 / TE-3〜TE-8 に結論が出ている。**（TE-1・TE-2 はベンダー照会が前提のため別途トラッキング。§8 W0 参照） |
| **W2** | **本番 Edge の設置**（冪等化不可・1 回きり） | DNS/TLS/ライセンス/インストール | — | P0 | 両テナント URL でログイン可能 + リストア試験成功 |
| **W3** | **Cumulocity テナント設定** | テナントオプション・ロール・拠点グループ・テナント CA | — | P1〜P4 | 設定の往復差分ゼロ |
| **W4** | **thin-edge プロファイルの実装** | supported operations 受け入れ確認 | プラグイン開発・標準イメージ化 | — | プロファイル A/B が検証環境で通しで動く |
| **W5** | **パイロット拠点の通し** | — | 実拠点 1 + BOX 1 系統 | P7 の一部 | E2E（検知 → イベント → アラーム → 通知 → 映像確認）が成立 |
| **W6** | **SSO・データ・通知・アプリ** | P5/P6 一式 | — | P5, P6 | 否定テスト（拠点分離）合格 |
| **W7** | **全拠点展開と受け入れ検証** | 負荷試験・監視接続 | 全拠点プロビジョニング | P7 | §10 の Ready for Service チェックリスト全項目 |

### 3.2 クリティカルパス

```
C-1/C-2 規約確定 ─┬─► W3 alarm.type.mapping（W3-8） ─► W6 リテンション（W6-2）
                  ├─► W4 thin-edge の entity 登録・ペイロード実装
                  └─► 画像解析側・BOXアダプタ側の実装（他チームが待っている）

SV-35 の結論 ─────► 拠点分離方式の確定 ─► W3 グループ設計 ─► W6 通知購読（W6-5）

TB-1 の決着 ──────► W4 標準イメージ・W5 プロビジョニング（実施主体が決まらない）

TB-3 の決着 ──────► W6 トークン発行プロキシ（W6-5）— 決めないと通知経路全体が止まる
TB-4 の決着 ──────► W6 オフロードバッチ（W6-6）— 90 日以内が期限
TB-5 の決着 ──────► W7 メタ監視・Edge 監視の接続（W7-3）— 基盤異常検知の唯一の経路

W2（設置）─────► 後戻り不可。ドメイン・TLS・storageClassName・CIDR は install 後に変更できない
```

> ⚠️ **W0 を「調整ごと」として軽く見ないでください。** C-1/C-2 は他チームの実装前提であり、**本チームの遅れがそのまま画像解析側・BOXアダプタ側・案件アプリ側の遅れになります**。逆にここが早く出れば、本チームの実装が終わる前から相手側が動けます。

---

## 4. Cumulocity Edge（T1）— 担当作業と設定

> **実施手順の正は [設定書] §5 です。本節はその中から本チームの担当分を抜き出し、[設定書] が「[要] 実値未定」としていた項目に具体値を与えます。**

### 4.1 [設定書] の項目 ID と本書の対応

| [設定書] 項目群 | 内容 | 本書での扱い |
|---|---|---|
| E-01〜E-11 | Edge 基盤層（L0） | [設定書] §3.1 のまま。**本書は §4.5 で「デバイス接続に効く前提」だけを再掲** |
| R-01〜R-09 | ロール・ユーザー | [設定書] のまま。**R-08（証明書アップロード用ローカルユーザー）はデバイス登録の必須前提**（§4.4） |
| T-01〜T-04 | テナントオプション | **T-03 `alarm.type.mapping` の実値を §4.6 で確定** |
| X-01〜X-03 | データ保持 | [設定書] のまま |
| S-01〜S-04 | ルール・イベント規約 | **S-04 の実体は C-1。§4.2 で型名を確定** |
| N-01〜N-04 | Notification 2.0 | [設定書] のまま。**購読の `source` が §4.3 のグループ設計に依存** |
| **G-01〜G-03** | 拠点グループ | **§4.3 で階層を確定** |
| **D-01〜D-09** | デバイスオンボーディング（**D-08/D-09 は本書での新規追加。[設定書] の D-01〜D-07 を拡張**） | **§4.4 で方式と実値を確定。本書の中核** |
| M-01〜M-03 | モデル配布 | §6.3 と対で設計 |
| O-01 | Edge 自体の監視 | TB-5 |

### 4.2 イベント/アラーム型の確定（C-1 / S-04）

**[判] 本チーム起案。[カメラ構成] §4.2/§4.3 と [連携仕様] §4.3 の命名規約を統合し、全型を一覧化します。**

| 種別 | 型名 | source | 生成元 | 用途 |
|---|---|---|---|---|
| Measurement | `x_CameraHealth` | カメラ child device | thin-edge A（ONVIF プラグイン） | 死活の定期記録（`rtt`・応答可否） |
| Event | `x_Detection_<種別>` | **対象カメラ** child device | 画像解析パイプライン（IF-S04）／BOXアダプタ（IF-H01） | AI 検知。`x_Detection` フラグメント必須（`eventUuid` / `modelVersion` / `aiProduct` / `confidence` / `clipHint`） |
| Event | `x_ModelApplied` | 画像解析装置 | thin-edge A（sm-plugin） | モデル適用完了。`modelVersion` 切替時刻の追跡（IF-S07） |
| Alarm | `c8y_UnavailabilityAlarm` | 画像解析装置 / BOXゲートウェイ | **Cumulocity（`c8y_RequiredAvailability`）** | ゲートウェイ断 |
| Alarm | `x_CameraDown` | カメラ child device | thin-edge A（ONVIF プラグイン） | カメラ無応答 N 回連続（既定 3 回） |
| Alarm | `x_BoxSilent` | BOX child device | BOXアダプタ | BOX 無通信タイムアウト |
| Alarm | `x_BoxParseError` | BOXゲートウェイ | BOXアダプタ | 変換不能ペイロード（デッドレター退避時） |
| Alarm | `x_ModelUpdateFailed` | 画像解析装置 | thin-edge A（sm-plugin） | モデル適用失敗・ロールバック実施 |
| Alarm | `x_Alarm_<種別>` | 対象カメラ | Smart Rules / EPL | 検知イベントからの昇格（**判定ロジックは案件側保守・D7**） |

> ⚠️ **`x_Detection_<種別>` の `<種別>` 一覧は案件要件です。** 本チームは**型名の文法（`x_Detection_` プレフィックス + PascalCase）と必須フラグメントだけ**を規約とし、種別の列挙は案件側から受領します。**列挙を基盤側で固定すると 2 案件目で壊れます。**

> ⚠️ **カスタムフラグメント名も `x_` プレフィックスで統一**する規約とします。[連携仕様] §4.3 が定義済みなのは `x_Detection`, `x_Camera`, `x_CameraDown`, `x_CameraHealth`, `x_BoxSilent`, `x_BoxParseError` です。**`x_SensingBox` と `x_Site` は本書が新たに定義する項目**（[連携仕様] にはまだ無い。§4.3 のデバイスモデル拡張に伴う追加）で、[連携仕様] の改訂として反映してください。**`x_NoAutoConfig` は [設定書] G-10 の「運用知識基盤の自動設定変更からデバイスを除外するタグ」であり、本節（カスタムフラグメント命名規約）とは無関係な別概念**なので削除しました。

### 4.3 デバイスモデルと拠点グループ階層（G-01〜G-03）

**[判] 案 β（§2.5）採用時の最終形**:

```
[group] 拠点 (root)                      extID: sites
├── [group] 拠点A                        extID: site-001    ★Inventory ロールの割当単位
│   │                                                        ★Notification 2.0 の source
│   ├── [device] 画像解析装置             extID: site-001:ANLZ-<serial>
│   │   │                                 type:  x_ImageAnalyzer     ← thin-edge A の main device
│   │   └── [child] IPカメラ ×N           extID: site-001:CAM-<serial>
│   │                                     type:  x_Camera
│   └── [device] BOXゲートウェイ          extID: site-001:GW-BOX
│       │                                 type:  x_BoxGateway        ← thin-edge B の main device
│       └── [child] 画像センシングBOX ×M  extID: site-001:BOX-<serial>
│                                         type:  x_SensingBox
└── [group] 拠点B …
```

**MO 種別ごとのフラグメント定義**:

| MO 種別 | `com_cumulocity_model_Agent` | `c8y_SupportedOperations` | `c8y_RequiredAvailability` | 必須カスタムフラグメント |
|---|---|---|---|---|
| 画像解析装置 | **あり** | フルセット（§5.8） | **あり**（§4.5） | `x_Site{siteId}` |
| IP カメラ（child） | なし | **なし** | **`responseInterval: 0`（メンテナンスモード）** ← §4.5 の落とし穴 | `x_Camera{vmsCameraId, location}` ← **`vmsCameraId` は必須。設定漏れは登録ツールがエラー**（IF-P02） |
| BOXゲートウェイ | **あり** | **縮退**（§7.3） | **あり** | `x_Site{siteId}` |
| 画像センシングBOX（child） | なし | **なし** | **`responseInterval: 0`** | `x_SensingBox{model, fwVersion, channels}` |

> ⚠️ **`com_cumulocity_model_Agent` を child device に付けないでください。** 付けるとオペレーションの宛先候補になり、実行できないオペレーションが PENDING のまま滞留します（オペレーションのリテンション X-01 を圧迫）。

**グループ設計の規約**:

| # | 規約 | 理由 |
|---|---|---|
| G-a | **すべてのグループに external ID を振る**（`site-001` 等） | 名前だけで参照すると再実行で重複グループが生える（[設定書] §4.1） |
| G-b | **拠点コードは英数字のみ**（C-3） | Notification 2.0 の `subscription` 名がパターン `^[a-zA-Z0-9]+$` [確]。グループ表示名（日本語可）とは別に持つ |
| G-c | **Inventory ロールは拠点グループに割り当てる**（デバイス個別に割り当てない） | 親グループ → サブグループ → デバイスへ継承される。個別割当は SSO の `inventoryMappings` に上書きされる（[設定書] ガード 3） |
| G-d | **段階ロールアウトの単位も拠点グループ**（G-5） | bulk operation の `groupId` がグループ指定を前提とする [確] |

### 4.4 デバイスオンボーディング（D-01〜D-09）の確定

> ⚠️ **D-08・D-09 は本書が新たに追加する項目です。** [設定書] は自身の D-nn 体系を「D-01〜D-07」と明示的に限定しています（[設定書] §3.3）。本書での拡張は [設定書] の改訂として正式に反映するか、[設定書] 側の再改訂時にこの2項目を吸収してもらう必要があります。**放置すると [設定書] 側が別の内容で「D-08」を採番した場合に衝突します。**

| ID | 項目 | **[判] 本書の決定** | 根拠・注意 |
|---|---|---|---|
| **D-01** | 登録方式 | **テナント CA + 証明書エンロール**（[設定書] 方式(1)） | thin-edge.io 公式も推奨。自動登録（方式(2)）は「有効なままだと承認なしにデバイスが自動登録される」ため、使う場合も**一括登録完了後に必ず無効化**（P4 4-6） |
| **D-02** | 登録主体 | **プロビジョニングツール（構成コード）**。手作業登録を禁止（D10・IF-P02） | 前提: **`ROLE_TENANT_ADMIN` または `ROLE_TENANT_MANAGEMENT_ADMIN` を持つローカルユーザー（R-08）**。⚠️ **SSO ユーザーでは証明書アップロードが実行できない** [確] |
| **D-03** | device type 規約 | `x_ImageAnalyzer` / `x_Camera` / `x_BoxGateway` / `x_SensingBox`（§4.3） | リテンション・スマートルール・購読フィルタの結節点 |
| **D-04** | `c8y_RequiredAvailability` | §4.5 で実値提案 | 値域 `-32768`〜`32767`。範囲外は境界に丸められる [確] |
| **D-05** | supported operations | §5.8（共通方針）・§7.3（B の縮退） | thin-edge の c8y-mapper が申告する。**Cumulocity 側で減らすのではなく装置側で制御する** |
| **D-06** | external ID | **`{siteId}:{機器種別}-{シリアル}`**（[連携仕様] §4.1） | ⚠️ **thin-edge の自動登録が付ける既定形式と一致しない。§5.5 の `@id` 明示で統制する** |
| **D-07** | child device 登録 | **本チームのプロビジョニングツールが明示登録**（自動登録に頼らない） | IF-P02 手順 4。`x_Camera.vmsCameraId` の設定漏れはツールがエラーにする |
| **D-08**〈新規〉 | **BOX の拠点グループ編成** | 案 β なら GW main device を拠点グループへ 1 回。案 α なら **BOX 全台を `childAssets` へ追加** | §2.4 (1)。冪等化は 409 黙殺（[投入ガイド] パターン E） |
| **D-09**〈新規〉 | **アラームストーム抑止** | §4.5 の「死活の一次責務を 1 箇所に寄せる」設計 | アラームは `CLEARED` にならない限り消えない（[設定書] X-01） |

> ⚠️ **テナント CA は毎年 10 月 2 日 02:00 に自動更新されます** [確]（[設定書] §4.2）。**日付が確定した障害リスク**です。閉域網で再エンロールが期待通り動かないと、深夜帯に全拠点のデバイス証明書が順次失効します。**W1 の検証環境でシミュレートし（SV-25）、証明書有効期限をメタ監視に入れ、運用カレンダーに載せてください。**

### 4.5 死活監視の設計（D-04 / D-09）

**[判] 死活検知の一次責務を対象ごとに 1 箇所へ寄せます。** 二重に検知させると、同じ事象で 2 種類のアラームが出て運用が混乱し、片方だけクリアされて残ります。

| 対象 | 一次検知の担当 | アラーム型 | `c8y_RequiredAvailability` |
|---|---|---|---|
| 画像解析装置 | **Cumulocity**（thin-edge のハートビート） | `c8y_UnavailabilityAlarm` | **設定する** |
| BOXゲートウェイ（thin-edge B） | **Cumulocity**（同上） | `c8y_UnavailabilityAlarm` | **設定する** |
| IP カメラ | **thin-edge A の ONVIF 監視サービス** | `x_CameraDown` | **`responseInterval: 0`（無効化）** |
| 画像センシング BOX | **BOXアダプタ**（無通信タイムアウト） | `x_BoxSilent` | **`responseInterval: 0`（無効化）** |

#### ⚠️⚠️ 落とし穴: thin-edge は既定で **全 child device に** `c8y_RequiredAvailability` を設定する [確]

thin-edge.io 2.0.1 の公式ドキュメントに逐語で:

> *"%%te%% main and child devices set their required interval during their first connection. Availability monitoring is enabled by default with a default required interval of 1 hour."*

つまり **何もしないと、代理登録した全カメラ・全 BOX に `c8y_RequiredAvailability = 60分` が付きます**。カメラ・BOX は自分でハートビートを送らないため（既定のヘルス判定元はそのデバイス上の `tedge-agent` サービスで、代理登録した child には存在しない）、**接続から 1 時間後に全 child が一斉にアラームを上げると推測されます** [推]。thin-edge.io 側のドキュメントは「ハートビートが送られなくなると Cumulocity 側が unavailable と判定する」ところまでしか述べておらず、その結果 Cumulocity がどのアラーム型を生成するか（`c8y_UnavailabilityAlarm` という名称）は Cumulocity 側の一般仕様からの推論です。**ただし本チームの対処方針（§4.5・下記）はこの推論の正否に関わらず有効**（child は最初からメンテナンスモードにするため）なので、設計判断としては未検証のままで問題ありません。

数百台 × N 拠点の規模では、これは**初回接続の 1 時間後に数百〜数千件のアラームが同時発生する**ことを意味します。しかもアラームは `CLEARED` にならない限り消えません。

**[判] 対処方針**:

| # | 対処 | 内容 |
|---|---|---|
| 1 | **ゲートウェイの死活は thin-edge のハートビート機構を使う** | `tedge config set c8y.availability.interval <値>`。**1 分未満または 0 を設定すると無効化される** [確]。BOXゲートウェイは BOX イベントが疎らでも無通信にならないよう、ハートビートが必須 |
| 2 | **child device は個別にメンテナンスモードへ落とす** | 登録時に twin で `c8y_RequiredAvailability = {"responseInterval": 0}` を設定（`te/device/<child>///twin/c8y_RequiredAvailability`、retained）**[要 TE-5: 実機で child への twin 反映を確認]** |
| 3 | **代替案: `@health` を使う** | child 登録メッセージの `@health` に ONVIF 監視サービスの topic id を指定すると、そのサービスが `up` の間だけハートビートが送られ、`down` になると Cumulocity 側が自動的に「利用不可」判定する [確]。**公式機構だが、アラーム型が `c8y_UnavailabilityAlarm` になり [連携仕様] の `x_CameraDown` 規約と食い違う**ため、本書は 2 を採る |

**`responseInterval` の提案値**（単位: 分。Cumulocity 側の値域は `-32768`〜`32767` [確]）:

| 対象 | 提案値 | 設定箇所 | 根拠 |
|---|---|---|---|
| 画像解析装置 | **15** | `c8y.availability.interval = 15m` | 死活計測が 1〜5 分間隔（IF-S08）。WAN の瞬断・再接続で誤報しない余裕を取る。**過小だと回線瞬断のたびに拠点数ぶんのアラームが上がる** |
| BOXゲートウェイ | **10** | `c8y.availability.interval = 10m` | 同一サーバールーム内で WAN 断の影響を受けないため短くできる |
| IP カメラ / BOX | **0（無効）** | 登録時 twin | 死活は `x_CameraDown` / `x_BoxSilent` に一元化 |

> ⚠️ **設定箇所を Cumulocity 側と thin-edge 側で二重管理しないでください。** ゲートウェイの `c8y_RequiredAvailability` は **thin-edge の `c8y.availability.interval` が唯一の情報源**とし、Cumulocity 側から手で書かないこと。書くと次回接続時に thin-edge の値で上書きされ、「なぜか設定が戻る」という切り分けの難しい事象になります。

> ⚠️ **[要] 上記は提案値です（SV-26）。** 確定には「許容できる検知遅れ」と「実測された WAN 瞬断の頻度・長さ」が要ります。**W1 の検証環境で瞬断を再現し、W5 のパイロット拠点で実測してから固定してください。**

### 4.6 `alarm.type.mapping` の実値（T-03）

`<SEVERITY>|<TEXT>` 形式でアラーム型ごとに severity とテキストを上書きします。severity `NONE` で抑止できます [確]。

| キー（アラーム型） | 値 | 自動クリア条件（S-03） |
|---|---|---|
| `c8y_UnavailabilityAlarm` | `MAJOR\|ゲートウェイ応答なし` | 通信再開時（Cumulocity が自動） |
| `x_CameraDown` | `MAJOR\|カメラ応答なし` | 応答再開時（thin-edge プラグインがクリア） |
| `x_BoxSilent` | `MAJOR\|BOX無通信` | 受信再開時（BOXアダプタがクリア） |
| `x_BoxParseError` | `WARNING\|BOXペイロード変換不能` | **要決定**（デッドレター処理後に手動クリアか、時限自動クリアか） |
| `x_ModelUpdateFailed` | `CRITICAL\|モデル適用失敗` | 再適用成功時 |
| `x_Alarm_<種別>` | **案件側が決定** | **要決定**（[設定書] §4.4 でも未決） |

> ⚠️ **自動クリアを設計しないとアラームが単調増加します。** リテンションは **`CLEARED` のアラームしか削除しません** [確]（[設定書] X-01）。全アラーム型についてクリア条件を決めてください。

### 4.7 デバイス接続に効く Edge 基盤層の前提（再掲）

**本節は [設定書] §1・§3.1 の再掲ですが、ここを外すと thin-edge が一切つながらないため、W2 の前に本チームが確認する項目として列挙します。**

| # | 前提 | 外すとどうなるか | [設定書] |
|---|---|---|---|
| 1 | **公開ポート 443 / 8443 / 1883 / 8883** が LoadBalancer で開いている | thin-edge の MQTT（8883）と REST（443）が到達不能 | P-0-2 |
| 2 | **TLS 証明書の SAN に両ホスト名**（`<domain>` と `management-<domain>`） | ワイルドカードは 1 ラベル階層しかカバーしない [確]。management 側が繋がらない | P-2 |
| 3 | **社内 CA ルートを thin-edge の信頼ストアに配布**（`c8y.root_cert_path`） | TLS ハンドシェイク失敗。**Edge operator 側にも同じ CA が必要**（E-11 `ca.crt`） | P-2, E-11 |
| 4 | **`c8yedge-operator-config` の `no_proxy` に両テナントドメイン + Pod CIDR + Service CIDR** | 閉域網で Edge 内部通信が壊れる | E-11 |
| 5 | **NTP** が Edge ホスト・画像解析装置・外部Gateway に設定されている | TLS の有効期限判定・JWT 検証・イベントと映像の突合が**同時に**壊れる（IF-P03） | P-0-3 |
| 6 | **CIDR 衝突なし**（K3s の Service/Pod CIDR と社内網） | 「Web UI は動くが一部だけ到達しない」散発的症状 | P-0-1 |
| 7 | **Messaging Service が稼働している** | Notification 2.0 が動かず**案件アプリにアラームが一切届かない** | N-03, P1 1-5 |

> ⚠️ **1〜6 は `c8yedge install` の後では変更できません**（[設定書] P-0）。**W2 に入る前に潰し切ってください。**

---

## 5. thin-edge.io 共通設計（プロファイル A / B 共通）

> **本節の記述は thin-edge.io の公式ドキュメント（バージョン 2.0.1）に基づきます。** 逐語根拠のあるものは [確]、ソースコード由来は [確S]、本チームの判断は [判] を付けます。出典は §12。

### 5.1 バージョン方針とパッケージ構成

| 項目 | **[判] 決定** | 根拠 |
|---|---|---|
| 基準バージョン | **2.0.1 系に固定**。A/B で同一版・同時アップグレード | 版が割れると mapper の挙動・supported operations が装置ごとに変わり、Cumulocity 側の運用が装置別になる |
| ⚠️ 1.x → 2.x | **2.0.0 は破壊的変更**（旧 `tedge/` トピック廃止・**built-in bridge 既定化**）。**1.x で作り始めないこと** | [確] リリースノート |
| リソース要件 | ゲートウェイ **RAM 約 40MB 以上** [確] | 公式 *"At least ~40MB of RAM on the gateway device"* |

#### パッケージ構成 [確]

thin-edge.io の実バイナリは `tedge` と `tedge-p11-server` の 2 つだけで、他は `tedge` へのシンボリックリンク（multi-call バイナリ）です。

| パッケージ | 本チームの採否 | 理由 |
|---|---|---|
| `tedge` | **✅ 必須** | CLI・設定・MQTT。他すべての依存元 |
| `tedge-mapper` | **✅ 必須** | Cumulocity 変換 + **built-in bridge** + c8y-proxy + **証明書自動更新の systemd unit を同梱** |
| `tedge-agent` | **✅ 必須** | オペレーション実行・File Transfer Service |
| `tedge-watchdog` | **✅** | サービス死活（§5.9） |
| `tedge-apt-plugin` | △ | OS パッケージを Cumulocity から更新する場合のみ。**AI モデル配布には不要**（独自 sm-plugin） |
| **`c8y-remote-access-plugin`** | **❌ 導入しない** | §5.10。閉域網で対話的な遠隔接続経路を作らない |
| `c8y-firmware-plugin` | ❌ | `c8y_Firmware` は使わない（§5.8） |
| `tedge-p11-server` | ❌（将来検討） | HSM/PKCS#11 で秘密鍵を保護する場合。**`tedge-full` にも含まれない** |
| **仮想パッケージ `tedge-full`** | **❌ 使わない** | **`c8y-remote-access-plugin` が自動で入る** [確] |

> ⚠️ **`tedge-full` を使わないでください。** 依存に `c8y-remote-access-plugin` が含まれており、§5.10 で無効化したいものが毎回入ってきます。**個別パッケージ指定が本チームの標準**です。

#### ⚠️⚠️ 閉域網（エアギャップ）導入 — **公式手順が存在しない**

| 事実 | 確度 |
|---|---|
| 配布はすべて **Cloudsmith**（`dl.cloudsmith.io`）経由。apt リポジトリ `tedge-release` 等 | [確] |
| **GitHub Releases にバイナリ資産が無い**（assets: 0） | [確] |
| 公式ドキュメントに**オフライン / エアギャップ導入の記載が一切ない**（`.deb` 事前取得 → `dpkg -i` も、リポジトリのミラーリングも未記載） | [確] |
| バージョン固定は `install.sh <VERSION>`（apt では `/etc/apt/preferences.d/tedge` に Pin-Priority 1001 を書く） | [確] |
| ⚠️ tarball モードは `<VERSION>` を**無視**して latest を取る（URL ハードコード） | [確] |

> ⚠️ **[判 / 作業・要 TE-17] W0 の必須作業: 社内配布経路の構築。**
> 1. Cloudsmith から対象バージョンの `.deb`（または `.apk` / `.rpm`）を**事前に取得して資産管理する**
> 2. 社内 apt リポジトリを立てる、または構成コードで `dpkg -i` する
> 3. **バージョンピンを構成コードに含める**（勝手に上がらないようにする）
> 4. 脆弱性対応のための**取得 → 検証 → 配布のリードタイム**を運用手順に織り込む
>
> これは「あとでやる」ではなく、**W1 の検証環境を作る時点で必要**になります。**方式（社内 apt リポジトリ / `dpkg -i` 直接配布のどちらにするか）とリードタイムの具体値は TE-17 として未確定**です。

> ⚠️ **[要 TE-1] thin-edge.io の商用サポートの有無を確認してください**（vendor-questions V20）。D16 は「開発元がメンテナンスする OSS に配管を委ねる」ことを採用根拠にしましたが、**商用サポートが無ければ障害時の切り分け責任がすべて本チームに来ます**。これは D16 の前提そのものに関わります。

> ⚠️ **[要 TE-2] Cumulocity Edge と thin-edge.io のバージョン互換マトリクスを確認してください**（同 V20）。50 拠点規模のフリートを将来アップグレードする手順・ダウンタイムの想定が必要です。

### 5.2 配置形態 — ホスト直インストールかコンテナか

| 観点 | ホスト直 | コンテナ |
|---|---|---|
| `c8y_Restart`（ホスト再起動） | 可能 | 不可（コンテナ再起動になる） |
| 設定管理でホスト上のファイルを配る | 可能 | バインドマウントした範囲のみ |
| **sm-plugin が AI モデルを配置し、パイプラインのコンテナを再起動する**（IF-S07） | **可能**（docker ソケット経由） | 可能だが特権付与が要る |
| ログ収集（journald） | 可能 | 追加設定が要る |
| 1 ホストに複数インスタンス（案 β） | 面倒（設定ディレクトリ・ポートの分離） | **容易** |

**[判] 決定**:

| 対象 | 形態 | 理由 |
|---|---|---|
| **プロファイル A（画像解析装置）** | **ホスト直インストール** | sm-plugin がホスト上のモデル配置ディレクトリに書き、Compose のパイプラインコンテナを再起動する必要がある（IF-S07）。構成図でも thin-edge.io は Docker Compose の**外**に描かれている |
| **プロファイル B（外部Gateway）** | **コンテナ**（案 β のとき）または ホスト直（案 α のとき） | 拠点ごとに N インスタンスを立てる案 β ではコンテナが自然 |

> ⚠️ **プロファイル A をホスト直にすると、thin-edge が「画像解析装置のホスト OS を操作できる部品」になります。** `c8y_Restart` はホストを再起動し、設定管理はホストのファイルを書き換えます。**これは TB-1（筐体の所有・OS 管理主体）を決めずに進められない**ことを意味します。

#### コンテナ運用の実情 [確]

| 事実 | 帰結 |
|---|---|
| **公式イメージは実在する**（`ghcr.io/thin-edge/tedge:2.0.1`。Docker Hub には無い） | 利用可能 |
| ⚠️ **公式ドキュメントにコンテナ導入のユーザー向けページが無い**（開発者向けのみ） | 手順を本チームが確立する必要がある |
| イメージは **Alpine ベース・apk 由来 → systemd unit は入らない**。1 コンテナ 1 プロセス | `tedge-watchdog` / `tedge-cert-renewer@.timer` が**そのままでは使えない**。代替の死活・証明書更新の仕組みが要る |
| **mosquitto はイメージに含まれない**（外部ブローカー前提） | §5.7 のバッファ設定を持つ mosquitto を別途用意する |
| `reboot` が `kill -15 1`（= **コンテナ再起動**）に再定義されている | `c8y_Restart` の意味が変わる |
| 公式 org の **`ghcr.io/thin-edge/tedge-container-bundle`** は s6-overlay で mosquitto 込みの全部入り 1 コンテナ | **[判] 案 β を採るならこれが第一候補**。ただし §5.7 の mosquitto 設定を注入できるか要確認 |
| 設定は環境変数で注入できる（`TEDGE_C8Y_URL` 等。**キーの `.` を `_` に置換**） | 構成コードとの相性は良い |

> ⚠️ **[要 TE-11] 案 β を採る場合、コンテナ版で ①証明書の自動更新（systemd timer が無い）②`tedge-watchdog` の代替 ③mosquitto のバッファ設定注入 の 3 点をどう成立させるかを W1 で確定してください。**

### 5.3 identity と証明書ライフサイクル（IF-P01）

#### 登録フロー [確]

**Cumulocity の certificate-authority（EST / RFC 7030）を使う 2 ステップ**です。

```bash
# ① テナント側の事前準備（1 回きり）
c8y features enable --key certificate-authority
#   → Device Management > Management > Trusted Certificates > Add CA Certificate
#     または  c8y devicemanagement certificate-authority create

# ② オペレータ端末: デバイス ID とワンタイムパスワードを登録
c8y deviceregistration register-ca --id "$DEVICE_ID" --one-time-password "$OTP"

# ③ デバイス上: CSR 生成 → EST simpleenroll → 証明書取得
sudo tedge config set c8y.url "$C8Y_URL"
sudo tedge cert download c8y --device-id "$DEVICE_ID" --one-time-password "$OTP"
sudo tedge connect c8y
```

- `tedge cert download c8y` は **`https://<c8y-url>/.well-known/est/simpleenroll`** を呼びます [確S]
- 管理者が CSR を承認するまでポーリングします（`--retry-every 30s`、既定タイムアウト 10 分）[確]

#### 証明書の自動更新 — **systemd timer が同梱されている** [確]

`tedge-mapper` パッケージが **`tedge-cert-renewer@c8y.timer` / `.service`** を提供します。

| Step | systemd オプション | 動作 |
|---|---|---|
| 1 | `ExecCondition` | `tedge cert needs-renewal c8y` — 更新が必要か判定 |
| 2 | `ExecStartPre` | `tedge cert renew c8y --ca c8y` — **EST `simplereenroll`** で新証明書を取得（`.new` 接尾辞で保存。現行は温存） |
| 3 | `ExecStart` | `tedge reconnect c8y` — 新証明書で接続を検証し、成功したら差し替え |
| 4 | `ExecStopPost` | 検証に失敗した `.new` を削除 |

- タイマーは **毎時（`OnCalendar=hourly` + `RandomizedDelaySec=5m`）**、`Persistent=true` [確S]
- 判定基準は `certificate.validity.minimum_duration`（**既定 30 日**）[確S]（公式ドキュメントの表示は "Example: 30d" であり、既定値そのものはソースコードの `default(from_str = "30d")` で確認）
- 非 systemd 環境では上記を cron 等で自前実行する [確]

#### ⚠️⚠️ 証明書まわりの 5 つの落とし穴

| # | 落とし穴 | 内容 | 対処 |
|---|---|---|---|
| **1** | **更新は c8y-proxy 経由。つまりクラウド接続が生きていることが前提** | `tedge cert renew` は `c8y.proxy.client.host:port`（既定 `127.0.0.1:8001`）に対して `simplereenroll` を呼ぶ [確S]。**mapper が動いていて Cumulocity に繋がっていなければ更新できない** | 長期オフラインを許さない運用、または 2 の設定 |
| **2** | **失効したら現地作業になる** | 公式逐語: *"If for any reason the certificate expires, and the device can no longer communicate with Cumulocity, then the device will need to be registered again."* | **[判] `certificate.validity.minimum_duration` を「想定最大オフライン期間 × 3 以上」に設定する**（公式推奨。既定 30 日では 10 日以上の断で危険） |
| **3** | **Cumulocity Edge は自己署名証明書を使う** | 公式逐語: *"Updating the local certificate store is notably required to connect **Cumulocity Edge**, as this distribution of Cumulocity uses self-signed certificates to authenticate itself."* | 社内 CA / Edge の CA を `c8y.root_cert_path` またはシステム信頼ストアに配置（下記手順） |
| **4** | **MQTT 用と HTTP 用で信頼ストアの設定キーが別** | `c8y.root_cert_path`（MQTT）と **`c8y.proxy.ca_path`（HTTP）** は別キー [確]。⚠️ **`c8y.proxy.ca_path` は公式ドキュメントが「既定 `/etc/ssl/certs`」と書いているが、ソース上は既定値が無い**（不一致）[確S] | **両方を明示設定する**。片方だけだと「MQTT は繋がるが添付だけ失敗する」 |
| **5** | 古い openssl での rehash 不備 | 証明書を置いても `c_rehash` が効かず SSL verify エラーになる既知事象 [確] | 標準イメージのビルド時に検証する |

**社内 CA の配置手順** [確]:

```bash
sudo cp <ca>.crt /usr/local/share/ca-certificates/     # PEM 形式・拡張子 .crt・パーミッション 644
sudo update-ca-certificates
sudo tedge reconnect c8y
# もしくはシステムストアを触らず直接指定:
sudo tedge config set c8y.root_cert_path /etc/tedge/ca/internal-ca.pem
sudo tedge config set c8y.proxy.ca_path  /etc/tedge/ca/internal-ca.pem
```

#### その他

| 項目 | 内容 | 確度 |
|---|---|---|
| デバイス識別子 | `device.id`。**証明書の CN から導出される**。MQTT ClientId の `deviceIdentifier` になる | [確] |
| 鍵・証明書のパス | `/etc/tedge/device-certs/tedge-private-key.pem` / `tedge-certificate.pem` | [確] |
| 証明書有効期間 | `certificate.validity.requested_duration` 既定 **365 日** | [確S]（同上。ドキュメント表示は "Example"、既定値はソース確認） |
| 失効 | 拠点撤収・機器盗難時の失効手順を runbook 化。**監査ログ対象**（[連携仕様] §4.7） | [判] |
| 秘密鍵の保護 | `tedge-p11-server` による PKCS#11 / HSM 対応あり。**built-in bridge が必須**（mosquitto ブリッジでは動かない）[確] | 将来検討 |

> ⚠️⚠️ **[要 TE-7 / SV-25] 最大の運用リスク: テナント CA の自動更新（毎年 10/2 02:00）。**
>
> 閉域網で再エンロールが期待どおり動かないと、**深夜帯に全拠点のデバイス証明書が順次失効し、全拠点が同時に接続不能**になります。しかも**配布経路（Cumulocity 経由のソフトウェア更新）も同時に使えません**。
>
> **必須対応**: ①W1 の検証環境で時計を進めてシミュレートする ②証明書有効期限をメタ監視の対象にする（TB-5）③運用カレンダーに載せ、当日は要員を確保する ④新旧 CA を並行して信頼させる移行期間を設計に入れる（SV-22）
>
> ⚠️ **さらに、復旧局面での「輻輳」も想定してください。** テナント CA が失効すると全拠点のデバイスが**同時に**再エンロールを試みます。EST エンドポイントへの同時アクセスが集中すると、正常な運用時とは異なる負荷パターンが発生します。①段階的な再接続（拠点グループ単位でずらす）②再エンロールのリトライにジッター・バックオフを持たせる、といった緩和策を runbook（RB-4）に含め、**W1 の検証環境でシミュレートする際に、単一デバイスの再エンロールだけでなく複数デバイス同時実行のケースも試してください。**

> ⚠️ **[要 TE-8] Cumulocity の certificate-authority 機能が Edge で使えるかは未確認**（vendor-questions V8。**同機能のドキュメントに Edge への言及がない**）。使えない場合は「自前 CA を trusted certificate として登録する従来方式」に落ちますが、**その場合の証明書更新の自動化方式を別途設計する必要があります**。**W1 の最優先検証項目**（[設定書] SV-08 と同一）。

### 5.4 `tedge config` 設定値（共通ベース）

**[判] すべて構成コードで管理し、手作業の `tedge config set` を禁止します**（D10）。

| キー | 値 | 目的 | 確度 |
|---|---|---|---|
| `c8y.url` | `<edge-domain>` | 接続先 Edge | [確] |
| `c8y.mqtt` | 既定は `c8y.url:8883` | MQTT の接続先。Edge の公開ポート（§4.7 前提 1）と一致すること | [確] |
| `c8y.http` | 既定は `c8y.url:443` | REST（添付・ダウンロード）の接続先 | [確] |
| `c8y.root_cert_path` | 社内 CA バンドルのパス | **MQTT の** TLS 検証。既定 `/etc/ssl/certs` | [確] |
| **`c8y.proxy.ca_path`** | 同上 | **HTTP の** TLS 検証。⚠️ **キーが別。片方だけだと添付だけ失敗する**（§5.3 落とし穴 4） | [確] |
| `certificate.validity.minimum_duration` | **想定最大オフライン期間 × 3 以上**（既定 30d） | §5.3 落とし穴 2 | [確S] |
| `device.id` | `{siteId}-anlz` / `{siteId}-boxgw` | 証明書 CN から導出される | [確] |
| `device.type` | `x_ImageAnalyzer` / `x_BoxGateway` | Cumulocity の device type（§4.3） | [確] |
| **`mqtt.bind.address`** | **既定 `127.0.0.1`**。§5.6 の要注意点を参照 | パイプラインコンテナからローカル MQTT に到達させるため | [確] |
| `mqtt.bind.port` | 既定 `1883` | | [確] |
| `mqtt.bridge.built_in` | **`true`**（2.0 の既定） | HTTP プロキシ・HSM を使うなら必須 | [確] |
| `mqtt.bridge.reconnect_policy.*` | 既定 `initial 5s` / `maximum 10m` / `reset_window 5m` | 断からの復帰挙動 | [確] |
| `http.bind.port` / `.address` | 既定 `8000` / `mqtt.bind.address` を継承 | File Transfer Service | [確] |
| `proxy.address` / `.no_proxy` | 社内 HTTP プロキシがある場合 | ⚠️ **mosquitto ブリッジはプロキシ非対応。built-in bridge が必須** | [確] |
| **`c8y.entity_store.auto_register`** | **`false`** | **§5.5。external ID を規約どおりに統制するため必須** | [確] |
| **`agent.entity_store.auto_register`** | **`false`** | 同上（エージェント側の層。両方切る） | [確S] |
| **`c8y.availability.enable`** | `true` | ゲートウェイの死活（§4.5） | [確] |
| **`c8y.availability.interval`** | `15m`（A） / `10m`（B） | §4.5。**1 分未満または 0 で無効化される** | [確] |
| `c8y.topics` | 既定から絞る（§5.6） | 不要なトピックの転送を止め、Edge の負荷を下げる | [確] |
| `c8y.mapper.mqtt.max_payload_size` | 既定 `16184`（16KB） | §5.6 のペイロードサイズ規約の根拠 | [確S] |
| `c8y.proxy.bind.address` / `.port` | §5.10 | パイプラインからの添付経路 | [確] |
| `c8y.enable.*`（7 キー） | §5.8 | supported operations の取捨。**公式ドキュメントのコマンド例には5キーのみ掲載**（`device_restart`/`software_update`はソースコード `tedge_config.rs` でのみ確認） | [確S] |
| `tmp.path` | 既定 `/tmp`。**モデルファイルのダウンロード先**。容量を確認して必要なら変更 | [確] |
| `data.path` | 既定 `/var/tedge`。File Transfer Service の実体 | [確] |
| `logs.path` | 既定 `/var/log/tedge` | [確] |
| `software.plugin.default` | `aimodel`（A のみ・§6.3） | 既定 sm-plugin | [確] |

### 5.5 ⭐ エンティティ登録規約（C-5）— external ID を規約どおりに統制する

**これは本設計で最も間違えやすい箇所です。**

#### 問題

thin-edge は既定で**自動登録**が有効です。データが publish された時点で未登録のエンティティを child device として自動作成し、**external ID を自分の規則で生成します** [確]:

> *"Where the unique external IDs to be used in the cloud are derived from the entity identifier subtopics, replacing the `/` characters with `:`."*

生成される形式は **`<main-device-id>:device:<child-id>`** です。これは本構成の規約 **`{siteId}:{機器種別}-{シリアル}`**（C-2 / D-06）と**一致しません**。放置すると:

- 規約外の external ID を持つ MO が生える
- プロビジョニングツールが規約どおりに登録した MO と**二重に存在する**
- 重複排除キー・映像突合キー・ベクトル DB 紐づけキーの三重の役割（C-2）が壊れる

公式も明示しています [確]:

> *"For any complex deployments requiring external id customizations or with nested child devices, auto-registration **must be disabled**."*
> *"Combining auto-registration with explicit registration using the MQTT API is highly discouraged"*

#### **[判] 規約**

| # | 規約 | 具体 |
|---|---|---|
| **E-a** | **自動登録を必ず無効化する** | `tedge config set c8y.entity_store.auto_register false` および `agent.entity_store.auto_register false`。**変更後は `tedge-agent` の再起動が必要** [確] |
| **E-b** | **すべてのエンティティを明示登録し、`@id` で external ID を直接指定する** | `@id` を指定すると **prefix なしでそのまま external ID になる** [確]。これが規約 C-2 を守る唯一の方法 |
| **E-c** | **登録は HTTP API（`POST /te/v1/entities`）を優先する** | MQTT は成功/失敗のフィードバックが無い。公式も *"the REST API provides an added advantage of immediate feedback"* と述べる [確] |
| **E-d** | **child の topic id には `/` `+` `#` を使わない** | topic id は MQTT トピックの一部になる。`/` は external ID 生成時に `:` に変換される [確]。**英数字・`-`・`_` に限定する** [判] |
| **E-e** | ⚠️ **登録後に `@type` と `@id` は変更できない** | *"Updates are limited to the `@parent` and `@health` properties only"* [確]。**命名を間違えたら削除して作り直すしかない**。プロビジョニングツールで検証を強制する |
| **E-f** | 削除は同トピックへの**空の retained メッセージ** | 子孫も一緒に解除される [確] |

**登録メッセージの例**（カメラ）:

```bash
# 推奨: HTTP API
curl -X POST http://127.0.0.1:8000/te/v1/entities -H 'Content-Type: application/json' -d '{
  "@topic-id": "device/site-001-cam-SN12345//",
  "@type": "child-device",
  "@id": "site-001:CAM-SN12345",
  "name": "東門カメラ",
  "type": "x_Camera",
  "x_Camera": { "vmsCameraId": "gsc-cam-8842", "location": "東門" },
  "x_Site": { "siteId": "site-001" }
}'
```

> ⚠️ **`@type` / `@id` 以外の追加フィールドは twin データとして扱われ、Cumulocity の MO フラグメントになります** [確]。`x_Camera` / `x_Site` はこの仕組みで載ります。**`@` プレフィックスは thin-edge の予約**なので独自キーに使わないこと。

> ⚠️ **`vmsCameraId` の設定漏れは登録ツールがエラーにしてください**（IF-P02）。これは**イベント → 映像の紐づけを成立させる唯一の結合点**です（[カメラ構成] §4.1）。

### 5.6 ⭐ ローカル MQTT 受け口の IF 契約（C-4 / IF-S04・IF-H01）

**本チームが画像解析パイプライン担当・BOXアダプタ担当に提供する契約仕様です。** 両者で同一の契約とし、差分は「どの child ID を名乗るか」だけにします。

#### ⚠️ 接続先 — ローカル MQTT の bind アドレス

**`mqtt.bind.address` の既定は `127.0.0.1` です** [確]。画像解析パイプラインは **Docker Compose のコンテナ内**で動くため、**既定のままでは thin-edge のブローカーに到達できません**。

| 対処 | 内容 |
|---|---|
| **[判] 推奨** | `mqtt.bind.address` を **Docker ブリッジネットワークのゲートウェイ IP に限定**して bind する（`0.0.0.0` にしない） |
| 併せて | `http.bind.address`（File Transfer Service, 8000）・`c8y.proxy.bind.address`（8001, §5.10）も同様に必要。**3 つとも別キー** |
| ⚠️ | **同一ホスト上のどのコンテナからも到達できる**ことになる。§5.10 (1) の信頼境界の議論と同じ論点。TB-1 で整理すること |

#### トピックと publish 規約

| 種別 | トピック | retain | QoS | 備考 |
|---|---|---|---|---|
| 計測 | `te/device/<child-id>///m/<measurement-type>` | **禁止** [確] | 0 or 1 | |
| イベント | `te/device/<child-id>///e/<event-type>` | **禁止** [確] | 1 | |
| アラーム | `te/device/<child-id>///a/<alarm-type>` | **必須** [確] | **2（`>1` が要件）** [確] | |
| アラームのクリア | 同一トピックに**空メッセージ** | **必須** | 2 | retain を付けないとクリアされない [確] |

> ⚠️ **source はトピックの `<child-id>` が決めます。ペイロードの `source.id` は使われません** [確]。[カメラ構成] §4.3 の JSON（Cumulocity REST 形式）と混同しないこと（[連携仕様] §3.2 の表と同旨）。

#### ペイロード規約

**計測** [確]:
- キーは**英数字と `_` のみ。先頭 `_` は不可**
- 値は**数値のみ**。文字列・真偽値・オブジェクトは不可
- **ネストは 1 段まで**
- 自由形式の付帯情報は **`properties` サブオブジェクト**に入れる
- `time` はルート直下のみ。ISO 8601 文字列 または unix 秒

**イベント** [確]:
- `text` と `time` は任意。**それ以外のフィールドはすべてカスタムフラグメント**として Cumulocity に載る
- 本構成の必須フラグメント: `x_Detection { eventUuid, modelVersion, aiProduct, confidence, boundingBoxes, clipHint }`（[カメラ構成] §4.3・C-1）

**アラーム** [確]:
- `severity`: `critical` / `major` / `minor` / `warning`
- ⚠️ **thin-edge 側は「型 + severity」を一意のアラームとして扱う**。公式逐語: *"Every alarm is uniquely identified by its type and severity. That is, for a given alarm type, alarms of varying severities are treated as independent alarms and hence, must be acted upon separately."* severity を変えると別のアラームになるため、昇格させる場合は**旧 severity のアラームを別途クリアする**必要がある

> ⚠️ **[要] これは thin-edge のローカル MQTT トピック上の一意性規則であり、[連携仕様] §3.5・§4.5 が述べる「Cumulocity サーバー側の de-duplicate は ACTIVE 中の同一 `source` + `type` を対象とする（severity は条件に含まれない）」とは**別レイヤーの規則**です。両者は矛盾しているのではなく、「thin-edge → Cumulocity へどう投入するか」と「Cumulocity 内でどう重複判定するか」という異なる段階の話である可能性がありますが、**この 2 つの規則の相互作用（severity を変えて再送した場合に Cumulocity 側で何が起きるか）は未検証**です。C-1（イベント/アラーム型規約）確定前に、検証環境で実際に確認してください。

#### ⚠️ ペイロードサイズ規約 — 16KB の壁

| 事実 | 帰結 |
|---|---|
| `c8y.mapper.mqtt.max_payload_size` の既定は **16,184 バイト** [確S] | |
| **計測**: 上限超過のメッセージは**拒否される** [確] | 破棄される。気づけない |
| **イベント**: 16KB 未満なら MQTT、超えると **HTTP に切り替わる** [確] | 挙動が変わる。HTTP はストア&フォワードの対象外（§5.7） |

> **[判] C-1 の規約に「1 イベントのペイロードは 16KB 未満」を明記し、`boundingBoxes` の件数上限（例: 32 件）を定めてください。** 混雑シーンで検知数が増えると容易に超えます。超えた場合の挙動（切り詰めるか、別イベントに分けるか）も規約で決めること。

#### スナップショット添付の経路

**[判] `tedge upload c8y` コマンドを推奨します。** [連携仕様] §3.2 は「c8y-proxy 経由で REST を 2 回叩く」設計ですが、thin-edge が 1 コマンドで同じことを行います [確]:

> *"The command creates a new event for the device, attaches the given file content to this new event, and returns the event ID."*

```bash
tedge upload c8y \
  --file /var/spool/pipeline/snap-0198c1d4.jpg \
  --mime-type image/jpeg \
  --device-id site-001:CAM-SN12345 \
  --type x_Detection_Intrusion \
  --text "侵入検知: 東門エリア" \
  --json '{"x_Detection":{"eventUuid":"0198c1d4-...","modelVersion":"2026.06-r3","confidence":0.92}}'
```

| 論点 | 内容 |
|---|---|
| 利点 | イベント作成 + 添付 + イベント ID 取得が 1 回。認証は c8y-proxy が JWT で代行するため**パイプライン側に資格情報が不要** [確] |
| 制約 | **既存イベントへの後付け添付はできない**（新規イベント作成とセット）。**1 イベント 1 バイナリ**（2 件目は HTTP 409） |
| ⚠️ **網断耐性なし** | HTTP 経路は**ストア&フォワードの対象外**。[連携仕様] §3.2 のとおり**発行側のローカルスプールが必須** |
| ⚠️ 前提 | c8y-mapper が稼働し、**c8y-proxy がパイプラインのコンテナから到達できること**（§5.10） |
| ⚠️ 既知の不安定性 | *"requests to the proxy API are occasionally spuriously rejected with a `401 Not Authorized` status code"* [確] → **リトライを実装規約に入れること** |

#### エラーの見え方

- 自動登録を無効化した状態（E-a）で未登録エンティティのデータが来ると、**マッパーはそのデータを無視し、`te/errors` トピックにエラーを出します** [確]
- **[判] `te/errors` を監視し、件数をメトリクス化してください。** ここを見ないと「送ったのに Cumulocity に出ない」の切り分けができません

### 5.7 ストア&フォワード（オフラインバッファ）

#### ⚠️⚠️⚠️ 最重要 — 既定のままでは **数分の網断でイベントが欠落します**

**調査で判明した事実**:

| # | 事実 | 確度 |
|---|---|---|
| 1 | **thin-edge 2.0.0 で built-in bridge が既定化**（`mqtt.bridge.built_in = true`）。クラウド接続は `tedge-mapper` が直接処理し、mosquitto はローカル MQTT 専用になった | [確] |
| 2 | **それでもバッファの実体は mosquitto です。** built-in bridge はローカル mosquitto に `clean_session=false` + QoS 1 で購読し、**`manual_acks` によりクラウド側で PUBACK が返るまでローカルに ACK を返しません**。つまり切断中のメッセージは **mosquitto 内のブリッジクライアントのセッションキュー**に溜まります | [確S] |
| 3 | **thin-edge は `max_queued_messages` 等の mosquitto 設定を一切書きません。** 生成される `tedge-mosquitto.conf` にキュー関連のディレクティブは 1 つもありません | [確S] |
| 4 | **`tedge config` にバッファ容量を指定するキーは存在しません** | [確S] |
| 5 | したがって**すべて mosquitto の既定値**で動きます: `max_queued_messages` = **1000 件**、`persistence` = **false（非永続）**、`max_queued_bytes` = 0（無制限）、`queue_qos0_messages` = false | [確] |
| 6 | 上限到達時、**メッセージは黙って捨てられます**（*"subsequent messages will be silently dropped"*）。エラーは出ません | [確] |
| 7 | **MQTT 経路のみが対象。HTTP 経路（スナップショット添付・16KB 超のイベント）は対象外** | [確] |
| 8 | 公式ドキュメントに**ストア&フォワードの記載は一切ありません**（全文検索でヒット 0）。上記は製品ドキュメント + ソース + mosquitto のマニュアルからの再構成です | [確] |

#### この既定値が本構成で何を意味するか

**1 拠点 300 台・死活 60 秒間隔の場合**:

```
定常レート ＝ 300 台 ÷ 60 秒 ＝ 5 msg/s
既定バッファ 1000 件 ÷ 5 msg/s ＝ 200 秒  →  ⚠️ 約 3 分 20 秒の網断でオーバーフロー
```

**死活間隔を 5 分に緩めても**:

```
1 msg/s → 1000 件 ÷ 1 ＝ 1000 秒  →  ⚠️ 約 17 分でオーバーフロー
```

さらに **非永続（`persistence false`）なので、装置の再起動・瞬電でバッファは全消失**します。

> ⚠️ **[連携仕様] §4.5 の「閉域網断は『あるもの』として設計する」「thin-edge のストア&フォワードが吸収する」（D16）という前提は、既定設定では成立しません。** 設定しない限り、**網断 3 分でイベントが黙って消えます**。しかも検知手段がありません。

#### **[判] 必須対応: mosquitto の設定を構成コードで管理する**

`tedge config` にキーが無いため、**`/etc/mosquitto/conf.d/` に本チームの設定ファイルを置き、構成コードで配布します**（D10）。

> ⚠️ **以下の数値は暫定プレースホルダです。** `max_queued_messages` / `max_queued_bytes` の具体値は、下記「サイジング式」の入力（想定最長 WAN 断時間・バースト時レート）が確定してから W1 の実測（W1-4）を経て決めてください。**この例をそのまま本番投入しないこと。**

```conf
# /etc/mosquitto/conf.d/50-tedge-buffer.conf  ← 本チームの管理対象
max_queued_messages 200000        # ← 暫定値・要実測。既定 1000 から引き上げ。§5.7 のサイジング式で決める
max_queued_bytes    268435456     # ← 暫定値・要実測。256MB。件数だけでなくバイト上限も必ず置く（メモリ枯渇の防止）
persistence         true          # 再起動・瞬電をまたいで保持
persistence_location /var/lib/mosquitto/
autosave_interval   60            # 既定 1800 秒（30 分）だと最大 30 分ぶん失う
```

| 設定 | 既定 | 本書の方針 |
|---|---|---|
| `max_queued_messages` | 1000 | **サイジング式で算出**（下記）。0（無制限）は**推奨しない**（メモリ枯渇で装置全体が死ぬ） |
| `max_queued_bytes` | 0（無制限） | **必ず明示**。件数上限と併用して二重に守る |
| `persistence` | false | **true**。電源断のある拠点で非永続は許容できない |
| `autosave_interval` | 1800 秒 | **60 秒**。永続化の粒度 |
| `queue_qos0_messages` | false | **false のまま**。QoS 0 は Cumulocity の JWT 取得（`s/uat`/`s/dat`）にしか使われず、失われて構わない |

> ⚠️ **`max_queued_messages` は「クライアントごと」の上限です** [確]。built-in bridge・各 mapper・agent がそれぞれ別クライアントとしてカウントされます。実効サイジングは **「ブリッジクライアントが購読するトピックに流れる件数」**で見積もってください。

#### サイジング式（[連携仕様] §4.5・A7）

```
必要件数 ≒ 想定最長断時間[秒] × バースト時メッセージレート[msg/s] × 安全率(2)
必要バイト ≒ 必要件数 × 1 メッセージの平均バイト数（検知イベントは最大 16KB・§5.6）

プロファイル A: 1 拠点分（カメラ N 台の死活 + 検知バースト）
プロファイル B（案 α）: 全拠点分の BOX イベント合計  ← 案 β なら 1 拠点分に縮む
```

**確定に必要な入力値**（すべて **[要]**）:

| 入力 | 対応する未確定事項 |
|---|---|
| 想定最長 WAN 断時間 | **SV-29 / TE-10**（RTO/RPO と併せて決める） |
| バースト時の検知イベントレート | 案件要件 |
| 装置のディスク・メモリ余裕 | TB-1（筐体仕様） |

#### **[判] 欠落は「起きないようにする」のではなく「起きたら分かるようにする」**

上限を無限にするとメモリ・ディスク枯渇で装置全体が死に、被害が「一部欠落」から「全断」に悪化します。したがって:

| # | 対策 |
|---|---|
| 1 | **キュー使用率を測定値として送出**（`$SYS/broker/...` または自作サービス）→ 閾値超過をアラーム化 |
| 2 | **復旧後の到達件数を突合できるようにする** — 発行側が連番/`eventUuid` を持つため、オフロード先で欠番を検出できる |
| 3 | **W1 の検証環境で実測する** — 断を発生させ、復旧後の到達件数を数える。**「設定した」ではなく「溢れる時間を測った」を完了条件にする** |
| 4 | ブリッジの再接続挙動を確認（`mqtt.bridge.reconnect_policy.initial_interval` 5s / `maximum_interval` 10m / `reset_window` 5m）[確] |

> ⚠️ **[要 TE-3] Debian の mosquitto パッケージが同梱する `/etc/mosquitto/mosquitto.conf` の実効既定値を、導入先で必ず確認してください。** ディストリによっては `persistence true` が既に入っている場合があります。**「設定ファイルを置いた」ではなく「`mosquitto` の実効設定を確認した」を完了条件に**してください。

### 5.8 supported operations の取捨（D-05）

#### 仕組み [確]

- `c8y_SupportedOperations`（SmartREST **114**）は**固定リストではなく、`/etc/tedge/operations/c8y` 配下のファイル名の集合**から動的に生成される
- ファイルは **MQTT capability メッセージ**（`te/<topic-id>/cmd/<operation>` に retained publish）を受けてマッパーが自動生成する。**手動でのファイル追加は deprecated**
- child device 用は `/etc/tedge/operations/c8y/<child-device-xid>/`。**child の supported operations は「その child の topic id に capability メッセージを publish するかどうか」で完全に制御できる**
- 追加で **SmartREST 118 = supported logs / 119 = supported configs** が申告される

#### **[判] 取捨の決定**

| オペレーション | プロファイル A | プロファイル B | 制御方法 |
|---|---|---|---|
| `c8y_SoftwareUpdate` | **✅ 必須**（AI モデル配布・§6.3） | ❌ | `c8y.enable.software_update` |
| `c8y_DownloadConfigFile`（config_update） | **✅**（パイプライン設定・死活間隔） | ✅（GW 自身のみ） | `c8y.enable.config_update` + `agent.enable.config_update` |
| `c8y_UploadConfigFile`（config_snapshot） | **✅**（現在値の吸い上げ = 逸脱検出とロールバック退避の前提） | ✅ | `c8y.enable.config_snapshot` |
| `c8y_LogfileRequest` | **✅**（現地に行かずログを取る唯一の手段） | ✅ | `c8y.enable.log_upload` |
| `c8y_Restart` | ✅（**変更ウィンドウ制約つき**） | ✅ | `c8y.enable.device_restart` |
| `c8y_Firmware` | **❌ 無効化** | **❌** | `c8y.enable.firmware_update = false` |
| `c8y_DeviceProfile` | △（K-04 を採るなら ✅） | ❌ | `c8y.enable.device_profile` |
| **`c8y_RemoteAccessConnect`** | **❌ 無効化**（§5.10） | 運用判断 | **設定フラグは存在しない**。§5.10 の手順 |
| `c8y_Command` | **❌**（任意コマンド実行経路を作らない） | ❌ | オペレーション定義ファイルを置かない |
| **child（カメラ / BOX）の全オペレーション** | **❌ 一切申告しない** | **❌** | capability メッセージを publish しない |

> ⚠️ **child device に supported operations を申告しないでください。** カメラも BOX（改造不可）もオペレーションを実行できません。申告すると Cumulocity の UI に実行ボタンが出て、実行すると **PENDING のまま滞留**し、オペレーションのリテンション（X-01）を圧迫します。

> ⚠️ **child device の supported operations は動的に削除できません** [確]（*"Dynamic removal of an operation from the supported operation list is not supported for child devices."*）。**一度申告すると消せない**ため、最初から出さないこと。

> **[確] 再同期の手段**: 設定を変えたのに Cumulocity 側の一覧が古い場合は `tedge mqtt pub te/device/main/service/tedge-mapper-c8y/signal/sync '{}'` で main とすべての child の supported operations を再送できます。

### 5.9 ヘルス・ログ・監視

| 項目 | 内容 | 確度 |
|---|---|---|
| サービスヘルス | `te/<service-topic-id>/status/health` に `{"pid":…, "status":"up"|"down", "time":…}` を **retained** で publish。Cumulocity には SmartREST **102**（サービス作成）/ **104**（状態更新）としてサービス表示される | [確] |
| LWT | *"The services are also expected to register an MQTT Last Will and Testament (LWT) message"*。**自作サービス（ONVIF 監視・添付ワーカー等）には必ず LWT を実装する** | [確] |
| ウォッチドッグ | `tedge-watchdog` + systemd の `WatchdogSec`。**unit ファイルに `After=tedge-watchdog.service` / `WatchdogSec=30` / `Restart=always` を追加**しないと有効にならない | [確] |
| ログ出力先 | 既定は **journald**（`journalctl -u tedge-mapper-c8y` 等）。ソフトウェアオペレーションのログは `/var/log/tedge/agent` にファイル出力 | [確] |
| ログレベル | **`tedge config` のキーは存在しない**。`--log-level`（trace/debug/info/warn/error）または `/etc/tedge/system.toml` の `[log]` セクション | [確] |
| **メトリクス** | ⚠️ **thin-edge に Prometheus / OTLP エクスポート機能は無い** [確]。公式のメトリクス手段は **collectd 連携のみ**（`tedge-collectd-setup` パッケージ → `tedge-mapper collectd` → Cumulocity の計測） | [確] |

> ⚠️⚠️ **監視経路の設計上の帰結**: 構成図では画像解析装置のテレメトリは **Grafana Alloy → OTLP → Otel インフラ**です（IF-W05）。**thin-edge 自身は OTLP を出せません。** したがって:
>
> | thin-edge の情報 | 送出経路 | 担当 |
> |---|---|---|
> | サービス死活（up/down） | **Cumulocity のサービス表示**（SmartREST 102/104） | 本チーム |
> | ログ | **journald → Alloy が拾う**（要調整） | 本チーム × 画像解析側 |
> | ホストのリソース（CPU/メモリ/ディスク） | collectd → Cumulocity の計測、**または** Alloy → OTLP | **[判] Alloy に寄せる**（Otel 側に一元化し、Operational Store を圧迫しない） |
> | バッファ使用率・`te/errors` 件数・欠落 | **自作サービスが計測として publish、または Alloy 経由** | 本チーム |
>
> **[判] collectd は使わず、ホストメトリクスは Alloy（既に装置に載っている）に寄せることを推奨します。** 理由: ①死活計測だけで既に Edge の受信量の大半を占める見込み（[連携仕様] §3.5 の試算）で、そこにホストメトリクスを足すと Operational Store が膨らむ ②Otel 側に集約したほうが VM1/VM2 のコンポーネントと同じ土俵で見られる。**ただし外部Gateway 側に Alloy が無い場合は別途手当が要ります（TB-2）。**

### 5.10 セキュリティ設定

#### (1) ⚠️ c8y-proxy の露出

**c8y-mapper はローカル HTTP プロキシを立て、そこへのリクエストをデバイス JWT で認証して Cumulocity に転送します。既定は `127.0.0.1:8001`・平文 HTTP・認証なしです** [確]:

> *"There is no need to provide an `Authorization` header (or any other authentication method) when accessing the API."*

つまり **そのホスト上の任意のプロセスが、そのデバイスとして Cumulocity API を叩けます**。画像解析装置は Docker Compose で他チームのコンテナが同居するため、これは無視できません。

**[判] 対処**:

| # | 対処 | 実効性 |
|---|---|---|
| 1 | `c8y.proxy.bind.address` を**パイプラインコンテナから到達できる最小のインターフェースに限定**する（`0.0.0.0` にしない） | ⚠️ **限定的**。Docker のブリッジネットワークに bind した場合、**そのブリッジ上のどのコンテナからも到達可能**（フロントエンド/バックエンド/ログ処理コンテナも含む）。「露出範囲をホスト全体からブリッジ内に絞る」効果はあるが、**コンテナ間の隔離にはならない** |
| **2** | **Docker のネットワーク分離**: パイプラインコンテナと thin-edge のみを専用ネットワークに置き、フロントエンド/バックエンド/ログ処理コンテナを別ネットワークにする | ✅ **これが実効的な対策**。1 だけでは不十分なので、**2 を主たる対処とする** |
| 3 | 可能なら **HTTPS + クライアント証明書**（`c8y.proxy.cert_path` / `key_path` / `ca_path`）を有効化する [確] | 認証を追加できるが、証明書配布の運用コストが増える |
| 4 | それでも**同一ネットワーク内のプロセスは信頼境界の内側**になることを、TB-1 の責務整理に明記する | 残余リスクの明示。**[要 TE-19]** |

#### (2) ⚠️ `c8y_RemoteAccessConnect` の無効化

**これは Cumulocity から装置への SSH / VNC を張る機能です。** D9（拠点への着信接続を設けない）の趣旨に照らし、**プロファイル A では無効化を推奨します**。

> ⚠️ **無効化のための設定フラグは存在しません** [確]（`c8y.enable.*` は 7 キーあるが remote access 用は無い）。次の組み合わせで封じます:

| # | 手段 | 確度 |
|---|---|---|
| 1 | **`c8y-remote-access-plugin` パッケージを導入しない**（`tedge-full` を使わない） | [判] 最も確実 |
| 2 | `sudo rm /etc/tedge/operations/c8y/c8y_RemoteAccessConnect` → 114 の再送で Cumulocity 側の一覧からも消える | [確]（一般規則の適用） |
| 3 | `sudo systemctl disable --now c8y-remote-access-plugin.socket` | [推] |
| 4 | **Cumulocity テナント側で Cloud Remote Access のロールを誰にも割り当てない**（サーバ側での封じ込め） | [判] |

> ⚠️ **2 だけではパッケージの再インストール・更新で復活し得ます。** 1 と 4 を主、2・3 を従としてください。**W7 の検証で「Cumulocity の UI に Remote access のボタンが出ないこと」を確認**します。

#### (3) 実行ユーザーと権限

| 項目 | 内容 | 確度 |
|---|---|---|
| プラグイン実行 | **`sudo` で `tedge` ユーザーとして実行される** [確]。*"most of the system level commands will not work"* | [確] |
| 保護されたパスへの書き込み | `tedge-write` + sudoers 設定 | [確] |
| config plugin の sudoers | `tedge ALL = (ALL) NOPASSWD:SETENV: /usr/share/tedge/config-plugins/[a-zA-Z0-9]*` | [確] |
| オペレーション定義ファイル | パーミッション `644`、**ファイル名は英数字と `_` のみ**（ハイフン不可。使うと定義が無視される） | [確] |

> **[判] sudoers に付与する権限は、AI モデル配置ディレクトリとパイプラインコンテナの再起動に必要な最小限に絞ってください。** thin-edge が `tedge` ユーザーで動く以上、ここが装置内の権限境界です。

---

## 6. プロファイル A — 画像解析装置向け thin-edge.io（T2）

### 6.1 全体像

```
画像解析装置（Linux・拠点内）
├─ ホスト OS ──────────────────────────── TB-1（所有・構成管理の主体が未確定）
│  ├─ thin-edge.io（ホスト直インストール）        ◎ 本チーム
│  │   ├─ tedge-agent            オペレーション実行・エンティティストア
│  │   ├─ tedge-mapper c8y       Cumulocity 変換 + built-in bridge + c8y-proxy
│  │   ├─ mosquitto              ローカル MQTT のみ（2.0 以降）
│  │   ├─ tedge-watchdog         サービス監視
│  │   ├─ 【自作】ONVIF/ICMP 死活監視サービス     ◎ §6.2（IF-S08）
│  │   ├─ 【自作】sm-plugin `aimodel`             ◎ §6.3（IF-D01/S07）
│  │   └─ 【自作】スナップショット添付ワーカー    ◎ §6.6（IF-S04・提案）
│  └─ Grafana Alloy（画像解析側）── journald からログを収集 ── △ 要調整
└─ Docker Compose（画像解析側）                    ×
   ├─ フロントエンド / バックエンド
   └─ 画像解析パイプライン ──► ローカル MQTT（§5.6 の契約）
```

### 6.2 カメラ死活監視サービス（IF-S08）

> ⚠️ **thin-edge.io に ONVIF / ICMP 監視の機能はありません** [確]（公式ドキュメントに該当記述なし）。**本チームの自作部品**です（D7/D16 の「ONVIF 死活監視プラグイン」の実体）。

#### **[判] 実装形態**

| 選択肢 | 評価 |
|---|---|
| **systemd サービス + ローカル MQTT publish** | **✅ 採用**。ONVIF/ICMP は外部ネットワーク I/O を伴うため |
| thin-edge の `flows`（2.0 の新機能・JS サンドボックス） | ❌ 入力コネクタが MQTT・ファイル・プロセスに限られ、任意のソケットを開く用途は想定外。**ただし「計測を集約して送信量を減らす」後処理には使える** |

#### 動作仕様

| 項目 | 内容 |
|---|---|
| 対象 | エンティティ登録済み（§5.5）のカメラ child device |
| 手段 | ONVIF `GetSystemDateAndTime` / ICMP（[連携仕様] IF-S08） |
| 間隔 | 1〜5 分（構成コードで管理・D10） |
| 正常時 | `te/device/<cam-id>///m/x_CameraHealth` に `{"rtt": <ms>, "reachable": 1}` を publish（**retain 禁止**） |
| 異常判定 | **無応答 N 回連続（既定 3 回）** でアラーム。ヒステリシスでフラッピング対策 |
| アラーム | `te/device/<cam-id>///a/x_CameraDown` に `{"severity":"major","text":"…","time":…}` を **retain + QoS 2** で publish |
| 復旧 | 同トピックに**空の retained メッセージ**でクリア |
| 自身の死活 | `te/device/main/service/onvif-monitor/status/health` に retained publish + **LWT で `down` を登録** |

#### ⚠️ 設計上の要注意点

| # | 論点 | 対処 |
|---|---|---|
| **1** | **監視サービスが死ぬと、カメラのアラームが古いまま残り、新規の断も検知できない** | LWT + `tedge-watchdog` による自動再起動。**起動時にアラーム状態を全カメラぶん再評価（reconcile）する**こと |
| **2** | **死活計測が Edge の受信量の支配項になる** | [連携仕様] §3.5 の試算では 50 拠点 × 300 台 × 60 秒 ≒ **250 msg/s（死活のみ）**。Edge の目安は約 100 tps/CPU コア **[要]**（[設定書] §4.5 で「2026 のページにも記載」とされる値だが、本書ではベンダー資料からの伝聞値として扱う。SV-14 の実測待ち）。**間隔は「短いほど良い」ではない** |
| **3** | 2 の緩和策（**[要 TE-15]**） | ①間隔を 5 分側に寄せる ②**正常時は計測を送らず、rtt の変化・閾値超過時のみ送る** ③`flows` の `onInterval` で集約する ④transient トピック（`t/us`）の利用を検討（vendor-questions V14-4・**保証レベルの差が未確認**） |
| **4** | 数百台の並列ポーリング | タイムアウト・並列度・リトライを構成値にする。**1 巡が間隔を超えないこと**を監視対象にする |
| **5** | `c8y_RequiredAvailability` の二重発報 | §4.5 のとおり child は `responseInterval: 0` で無効化する |

### 6.3 AI モデル配布 sm-plugin（IF-D01 / IF-S07）

#### 仕組み [確]

```
保守端末 ──► Cumulocity ソフトウェアリポジトリ（softwareType = aimodel）
                 │ c8y_SoftwareUpdate オペレーション（拠点グループ単位の bulk operation）
                 ▼ 既存の拠点発 MQTT 接続で配信（D9 整合）
        tedge-mapper ── URL を c8y-proxy URL に書き換え ──► tedge-agent がダウンロード
                 ▼
        /etc/tedge/sm-plugins/aimodel  ← 本チームの自作プラグイン
           prepare → update-list（または install/remove）→ finalize
```

- **ダウンロードは tedge-agent が行い、プラグインには `--file <ダウンロード済みパス>` で渡される** [確]。**認証は c8y-proxy が JWT で代行するため、プラグイン側に資格情報は不要** [確]
- ダウンロード先は `tmp.path`（既定 `/tmp`）[確]。**モデルサイズに対して十分な容量があるか確認すること**

#### プラグイン API 仕様 [確]

| コマンド | 引数 | 本プラグインでの実装 |
|---|---|---|
| `list` | — | 導入済みモデルを **TSV（`名前<TAB>バージョン`）**で出力 |
| `prepare` | — | **現行モデルをバックアップに退避**（ロールバックの起点）・作業ディレクトリ作成 |
| `install` | `NAME [--module-version V] [--file F]` | **ハッシュ/署名を検証**（不一致は即失敗）→ 所定ディレクトリに配置 |
| `remove` | `NAME [--module-version V]` | 旧世代モデルの削除 |
| `update-list` | stdin から TSV | 実装しない場合は **exit 1** を返す（エージェントが install/remove に切り替える）[確] |
| `finalize` | — | **パイプラインコンテナを再起動 → 起動時ヘルスチェック → 失敗ならロールバック** |

**終了コード規約** [確]:

| コード | 意味 |
|---|---|
| `0` | 成功 |
| `1` | 引数解釈不能（コマンドは実行されていない） |
| `2` | 失敗。**リトライしても無意味** |
| `3` | 失敗。**後で成功し得る**（例: ネットワーク復旧後） |
| `4` | タイムアウト（エージェントが付与） |

> ⚠️ **配下のパッケージマネージャの終了コードをそのまま返してはいけません** [確]。必ず上記のいずれかにマップすること。

#### ⚠️⚠️ 要注意点 1: **software_update に自動ロールバックは存在しない**

公式の builtin ワークフローに rollback 状態はありません [確]。ロールバックは**プラグイン実装者の責務**です:

> `finalize` の説明（逐語）: *"Commit or rollback the sequence of actions."* / *"Restart any processes using the modules, e.g. restart the analytics engines if the modules have changed"*

**[判] 実装方針**: `prepare` で退避 → `finalize` で「再起動 → ヘルスチェック → NG ならバックアップを戻して再起動 → 非 0 で終了」。`finalize` が失敗すると**個々の install が成功していてもオペレーション全体が失敗として報告される** [確] ので、これが正しい設計です。

**より堅牢にするなら**、`/etc/tedge/operations/software_update.toml` をカスタマイズして `[rollback]` 状態を明示的に足せます [確]:

```toml
[executing]
action = "await-operation-completion"
on_success = "commit"
on_error   = "rollback"

[commit]
script = "/usr/bin/commit-model-update.sh ${.}"
on_success = "successful"

[rollback]
script = "/usr/bin/rollback-model-update.sh ${.}"
on_success = "failed"
```

#### ⚠️⚠️ 要注意点 2: **プラグインコマンドの 5 分タイムアウト**

> *"If the command fails to return within 5 minutes, the sm-agent reports a timeout error"* [確]

**モデル配置 + コンテナ再起動 + モデルロード + ヘルスチェックが 5 分以内に完了する必要があります。** 大きなモデルでは現実的でない可能性があります。

**[判] 対処**: 5 分に収まらない場合は **`finalize` の中で完結させず、カスタムワークフローの `background_script` + `await-operation-completion` + `timeout_second`** に切り出してください（`timeout_second` は任意に設定できる [確]）。**W1 の検証環境で実測してから決めること（TE-4）。**

#### その他

| # | 項目 | 内容 |
|---|---|---|
| 1 | 適用結果の記録 | 成功時に `x_ModelApplied` イベント（`modelVersion` 付き）を publish し、**モデル切替時刻を追跡可能にする**（§4.2・IF-S07） |
| 2 | 失敗時 | `x_ModelUpdateFailed` アラーム（CRITICAL・§4.6） |
| 3 | 冪等性 | 同一 `modelVersion` の再適用は「適用済みならスキップ」（[連携仕様] §3.4） |
| 4 | ⚠️ **[要 TE-16]** **モデルのファイルサイズ上限** | Cumulocity 側の `files/max.size` の実値（[設定書] SV-13）。thin-edge 側にはサイズ上限の記載なし [確] |
| 5 | ⚠️ **[要 TE-20]** **旧世代の削除運用** | ソフトウェアリポジトリは MongoDB 格納のため肥大化する。**保持世代数と削除運用を決める**（[連携仕様] A10） |
| 6 | 段階ロールアウト | 拠点グループ単位の bulk operation + **`creationRamp` の必須指定**（[設定書] G-4）。段階の進行条件を数値で置く（G-5） |

### 6.4 設定管理（`tedge-configuration-plugin.toml`）

**書式** [確]: `/etc/tedge/plugins/tedge-configuration-plugin.toml`

```toml
files = [
  { path = '/etc/tedge/plugins/onvif-monitor.toml', type = 'onvif-monitor', service = 'onvif-monitor' },
  { path = '/etc/tedge/plugins/tedge-log-plugin.toml', type = 'tedge-log-plugin' },
  # ↓ 画像解析側との合意が必要（§6.4 の注意）
  # { path = '/opt/pipeline/config/detector.yaml', type = 'pipeline-detector', service = 'pipeline' },
]
```

| フィールド | 意味 | 確度 |
|---|---|---|
| `path` | 対象ファイルの絶対パス | [確] |
| `type` | 設定タイプ名（未指定なら `path` が使われる）。**Cumulocity 側の設定リポジトリのキーになる** | [確] |
| `service` | **適用成功後に再起動するサービス名**。`system.toml` の `restart` コマンドが使われる | [確] |
| `user` / `group` / `mode` | ファイルが**新規作成される場合のみ**適用。既存ファイルは現在の所有権が保持される | [確] |
| `parent_user` / `parent_group` / `parent_mode` | 親ディレクトリが存在しない場合のみ適用 | [確] |

> **[確] `tedge-configuration-plugin.toml` 自身は暗黙で管理対象**（type = `tedge-configuration-plugin`）。**つまり「管理対象リストそのもの」を Cumulocity 経由で配れます。**

> **[確] 原子的置換**: ダウンロードは一時名で行われ、完全にダウンロードできてから rename される。半端なファイルでシステムが壊れることはない。

#### ⚠️ 要注意点

| # | 論点 | 内容 |
|---|---|---|
| **1** | **届くのは toml に登録した設定タイプだけ** | 画像解析パイプラインの設定を配るには、**そのファイルを登録することについて画像解析側の合意が必要**。設定ファイルの所有権が移ることになる |
| **2** | ⚠️ **ファイルベース設定更新にロールバックは無い** [確] | *"When using the out-of-the-box configuration update, powered by the `file` plugin, no rollback to the old configuration is attempted to maintain backward compatibility."* |
| **3** | 2 の対処 | ①**独自 config plugin（2.0 の新機能、`/usr/share/tedge/config-plugins`）を書く** — `list`/`get`/`prepare`/`set`/`verify`/`rollback` の 6 サブコマンドを実装すればロールバックが効く [確] ②または配信前の値検証（[設定書] G-3）で守る |
| **4** | 現在値の吸い上げ（`config_snapshot`） | **逸脱検出（K-04）もロールバック退避も、これが前提**。必ず有効にする（§5.8） |
| **5** | 単位違い・null の危険 | 秒/ミリ秒の取り違えや null でも thin-edge は「適用成功」を返す。**ホワイトリストと値域を配信側で検証すること**（[設定書] G-3） |

### 6.5 ログ管理（`tedge-log-plugin.toml`）

**書式** [確]: `/etc/tedge/plugins/tedge-log-plugin.toml`。フィールドは **`type` と `path` の 2 つのみ**（`path` は glob 可）。

```toml
files = [
  { type = "tedge-agent",       path = '/var/log/tedge/agent/*' },
  { type = "software-update",   path = '/var/log/tedge/agent/workflow-software_*' },
  { type = "onvif-monitor",     path = '/var/log/onvif-monitor/*.log' },
]

# journald プラグイン（systemd 環境では同梱）でサービスログを取得
[[plugins.journald.filters]]
include = "tedge-.*"
```

- 型名は `<log-type>::<plugin-name>`（例 `mosquitto::journald`）[確]
- 同梱プラグインは **`file`（全ディストリ）と `journald`（systemd 系）** [確]
- **`c8y_LogfileRequest` は「現地に行かずにログを取る唯一の手段」**です。閉域網・D9 の構成では特に重要

> ⚠️ **[要 TE-18]** **ログに個人情報・業務データを含めない**（[連携仕様] §4.6）。**ログマスキング規約を本チームが定義**し、`c8y_LogfileRequest` で吸い上げたログが Cumulocity（＝拠点横断で見える場所）に載ることを前提にレビューしてください。

### 6.6 スナップショット添付ワーカー（[判] 提案 — [連携仕様] §3.2 の改訂要求）

#### 現行の仕様と論点

[連携仕様] §3.2 は「**画像解析パイプラインが**ローカルスプールを持ち、c8y-proxy 経由で `POST /event/events` → `POST /event/events/{id}/binaries` を実行する」設計です。技術的には成立しますが、次の負担が**すべてパイプライン側（案件/画像解析チーム）に乗ります**:

- ローカルスプール（ディスク永続）とローテーション
- 網断からの再送ワーカー
- c8y-proxy の **散発的な 401 応答**へのリトライ [確]
- Cumulocity 側のイベント添付バイナリのサイズ上限（既定 50MiB・チャンク 5MiB、[定義書]・vendor-questions V16 参照）の確認。**⚠️ これは REST 経路の制約であり、§5.6 の 16KB 制約（`c8y.mapper.mqtt.max_payload_size`）はMQTT トピック投入経路にのみ適用され、この REST（`tedge upload c8y` / c8y-proxy 直叩き）経路には及ばない**。混同しないこと
- Cumulocity のイベント JSON 形式（トピック投入とは別形式）の理解

#### **[判] 提案: 本チームが「添付ワーカー」を基盤標準部品として提供する**

| 案 | 内容 | 評価 |
|---|---|---|
| **(A) 現行**: パイプラインが直接 REST | 部品が減る | **案件ごとに同じものを再実装することになる**。D2/D7 の「コストダウン」目的に反する |
| **(B) 提案**: 本チームが添付ワーカーを提供 | パイプラインは「イベント JSON + 画像パス」をローカル MQTT / 所定ディレクトリに置くだけ。ワーカーが `tedge upload c8y` を呼び、スプール・リトライ・401 対応・サイズ検証を担う | **✅ 推奨**。IF-S04 の契約（§5.6）が単純になり、2 案件目でそのまま使える |

> **[判] 推奨は (B)。** ただしこれは [連携仕様] §3.2 の改訂と、画像解析側との再合意を要します（**未決定事項として TE-14**）。**W0-11 で決着させてください。** (A) のままとする場合、上記 5 点を IF 仕様書に明記して相手チームに渡すことが本チームの責務になります。

> ⚠️ **どちらの案でも、HTTP 経路はストア&フォワードの対象外**（§5.7）です。**網断中のスナップショットを欠落させないためのスプールは必須**で、これを誰が持つかが (A)/(B) の違いです。

### 6.7 標準イメージとプロビジョニング（IF-P02）

**[判] 「この構成コードを流せば同じ装置になる」状態を 1 拠点目から作ります**（D10 / 転換 3「フリート管理こそが中核プロダクト」）。

#### 標準イメージに含めるもの（本チーム担当分）

| # | 項目 |
|---|---|
| 1 | thin-edge.io パッケージ（**版固定**・`c8y-remote-access-plugin` を除く） |
| 2 | **社内 CA ルート証明書**（`c8y.root_cert_path`）※これが無いと接続できない |
| 3 | 共通ベース設定（§5.4）+ プロファイル A オーバーレイ（§2.6） |
| 4 | 自作部品: ONVIF 死活監視サービス / sm-plugin `aimodel` / **添付ワーカー（W0-11 で案 B を採る場合のみ。§6.6）** |
| 5 | `tedge-configuration-plugin.toml` / `tedge-log-plugin.toml` / sudoers |
| 6 | systemd unit（`WatchdogSec` 設定済み）+ `tedge-watchdog` 有効化 |
| 7 | NTP 設定（IF-P03）※**時刻ずれはイベント×映像の突合と重複判定を同時に壊す** |

#### 拠点プロビジョニング手順

| # | 手順 | 完了条件 | 冪等 |
|---|---|---|---|
| 1 | `siteId` 採番・インベントリ登録 | 構成コードに拠点定義が存在 | ○ |
| 2 | デバイス証明書の取得（§5.3） | `tedge connect c8y --test` が成功 | ○ |
| 3 | main device が**正しい拠点グループに**現れる | Cumulocity 上で拠点 A 配下に表示 | ○（409 黙殺） |
| 4 | **カメラ child device の明示登録**（§5.5・`@id` 指定） | 全カメラが規約どおりの external ID で登録済み | ○ |
| 5 | **`x_Camera.vmsCameraId` の設定** | **未設定はツールがエラー**（IF-P02） | ○ |
| 6 | child の `c8y_RequiredAvailability = {"responseInterval": 0}` | メンテナンスモードになっている（§4.5） | ○ |
| 7 | 死活監視サービスの対象リスト反映・起動 | `x_CameraHealth` が全カメラぶん届く | ○ |
| 8 | supported operations の確認 | §5.8 の表どおり（**child には何も出ていないこと**） | — |

> ⚠️ **手作業登録を禁止してください**（D10）。手で 1 台登録すると、その拠点だけ external ID の規約が崩れ、**重複排除・映像突合・特徴量紐づけの三重の役割（C-2）が静かに壊れます**。壊れたことは長期間気づけません。

---

## 7. プロファイル B — 外部Gateway 向け thin-edge.io（T3）

> **§2.5 の案 β（拠点ごとにインスタンスを分ける）を採用した前提で記述します。** 案 α を採る場合の差分は §7.5 に示します。

### 7.1 全体像

```
本社 VM1 / 外部Gateway ホスト（TB-2: 所有主体が未確定）
├─ BOXアダプタ                                   △ TB-2（本チーム担当外）
│   ├─ BOX 独自形式 → 標準ペイロード変換
│   ├─ 重複排除（複合キー + 永続ストア・72h 窓）
│   └─ siteId で publish 先を振り分け ────┐
│                                          ▼
├─ thin-edge インスタンス [site-001]（コンテナ）  ◎ 本チーム
│   ├─ main device: site-001:GW-BOX（type: x_BoxGateway）
│   └─ child: site-001:BOX-xxxx …（type: x_SensingBox）
├─ thin-edge インスタンス [site-002] …
└─ 各インスタンス: 独立した device.id / 証明書 / ローカル MQTT ポート
```

### 7.2 プロファイル A との差分（設定レベル）

| 項目 | 値 | 理由 |
|---|---|---|
| `device.type` | `x_BoxGateway` | §4.3 |
| `c8y.availability.interval` | **`10m`** | 同一サーバールーム内。BOX イベントは疎らなのでハートビートが必須（§4.5） |
| `c8y.enable.software_update` | **`false`** | BOX は改造不可。モデル配布の対象外 |
| `c8y.enable.firmware_update` | **`false`** | 同上 |
| `c8y.enable.device_profile` | **`false`** | 同上 |
| `c8y.enable.config_update` / `config_snapshot` | `true`（**GW 自身の設定のみ**） | §7.3 |
| `c8y.enable.log_upload` | `true` | 障害調査 |
| `c8y.enable.device_restart` | `true` | 自社 VM |
| `c8y_RemoteAccessConnect` | 運用判断（自社 VM のため A ほど厳格でなくてよい） | §5.10 |
| ONVIF 死活監視サービス | **不要** | カメラは担当外 |
| sm-plugin `aimodel` | **不要** | — |
| 添付ワーカー | **不要**（BOX はスナップショットを出さない **[要 TE-9: N2 で確認]**） | — |
| ストア&フォワード | WAN を越えないため要件が緩い。**ただし Edge 停止時に溜まる分のサイジングは必要** | §5.7 |

### 7.3 supported operations の縮退

**プロファイル B の main device で申告するのは 4 種のみ**（§5.8 の表）:

```
114,c8y_DownloadConfigFile,c8y_LogfileRequest,c8y_Restart,c8y_UploadConfigFile
```

**child device（BOX）には一切申告しません。**

> ⚠️ **これは「設定を減らす」以上の意味があります。** BOX は改造不可であり、Cumulocity から何かを送っても**絶対に成功しません**。申告すると運用者が UI から実行してしまい、PENDING が溜まり続けます。**§9 の検証で「BOX の詳細画面にオペレーションの実行 UI が出ないこと」を確認してください。**

### 7.4 BOXアダプタとの分界（IF-H01 の契約）

**本チームが提供するのは §5.6 の契約そのものです。** ただしプロファイル B 固有の追加事項があります。

| # | 事項 | 内容 |
|---|---|---|
| **1** | **publish 先の決定規則**（案 β 固有） | アダプタは `siteId` から接続先ブローカー（ホスト:ポート）を解決する。**その対応表を誰がどう配るかを決めること**（構成コード / 環境変数 / DNS）→ TB-2。⚠️ **アダプタが誤った拠点の thin-edge に publish する（宛先解決ミス）と、他拠点のイベントとして登録されてしまう。** アダプタ自体は TB-2（本チーム担当外）だが、結果として本チーム所有の Cumulocity にデータが着地するため、**Cumulocity 側で軽減できる範囲（例: BOX の external ID に埋め込んだ `siteId` プレフィックスと、実際に登録された拠点グループの不一致を定期検査する）を検討する**か、「アダプタの誤配送への防御は範囲外」と明示的にスコープを切るかを、TB-2 の合意時に決めること |
| **2** | child ID と external ID | `@id = {siteId}:BOX-{BOXシリアル}`（C-2）。**アダプタは child ID を名乗るだけで、登録は本チームのプロビジョニングツールが行う**（§5.5 E-b） |
| **3** | 重複排除は**アダプタの責務** | thin-edge / Cumulocity に重複排除機能は無い。責務の一元化（[連携仕様] §4.5） |
| **4** | `time` は **BOX 側のイベント発生時刻** | 受信時刻ではない。再送時も同一値であることが重複判定の前提 |
| **5** | アラームの retain 要件 | `x_BoxSilent` / `x_BoxParseError` は **retain + QoS 2**、クリアは空 retained（§5.6）。**アダプタ実装者が最も間違えやすい点** |
| **6** | デッドレター | 変換不能ペイロードは破棄せず退避 + `x_BoxParseError`（[連携仕様] §3.8） |
| **7** | ⚠️ **[要 TE-9]** | **BOX の送信仕様（プロトコル・シーケンス ID の有無・再送間隔・バッファ容量）が未確認**（[連携仕様] N2）。重複判定キーと保持ウィンドウ（仮 72h）が確定できない |

### 7.5 案 α（単一インスタンス）を採る場合の追加設計

**§2.5 で案 α を選ぶ場合、次の 5 つを追加で設計・実装してください。**

| # | 追加作業 | 内容 |
|---|---|---|
| **α-1** | **BOX 全台の拠点グループ編成** | `POST /inventory/managedObjects/{siteGroupId}/childAssets` に BOX の MO を追加。台数分すべて。冪等化は 409 黙殺 |
| **α-2** | **GW 本体の配置と可視性の手当** | GW はどの拠点にも属せない。共通グループに置き、**GW 障害を拠点オペレーター・案件アプリに伝える別経路**（EPL によるロールアップアラーム等）を設計する |
| **α-3** | **アラームストームの抑止** | GW 停止時に全拠点の BOX が影響を受ける。§4.5 の `responseInterval: 0` は前提として、**BOX の `x_BoxSilent` を EPL で拠点単位に集約**することを検討 |
| **α-4** | **child ID 名前空間の統制** | 全拠点分の BOX 名が 1 インスタンス内で一意である必要。C-2 の `{siteId}:` プレフィックスで担保 |
| **α-5** | **単一インスタンスのスループット実測** | 全拠点分のイベントが 1 プロセスに集中する。**[要 TE-13]**（child device 数の上限はドキュメントに記載なし [確]） |

### 7.6 プロビジョニング手順（プロファイル B）

| # | 手順 | 完了条件 |
|---|---|---|
| 1 | thin-edge インスタンス（コンテナ）の生成 | 拠点変数から N 個生成される |
| 2 | デバイス証明書の取得 | 各インスタンスが接続できる |
| 3 | main device が拠点グループ配下に現れる | Cumulocity 上で確認 |
| 4 | **BOX child device の明示登録**（`@id` 指定） | 規約どおりの external ID |
| 5 | child の `responseInterval: 0` | メンテナンスモード |
| 6 | supported operations が §7.3 のとおり | **child には何も出ていない** |
| 7 | アダプタの publish 先解決の疎通 | テストイベントが正しい拠点の main device 配下に着く |

---

## 8. 作業一覧（タスクレベル）

> **凡例**: 対象 T1=Cumulocity Edge / T2=thin-edge A / T3=thin-edge B / X=横断。冪等 ○=再実行可 / ×=1 回きり

### W0. 前提・規約の確定

| ID | 作業 | 対象 | 完了条件 | 依存 |
|---|---|---|---|---|
| W0-1 | **担当境界 TB-1〜TB-5 の決着** | X | 各対象物の所有・構成管理・運用の主体が**関係する他部門（画像解析側・基盤運用側等）との間で合意され、文書化されている**（本チーム単独の記録では不可）。決着済みの担当・担当窓口・決着日を記録する | — |
| W0-2 | **C-1 イベント/アラーム型・ペイロード規約**（16KB 制約・`boundingBoxes` 上限を含む） | X | 規約文書 + バリデータが**存在し、画像解析側・案件側が合意済み** | — |
| W0-3 | **C-2 external ID 採番規約**（仮名化要否を含む） | X | 規約文書が**存在し、法務/顧客合意が必要な部分がその文書内に明示され、必要な承認プロセスが開始されている** | — |
| W0-4 | **C-3 拠点コード（英数字）命名規約** | X | 拠点グループ名と別に「トピック名用コード」が定義され、**関係チームに合意されている** | — |
| W0-5 | **C-4 ローカル MQTT 受け口の IF 契約**（§5.6） | T2/T3 | IF 仕様書を相手チームに提供済み | W0-2 |
| W0-6 | **C-5 エンティティ登録規約**（§5.5） | T2/T3 | `@id` 統制・自動登録無効化が規約化されている | W0-3 |
| W0-7 | ドメイン 2 つ・TLS SAN・ライセンス・registry credentials の受領 | T1 | 現物が揃っている（[設定書] P-2/P-3） | — |
| W0-8 | ネットワーク前提の確認（[設定書] P-0-1〜P-0-6） | T1 | CIDR 衝突なし・公開ポート・NTP・到達性・HW 要件確定 | — |
| **W0-8b** | **Edge のディスクサイジング**（`storageClassName` 確定の前提。TE-22） | T1 | リテンション日数（X-01）× 受信レート試算から MongoDB・監査ログの必要容量を算出し、`storageClassName`/`mongodb.resources.requests.storage` に反映 | W0-8 |
| W0-9 | **thin-edge のエアギャップ配布経路の確立**（§5.1） | T2/T3 | 対象バージョンの資産を社内で取得・配布できる | — |
| W0-10 | 配置方式の決定（**案 α / β / γ**・§2.5） | T3 | 決定と根拠が記録されている | W0-1 |
| W0-11 | 添付ワーカーの担当決定（§6.6・案 A/B） | T2 | [連携仕様] §3.2 の扱いが確定 | W0-1 |
| **W0-12** | **ベンダー照会の発行**（TE-1: 商用サポート有無 / TE-2: バージョン互換マトリクス、他ベンダー照会項目一覧は §11） | X | 照会が**発行され、回答期限が設定されている**（回答自体は W1 の間に受領できなくてもよいが、発行が W0 で完了していること） | — |

### W1. 検証環境の構築と重大未確定の解消

| ID | 作業 | 対象 | 完了条件 |
|---|---|---|---|
| W1-1 | 検証用 Edge の構築 | T1 | 両テナントでログイン可能 |
| W1-2 | ⭐ **TE-12（SV-35）**: `alarmsWithChildren` が拠点グループ配下を辿るか | T1 | 拠点グループを `source` にして配下デバイスのアラームが届くことを確認。**NG なら拠点分離方式を再設計** |
| W1-3 | ⭐ **TE-8（SV-08）**: Edge で certificate-authority（EST）が使えるか | T1 | `c8y features enable --key certificate-authority` → CA 作成 → `tedge cert download c8y` が通る |
| W1-4 | ⭐ **TE-3**: mosquitto バッファの実効既定値と**溢れる時間の実測** | T2 | 断を発生させ、復旧後の到達件数から欠落開始点を測定。設定値を確定 |
| W1-5 | **TE-7（SV-25）**: テナント CA 自動更新（10/2）のシミュレート | X | 時計を進めて再エンロールが成功することを確認 |
| W1-6 | **TE-5**: child device への twin（`responseInterval: 0`）反映 | T2 | カメラが `c8y_UnavailabilityAlarm` を上げないことを確認 |
| W1-7 | **TE-4**: sm-plugin の 5 分タイムアウト内でモデル適用が完了するか | T2 | 実サイズのモデルで実測。超える場合はワークフロー化 |
| W1-8 | **TE-6（V18）**: external ID / topic id の文字種・長さ制約 | X | `site-001:CAM-SN12345` 形式が通ることを実機で確認 |
| W1-9 | **TE-13（V15）**: child device 数と mapper スループット | T2/T3 | 想定台数で実測 |
| W1-10 | **SV-05**: Edge 上での Notification 2.0 実動作 | T1 | WebSocket 到達性を含めて確認 |
| W1-11 | Messaging Service の稼働確認（N-03） | T1 | [設定書] P1 1-5 |

> ⚠️ **W1-2 / W1-3 / W1-4 の 3 つは「NG だと設計が変わる」項目です。** 他の作業に先んじて着手してください。

### W2. 本番 Edge の設置（[設定書] P0）— **冪等化不可**

| ID | 作業 | 冪等 | 完了条件 |
|---|---|---|---|
| W2-1〜W2-7 | [設定書] §5 P0（0-1〜0-7）に従う | × | 両 URL でログイン可能・CA 配布済み・`no_proxy` 設定済み |
| W2-8 | スナップショット取得 + **リストア試験** | ○ | **試験成功まで W3 に進まない**（[設定書] P1 1-6） |

### W3. Cumulocity テナント設定（[設定書] P1〜P4）

| ID | 作業 | 対象 | 参照 |
|---|---|---|---|
| W3-1 | Edge CR エクスポート・admin パスワード・エスクロー | T1 | [設定書] P1 |
| W3-2 | CLI セッション準備（**`C8Y_MODE=ci`。`prod` は使わない**） | T1 | [設定書] P1.5 |
| W3-3 | テナントオプション・CORS・フィーチャートグル | T1 | [設定書] P2 |
| W3-4 | ロール・ユーザー（**R-08 証明書アップロード用ローカルユーザーを忘れない**） | T1 | [設定書] P3 |
| W3-5 | **拠点 device group 階層 + external ID**（G-01/G-02） | T1 | §4.3 |
| W3-6 | **テナント CA 作成**（D-01） | T1 | §4.4・W1-3。**W1-3 が NG の場合**: この工程は「自前 CA を trusted certificate として登録する従来方式」に切り替え、証明書更新の自動化方式を別途設計してから着手する（§5.3） |
| W3-7 | Inventory ロール定義と割当（G-03） | T1 | §4.3 G-c |
| W3-8 | `alarm.type.mapping`（T-03）投入 | T1 | §4.6 |

### W4. thin-edge プロファイルの実装

| ID | 作業 | 対象 | 完了条件 |
|---|---|---|---|
| W4-1 | 共通ベース設定の構成コード化（§5.4） | T2/T3 | `tedge config` の手作業がゼロ |
| W4-2 | **mosquitto バッファ設定の構成コード化**（§5.7） | T2/T3 | W1-4 の実測値が反映されている |
| W4-3 | エンティティ登録ツール（`@id` 指定・§5.5） | T2/T3 | `vmsCameraId` 未設定をエラーにする |
| W4-4 | supported operations の取捨（§5.8） | T2/T3 | §5.8 の表どおり。**child に何も出ていない** |
| W4-5 | **ONVIF/ICMP 死活監視サービス**（§6.2） | T2 | LWT・ヒステリシス・起動時 reconcile を実装 |
| W4-6 | **sm-plugin `aimodel`**（§6.3） | T2 | ハッシュ検証・ロールバック・`x_ModelApplied` を実装 |
| W4-7 | 添付ワーカー（W0-11 が案 B の場合） | T2 | スプール・401 リトライ・16KB 検証 |
| W4-8 | `tedge-configuration-plugin.toml` / `tedge-log-plugin.toml` | T2/T3 | §6.4 / §6.5 |
| W4-9 | `c8y_RemoteAccessConnect` の無効化（§5.10） | T2 | Cumulocity UI に Remote access のボタンが出ない |
| W4-10 | 標準イメージのビルドと版管理 | T2 | 同一イメージから同一の装置ができる |
| W4-11 | プロファイル B の実装（§7） | T3 | supported operations が 4 種のみ |
| W4-12 | **supported operations の Cumulocity 側受け入れ確認**（T1） | T1 | W4-4/W4-11 で申告した一覧が Cumulocity の SmartREST 114 / UI 上に正しく反映され、**child device には何も表示されていない**ことを Edge 側で確認 |

### W5. パイロット拠点の通し

| ID | 作業 | 対象 | 完了条件 |
|---|---|---|---|
| W5-1 | 1 拠点の実機プロビジョニング（§6.7） | T2/T3 | 全カメラが規約どおりの external ID で登録され、拠点グループ配下に見える |
| W5-2 | BOX 1 系統の疎通（§7.6） | T3 | BOX イベントが正しい拠点の配下に着く |
| W5-3 | **E2E 通し**: 検知 → イベント → アラーム → 通知 → 映像確認 | X | 案件アプリまで届く |
| W5-4 | **網断試験** | T2/T3 | 断 → 復旧で欠落なし（設定した容量の範囲内）。欠落したら検知できる |
| W5-5 | モデル配布の通し（IF-D01 → IF-S07） | T1/T2 | 適用成功・失敗時ロールバックの両方 |
| W5-6 | `responseInterval` の実測に基づく確定（§4.5） | T1 | **観測期間を定めて実測し**（例: 2 週間）、その期間中の誤報件数が**合意した許容件数以下**であること。値と観測結果を記録する |

### W6. SSO・データ・通知・アプリ（[設定書] P5/P6）

> ⚠️ **W6-5・W6-6 は TB-3・TB-4 の決着が前提です。** W0-1 で決着していない場合、この 2 タスクは着手できません（担当が誰か分からない）。

| ID | 作業 | 対象 | 参照 |
|---|---|---|---|
| W6-1 | Keycloak SSO（**切替前ゲート必須**） | T1 | [設定書] P5 |
| W6-2 | リテンション（**新規作成 → 旧削除の順**） | T1 | [設定書] 6-1 |
| W6-3 | スマートルール・EPL（**リプレイ抑止ガードを雛形に必須で組み込む**） | T1 | [設定書] 6-4/6-6・[連携仕様] §3.5 |
| W6-4 | ソフトウェアリポジトリ（M-01） | T1 | [設定書] 6-9 |
| W6-5 | **Notification 2.0 サブスクリプション + トークン発行プロキシ**（**前提: TB-3 決着**） | T1 | [設定書] 6-12/6-13 |
| W6-6 | オフロードバッチ（**前提: TB-4 決着**）— **稼働開始から 90 日以内が期限** | T1 | [設定書] 6-10 |

### W7. 全拠点展開と受け入れ検証

| ID | 作業 | 対象 | 完了条件 |
|---|---|---|---|
| W7-1 | 全拠点プロビジョニング（**段階ロールアウト・`creationRamp` 必須**） | T1/T2/T3 | 全拠点の main device / child device が規約どおりに登録され、拠点グループ配下に見える |
| W7-2 | §9 の検証項目をすべて実施 | X | §9 の全 VT-nn が実施され、結果（合格/不合格）が記録されている |
| W7-3 | メタ監視・Edge 監視の接続（**前提: TB-5 決着**） | X | Edge の Prometheus エンドポイントが保守拠点側の監視系に接続され、**基盤自身の異常が実際に人に通報される**ことを試験で確認済み |
| W7-4 | §10 の Ready for Service チェックリスト | X | §10.2 の全項目が ✅ |

---

## 9. 検証（受け入れ試験）

> [設定書] §5 P7 の項目に加え、**本チームの担当範囲に固有の項目**を挙げます。ID は `VT-nn`（vendor-questions の `V-nn` とは別体系）。⭐ は**合格しないと提供開始できない項目**です。

### 9.1 デバイスモデルと拠点分離

| # | 試験 | 合格条件 |
|---|---|---|
| VT-1 | 実デバイス 1 台の疎通 | **正しい拠点グループの配下**に現れる |
| VT-2 | external ID の規約準拠 | 全デバイス・全 child が `{siteId}:{種別}-{シリアル}`。**規約外の MO が 1 つも無い**（スクリプトで機械検査し CI に載せる） |
| VT-3 | `vmsCameraId` の設定 | **全カメラに設定されている**（未設定が 0 件） |
| VT-4 | ⭐ 拠点グループ購読（TE-12） | 拠点 A のアラームが拠点 A の購読で届き、**拠点 B では届かない** |
| VT-5 | Inventory ロールの否定テスト | 拠点 A のユーザー資格情報で拠点 B のデータを **API 直叩き**して 403/404 |
| VT-6 | `ROLE_NOTIFICATION_2_ADMIN` の機械検査 | R-09 以外のどのユーザー・ロールにも付いていない（CI に載せる） |

### 9.2 死活監視

| # | 試験 | 合格条件 |
|---|---|---|
| VT-7 | ゲートウェイ断 | 通信停止 → `responseInterval` 後に `c8y_UnavailabilityAlarm` → 復旧で自動クリア |
| VT-8 | カメラ断 | 無応答 N 回で `x_CameraDown` → 復旧でクリア。**ヒステリシスが効きフラッピングしない** |
| VT-9 | ⭐ **child のアラーム抑止** | 接続から 1 時間以上経過しても、**カメラ・BOX に `c8y_UnavailabilityAlarm` が 1 件も出ない**（§4.5 の落とし穴） |
| VT-10 | 死活監視サービスの死 | サービスを kill → **Cumulocity 上でサービスが `down` になる**（LWT）→ watchdog が再起動 → **起動時にアラーム状態が再評価される** |

### 9.3 ストア&フォワード（最重要）

| # | 試験 | 合格条件 |
|---|---|---|
| VT-11 | ⭐ **網断 → 復旧の欠落測定** | 想定最長断時間の断を発生させ、**復旧後に全イベントが到達する**（`eventUuid` で突合）。到達件数を数値で記録する |
| VT-12 | ⭐ **バッファ溢れの検知** | 意図的に上限を超える断を起こし、**欠落が発生したことを検知できる** |
| VT-13 | **電源断試験** | 装置の電源を落として再投入し、**バッファが残っている**（`persistence true` が効いている） |
| VT-14 | スナップショットの網断耐性 | 断中の検知イベントの画像が、復旧後に**遅れて到達する**（欠落しない） |
| VT-15 | 復旧時のリプレイ | 一括再送で**通知が復旧時刻にまとめて発火しない**（ルール雛形のリプレイ抑止ガード） |

### 9.4 オペレーション

| # | 試験 | 合格条件 |
|---|---|---|
| VT-16 | ⭐ **supported operations の否定テスト** | **カメラ・BOX の詳細画面にオペレーションの実行 UI が出ない** |
| VT-17 | ⭐ **Remote access の無効化** | Cumulocity UI に Remote access のボタンが出ない。`114` の一覧に `c8y_RemoteAccessConnect` が無い |
| VT-18 | モデル配布の成功 | 適用 → `x_ModelApplied` イベント → 以後の検知イベントの `modelVersion` が新しい |
| VT-19 | **モデル配布の失敗とロールバック** | 壊れたモデルを配布 → **旧モデルに戻って稼働継続** → Operation が FAILED → `x_ModelUpdateFailed` |
| VT-20 | ハッシュ/署名検証 | 改ざんしたモデルが**適用されずに即 FAILED** |
| VT-21 | 設定配布 | `c8y_DownloadConfigFile` で死活間隔が変わる。`c8y_UploadConfigFile` で現在値が吸い上がる |
| VT-22 | ログ取得 | `c8y_LogfileRequest` で thin-edge のログが取得でき、**個人情報がマスクされている** |
| VT-23 | キルスイッチ（G-1） | 一括オペレーション発行 → 3 手順で実際に止まる |

### 9.5 証明書

| # | 試験 | 合格条件 |
|---|---|---|
| VT-24 | 自動更新 | `tedge-cert-renewer@c8y.timer` が動作し、`.new` → 検証 → 差し替えが完了する |
| VT-25 | ⭐ **テナント CA 自動更新（10/2）** | 時計を進めて再エンロールが成功する |
| VT-26 | 有効期限の監視 | 証明書有効期限がメタ監視の対象に入っている |
| VT-27 | 長期オフラインからの復帰 | `minimum_duration` を超えない断からの復帰で、更新が成功する |

### 9.6 規模

| # | 試験 | 合格条件 |
|---|---|---|
| VT-28 | 合算規模の負荷試験 | [設定書] 7-4。Wide シナリオ 1,200 クライアントに対する実測。**tps・E2E 遅延・CPU・MongoDB IO で数値化** |
| VT-29 | 死活計測の送信量 | [連携仕様] §3.5 の試算（50 拠点 × 300 台 ≒ 250 msg/s）に対する実測と余裕率 |

---

## 10. 「IoT基盤として提供できる」状態の定義（Ready for Service）

### 10.1 提供物カタログ

**本チームが「IoT基盤」として他チームに提供するものの全リストです。** これが揃って初めて「提供開始」と言えます。

> ⚠️ **ID は `DL-nn`（deliverable）とします。** [設定書] が `P0`〜`P7`（フェーズ）・`P-0`〜`P-4`（前提）・`P-01`〜`P-04`（項目ID）という3系統の `P` 記法を既に使っているため、本節を「P1」等にすると衝突します。

| # | 提供物 | 形態 | 受け取る人 |
|---|---|---|---|
| **DL-1** | **Cumulocity Edge**（稼働・設定済み） | 実環境 + REST/MQTT エンドポイント | 全チーム |
| **DL-2** | **デバイス接続の配管** — thin-edge プロファイル A / B | 標準イメージ + 構成コード | 画像解析側・BOX 側 |
| **DL-3** | **ローカル MQTT 受け口の IF 仕様**（C-4・§5.6） | 仕様書 | 画像解析側・BOXアダプタ側 |
| **DL-4** | **イベント/アラーム型・ペイロード規約**（C-1） | 規約文書 + バリデータ | 画像解析側・案件側 |
| **DL-5** | **external ID 採番規約**（C-2） | 規約文書 | 全チーム |
| **DL-6** | **デバイス登録（プロビジョニング）ツール** | 構成コード + CLI | 本チーム（拠点追加時） |
| **DL-7** | **拠点グループ階層と Inventory ロール** | Cumulocity 設定 | 案件アプリ・業務端末 |
| **DL-8** | **REST API アクセス**（サービスユーザー・CORS・ロール） | 資格情報 + 手順 | VM2 の各アセット |
| **DL-9** | **Notification 2.0 サブスクリプションとトークン発行**（TB-3） | エンドポイント + 手順 | 案件アプリ |
| **DL-10** | **AI モデル配布経路**（ソフトウェアリポジトリ + 更新オペレーション） | 手順 + ロール | 保守端末 |
| **DL-11** | **運用 runbook 一式**（§10.3） | 文書 | 基盤運用 |
| **DL-12** | **監視・アラート**（Edge 自身の異常が人に届く経路） | 設定 + 通報先 | 基盤運用 |

### 10.2 Ready for Service チェックリスト

**全項目が ✅ になるまで「提供開始」と言わないでください。**

#### A. 機能

- [ ] 実拠点 1 つで E2E（検知 → イベント → アラーム → 通知 → 映像確認）が成立している（W5-3）
- [ ] BOX 1 系統が E2E で成立している
- [ ] 拠点分離が**否定テストで**確認されている（VT-4 / VT-5 / VT-6）
- [ ] 死活監視が実効的に動く（VT-7 / VT-8）。**child に不要なアラームが出ていない**（VT-9）
- [ ] AI モデル配布が**失敗時のロールバックを含めて**動く（VT-18 / VT-19 / VT-20）
- [ ] ⭐ **child device に supported operations の実行 UI が一切出ていない**（VT-16）
- [ ] ⭐ **`c8y_RemoteAccessConnect` が無効化されている**（VT-17）

#### B. 信頼性

- [ ] **網断からの欠落ゼロが実測されている**（VT-11）。**欠落する条件と、そのときの検知手段が文書化されている**（VT-12）
- [ ] **電源断でバッファが失われない**（VT-13）
- [ ] Edge のリストア試験が成功している（W2-8）
- [ ] **RTO/RPO が数値で決まっている**（TE-10）。「Edge 停止許容時間」が BOX / thin-edge のバッファ保持時間の実測から導かれている
- [ ] 証明書の自動更新が動く（VT-24）。**10/2 のシミュレートが済んでいる**（VT-25）

#### C. 規約と IF

- [ ] C-1〜C-5 が**文書として存在し、相手チームが合意している**
- [ ] IF 仕様（DL-3）が相手チームに渡っており、**相手が実装できている**
- [ ] external ID の規約準拠が**機械検査で保証されている**（VT-2・CI に載っている）

#### D. 運用

- [ ] **手作業でのデバイス登録・設定変更が禁止され、構成コードだけで拠点が立つ**（D10）
- [ ] §10.3 の runbook がすべて存在し、**うち「止める手順」は実際に実行して確認済み**（VT-23）
- [ ] **基盤自身の異常が人に届く**（TB-5・SV-34）。⚠️ **メール不採用（変-2）のため、これが唯一の経路** です
- [ ] 証明書有効期限・バッファ使用率・`te/errors` 件数が監視対象になっている
- [ ] エアギャップ配布経路が確立し、**脆弱性対応のリードタイムが運用手順に入っている**（W0-9）
- [ ] ⭐ **TB-1〜TB-5 の担当境界がすべて決着し、決定内容が記録されている**（§1.4）— 決着していないものは「未決着のまま提供開始する」という判断として明示的に受容されていること

#### E. 引き渡し

- [ ] 案件チームが**本チームに聞かずに**デバイスを繋げる状態になっている（DL-3/DL-4/DL-5 が自己完結している）
- [ ] 2 拠点目が **`sites/` の追加だけ**で立ち上がることを確認済み（§2.6）
- [ ] 未確定事項（§11 TE-nn、§1.4 TB-nn）のうち **未解決のまま残っているものが、リスクとして明示的に受容されている**

### 10.3 運用 runbook 一覧（本チームが用意する）

> ⚠️ **ID は `RB-nn`（runbook）とします。** §3.2/§4/[設定書] で多用される「ロール」の `R-01`〜`R-09` と字面が衝突するため区別します（特に RB-8「SSO からローカル認証への切り戻し」と R-08「証明書アップロード用ローカルユーザー」は無関係な別概念です）。

| # | runbook | 契機 | 検証 |
|---|---|---|---|
| RB-1 | 拠点の追加（プロビジョニング） | 拠点追加 | W5 |
| RB-2 | カメラ / BOX の増設・撤去 | 随時 | W5 |
| RB-3 | **デバイス証明書の失効**（拠点撤収・機器盗難） | 随時 | — |
| RB-4 | **テナント CA 自動更新日（毎年 10/2）の対応**（**大量デバイスが同時に再エンロールを試みる輻輳への対処を含む**。TE-7 参照） | 年 1 回 | VT-25 |
| RB-5 | **キルスイッチ**（一括オペレーションの停止） | 緊急 | VT-23 |
| RB-6 | thin-edge のバージョンアップ（フリート） | 随時 | TE-2 |
| RB-7 | Edge のバックアップ / リストア | 定期 / 緊急 | W2-8 |
| RB-8 | **SSO からローカル認証への切り戻し** | 緊急 | [設定書] SV-04 |
| RB-9 | サービスアカウントの復旧（削除 → 再作成） | 緊急 | [設定書] 3-9 |
| RB-10 | Notification 2.0 コンシューマの棚卸し（unsubscribe） | 定期 | [設定書] 7-6d |
| RB-11 | **バッファ溢れ発生時の対応**（欠落範囲の特定と手動突合） | 障害 | VT-12 |
| RB-12 | 装置の交換（同一 `siteId` での再登録） | 障害 | — |
| RB-13 | ⚠️ **R-08（証明書アップロード用ローカル管理者アカウント）のライフサイクル管理**（パスワードローテーション・保管方法・ロックアウト時のブレークグラス手順） | 定期 / 緊急 | — |

---

## 11. 未確定事項（TE-nn）

> **ID 体系**: 本書は `TE-nn`（thin-edge / 本チーム起点）を使います。既存の `SV-nn`（[設定書]）・`N-nn` / `A-nn`（[連携仕様]・[カメラ構成]）・`V-nn`（vendor-questions）との対応を併記します。
> **担当境界は `TB-nn`**（§1.4）として別建てです。

### 11.1 設計が変わる可能性のあるもの（最優先）

| # | 項目 | 確認方法 | 優先度 | 既存 ID |
|---|---|---|---|---|
| **TE-12** | **`alarmsWithChildren` が拠点グループ（`childAssets`）を辿るか** — **NG なら D17 の拠点分離方式そのものが破綻** | W1-2 検証環境 | **最高** | SV-35 |
| **TE-8** | **Cumulocity certificate-authority（EST）が Edge で使えるか** — NG なら証明書運用の自動化方式を作り直し | W1-3 検証環境 + ベンダー照会 | **最高** | SV-08 / V8 |
| **TE-3** | **mosquitto バッファの実効既定値と「溢れる時間」** — §5.7。障害モードが「一部欠落」か「装置全断」かを分ける | W1-4 実測 | **最高** | A7 / V15-3 |
| **TE-1** | **thin-edge.io の商用サポートの有無** — 無ければ D16 の採用根拠（運用負荷の軽減）が崩れる | ベンダー照会 | **最高** | V20 |
| **TE-10** | **想定最長 WAN 断時間 / RTO・RPO** — バッファサイジングの唯一の入力 | 設計判断 + 実測 | **最高** | SV-29 / N9 |

### 11.2 実装方式が変わるもの

| # | 項目 | 確認方法 | 優先度 | 既存 ID |
|---|---|---|---|---|
| **TE-4** | sm-plugin の **5 分タイムアウト**内にモデル適用が収まるか。超えるならワークフロー化 | W1-7 実測 | 高 | A10 |
| **TE-5** | child device への twin（`c8y_RequiredAvailability = 0`）反映 | W1-6 検証環境 | 高 | — |
| **TE-6** | **external ID / `device.id` / topic id の文字種・長さ制約**（`site-001:CAM-SN12345` が通るか） | W1-8 実機 | 高 | V18 |
| **TE-13** | 1 インスタンスあたりの **child device 数上限と mapper スループット**（ドキュメントに記載なし） | W1-9 実測 + ベンダー照会 | 高 | A7 / V15 |
| **TE-11** | **コンテナ版 thin-edge で ①証明書自動更新 ②watchdog ③mosquitto バッファ設定 をどう成立させるか**（案 β 採用時） | W1 検証環境 | 高 | — |
| **TE-9** | **BOX の送信仕様**（プロトコル・シーケンス ID の有無・再送間隔・バッファ容量） | 機器調査 | 高 | N2 |
| **TE-14** | 添付ワーカーの担当（§6.6 案 A / B） | 設計判断 | 中 | — |
| **TE-15** | 死活計測の送信レート削減方針（間引き / 集約 / transient トピック `t/us` の保証レベル差） | 設計判断 + ベンダー照会 | 中 | V14 |
| **TE-16** | Cumulocity の `files/max.size`（AI モデルのサイズ上限） | `GET /tenant/system/options` | 中 | SV-13 |
| **TE-21** | **案 β 採用時、拠点数ぶんの thin-edge インスタンス（コンテナ）が VM1 のリソースで収まるか**（§2.5 の「軽量なので収まる見込み」は未実測） | W1 検証環境で想定拠点数分を起動し CPU/メモリ/ディスクを実測 | 高 | — |
| **TE-22** | **Cumulocity Edge の Operational Store（MongoDB）+ 監査ログのディスクサイジング手法**。`storageClassName` は install 後変更不可のため W2 前に確定が必要 | リテンション日数（X-01）× 受信レート試算（§6.2・SV-14）から逆算する手順を確立し、W2 前に実施 | **最高** | — |

### 11.3 運用に効くもの

| # | 項目 | 確認方法 | 優先度 | 既存 ID |
|---|---|---|---|---|
| **TE-7** | **テナント CA 自動更新（毎年 10/2）時の再エンロール動作** | W1-5 シミュレート | **最高** | SV-25 |
| **TE-2** | Edge と thin-edge のバージョン互換マトリクス・LTS の考え方 | ベンダー照会 | 高 | V20 |
| **TE-17** | エアギャップ配布経路（社内リポジトリ）の方式と脆弱性対応リードタイム | 設計判断 | 高 | — |
| **TE-18** | thin-edge のログのマスキング規約（`c8y_LogfileRequest` で拠点横断に見える） | 設計判断 | 中 | — |
| **TE-19** | c8y-proxy の信頼境界（同一ホスト上の他コンテナから叩ける・§5.10） | 設計判断 | 中 | — |
| **TE-20** | モデル世代の保持数と旧版削除運用（ソフトウェアリポジトリ = MongoDB の肥大化） | 設計判断 | 中 | A10 |

### 11.4 [設定書] から引き継ぐもの（本チームの担当範囲に関係するもののみ）

| # | 項目 | 優先度 |
|---|---|---|
| SV-21 | D6/D7/D15 の責務境界（thin-edge を載せる筐体の所有・構成管理主体） | **最高**（= TB-1） |
| SV-26 | `responseInterval` の装置種別ごとの値 | 高（§4.5 で提案済み・実測待ち） |
| SV-22 | 証明書・社内 CA ローテーション手順（新旧並行の移行期間） | 高 |
| SV-14 | 全拠点合算のスケール | **最高** |
| SV-05 | Edge 上での Notification 2.0 実動作 | **最高** |
| SV-04 | SSO からローカル認証へ戻す手順 | **最高** |
| SV-34 | 基盤自身の異常のアラート通知先 | 高（= TB-5） |
| SV-13 | `files/max.size` の実値 | 中（= TE-16） |
| SV-27 | BOXアダプタのマッピング定義の配布方式 | 中（= TB-2） |
| SV-28 | カメラ（child device）への構成オペレーション | 中 |

---

## 12. 出典

### 社内文書

- [Cumulocity構成準拠セットアップ設定書.md](Cumulocity構成準拠セットアップ設定書.md) — **Cumulocity 側の実施手順の正**
- [Cumulocity設定定義書.md](Cumulocity設定定義書.md) / [Cumulocity設定エクスポート投入ガイド.md](Cumulocity設定エクスポート投入ガイド.md)
- [../../IoTPlatform_cc/data-integration-spec_new.md](../../IoTPlatform_cc/data-integration-spec_new.md) — IF-nn の正
- [../../IoTPlatform_cc/architecture-camera-monitoring.md](../../IoTPlatform_cc/architecture-camera-monitoring.md) — デバイスモデル・ペイロード規約の原典
- [../../IoTPlatform_cc/design-decisions.md](../../IoTPlatform_cc/design-decisions.md) — D1〜D17
- [../../IoTPlatform_cc/vendor-questions-cumulocity.md](../../IoTPlatform_cc/vendor-questions-cumulocity.md) — V1〜V20
- [cumulocity-iot-architecture.drawio](cumulocity-iot-architecture.drawio) — **「全体構成(配置構成・レビュー反映)」タブのみ**

### thin-edge.io（バージョン 2.0.1）

| 節 | URL |
|---|---|
| エンティティ管理（MQTT API / REST API / 自動登録） | `https://thin-edge.github.io/thin-edge.io/operate/entity-management/` |
| MQTT API リファレンス | `https://thin-edge.github.io/thin-edge.io/references/mqtt-api/` |
| Cumulocity マッパー | `https://thin-edge.github.io/thin-edge.io/references/mappers/c8y-mapper/` |
| **Builtin mapping rules（`max_payload_size` の逐語根拠）** | `https://thin-edge.github.io/thin-edge.io/references/mappers/builtin-flows/`（⚠️ `c8y-mapper` ページではなくこちら） |
| Supported Operations | `https://thin-edge.github.io/thin-edge.io/operate/c8y/supported-operations/` |
| Cloud Profiles（`c8y.enable.*`） | `https://thin-edge.github.io/thin-edge.io/operate/c8y/cloud-profiles/` |
| Availability Monitoring | `https://thin-edge.github.io/thin-edge.io/operate/c8y/device-availability/` |
| Health Monitoring / systemd watchdog | `https://thin-edge.github.io/thin-edge.io/operate/c8y/health-monitoring/` , `/operate/monitoring/systemd-watchdog/` |
| Cumulocity Proxy | `https://thin-edge.github.io/thin-edge.io/references/cumulocity-proxy/` |
| ファイルアップロード（`tedge upload c8y`） | `https://thin-edge.github.io/thin-edge.io/operate/c8y/upload-files/` |
| **証明書管理（EST / 自動更新）** | `https://thin-edge.github.io/thin-edge.io/references/certificate-management/` |
| クラウド認証・CA 配置 | `https://thin-edge.github.io/thin-edge.io/operate/security/cloud-authentication/` |
| **設定可能ブリッジ（built-in bridge）** | `https://thin-edge.github.io/thin-edge.io/references/mappers/configurable-bridge/` |
| sm-plugin API | `https://thin-edge.github.io/thin-edge.io/references/software-management-plugin-api/` |
| 設定管理 | `https://thin-edge.github.io/thin-edge.io/references/agent/tedge-configuration-management/` , `/extend/config-management/` |
| ログ管理 | `https://thin-edge.github.io/thin-edge.io/references/agent/tedge-log-management/` |
| オペレーションワークフロー | `https://thin-edge.github.io/thin-edge.io/references/agent/operation-workflow/` |
| Remote Access | `https://thin-edge.github.io/thin-edge.io/operate/c8y/remote-access/` |
| Flows（2.0 の新機能） | `https://thin-edge.github.io/thin-edge.io/extend/flows/` |
| リリースノート 2.0.0（**built-in bridge 既定化**） | `https://github.com/thin-edge/thin-edge.io/releases/tag/2.0.0` |

> ⚠️ **ドキュメントの既知の不備**（調査で判明）:
> - `references/cli/tedge-cert.md` / `tedge-connect.md` は**内容が古く**、`tedge cert download` / `renew` / `needs-renewal` が載っていません
> - `references/mappers/configurable-bridge/` は **キー名を `mqtt.bridge.built-in` と誤記**（正しくは `built_in`）し、**既定値の記述もありません**
> - **ストア&フォワードに関する記述が一切ありません**（§5.7 はソースコードと mosquitto のマニュアルからの再構成）
> - **`c8y.proxy.ca_path` の既定値がドキュメントとソースで不一致**
>
> **thin-edge.io の設定値を確認するときは、実機で `tedge config list --doc` を正としてください。**

### 外部

- mosquitto.conf(5) — `https://mosquitto.org/man/mosquitto-conf-5.html`（§5.7 のキュー設定）
- Cumulocity Core OpenAPI — `https://cumulocity.com/api/core/dist/c8y-oas.yml`
- Cumulocity Edge 2026 — `https://cumulocity.com/docs/2026/edge/`
